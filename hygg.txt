local WindUI = loadstring(game:HttpGet("https://raw.githubusercontent.com/Footagesus/WindUI/refs/heads/main/dist/main.lua"))()
local BRAND = {name = "hygg脚本", version = "v1.0", author = "hygg", folder = "hyggHub", icon = "zap", theme = "Dark", accent = "#FF6B35"}

local Window = WindUI:CreateWindow({
    Title = BRAND.name,
    Icon = BRAND.icon,
    IconThemed = true,
    Author = BRAND.version,
    Folder = BRAND.folder,
    Size = UDim2.fromOffset(640, 420),
    Transparent = true,
    Theme = BRAND.theme,
    HideSearchBar = true,
    Resizable = true,
    SideBarWidth = 240,
    Background = "rbxassetid://84674071500810",
    BackgroundImageTransparency = 0.5,
    User = {Enabled = true, Callback = function() WindUI:Notify({Title = "玩家信息", Content = game.Players.LocalPlayer.Name, Duration = 2, Icon = "user"}) end},
})

Window:EditOpenButton({
    Title = BRAND.name,
    Icon = BRAND.icon,
    CornerRadius = UDim.new(0, 26),
    StrokeThickness = 4,
    Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromHex("00BFFF")),
        ColorSequenceKeypoint.new(1, Color3.fromHex("8A2BE2")),
    }),
    Draggable = true,
})

task.spawn(function()
    task.wait(0.5)
    pcall(function()
        local mainFrame = Window.Frame or Window.Main or (Window.UIElements and Window.UIElements.Main)
        if not mainFrame and Window.Instance then
            mainFrame = Window.Instance:FindFirstChildWhichIsA("Frame", true)
        end
        if mainFrame then
            local Stroke = Instance.new("UIStroke")
            Stroke.Name = "RainbowBorder"
            Stroke.Thickness = 3.5
            Stroke.Color = Color3.new(1, 1, 1)
            Stroke.LineJoinMode = Enum.LineJoinMode.Round
            Stroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
            Stroke.Parent = mainFrame
            
            local Gradient = Instance.new("UIGradient")
            Gradient.Name = "RainbowGradient"
            Gradient.Color = ColorSequence.new({
                ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)),
                ColorSequenceKeypoint.new(0.16, Color3.fromRGB(255, 255, 0)),
                ColorSequenceKeypoint.new(0.33, Color3.fromRGB(0, 255, 0)),
                ColorSequenceKeypoint.new(0.5, Color3.fromRGB(0, 255, 255)),
                ColorSequenceKeypoint.new(0.66, Color3.fromRGB(0, 0, 255)),
                ColorSequenceKeypoint.new(0.83, Color3.fromRGB(255, 0, 255)),
                ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 0, 0))
            })
            Gradient.Parent = Stroke
            
            local Corner = mainFrame:FindFirstChildOfClass("UICorner")
            if not Corner then
                Corner = Instance.new("UICorner")
                Corner.CornerRadius = UDim.new(0, 12)
                Corner.Parent = mainFrame
            end
            
            local currentAngle = 0
            game:GetService("RunService").RenderStepped:Connect(function(dt)
                if Stroke and Stroke.Parent then
                    currentAngle = (currentAngle + dt * 150) % 360
                    Gradient.Rotation = currentAngle
                end
            end)

            local topbar = mainFrame:FindFirstChild("Topbar") or mainFrame:FindFirstChild("Header") or mainFrame
            local titleObj = nil
            for _, child in ipairs(topbar:GetDescendants()) do
                if child:IsA("TextLabel") and (child.Text == BRAND.name or child.Name:lower():find("title")) then
                    titleObj = child
                    break
                end
            end

            local timeLabel = Instance.new("TextLabel")
            timeLabel.Name = "CustomTimeLabel"
            timeLabel.Parent = topbar
            timeLabel.BackgroundTransparency = 1
            timeLabel.Size = UDim2.new(0, 100, 0, 30)
            timeLabel.Position = UDim2.new(0, 150, 0, 8)
            timeLabel.Font = Enum.Font.GothamBold
            if titleObj then
                timeLabel.TextColor3 = titleObj.TextColor3
            else
                timeLabel.TextColor3 = Color3.fromHex(BRAND.accent)
            end
            timeLabel.TextSize = 14
            timeLabel.TextXAlignment = Enum.TextXAlignment.Left
            timeLabel.ZIndex = 100
            
            task.spawn(function()
                while timeLabel and timeLabel.Parent do
                    timeLabel.Text = os.date("%H:%M:%S")
                    if titleObj then
                        timeLabel.TextColor3 = titleObj.TextColor3
                    end
                    task.wait(1)
                end
            end)
        end
    end)
end)

local okGeneral, loadGeneralTab = pcall(function()
    return loadstring(game:HttpGet("https://hygg3795-debug.github.io/1/通用.txt"))()
end)
if okGeneral and loadGeneralTab then
    loadGeneralTab(Window, WindUI)
else
    WindUI:Notify({Title = "通用模块加载失败", Content = "请检查网络或仓库地址", Duration = 3, Icon = "alert"})
end

local okTeleport, loadTeleportTab = pcall(function()
    return loadstring(game:HttpGet("https://hygg3795-debug.github.io/1/传送.txt"))()
end)
if okTeleport and loadTeleportTab then
    loadTeleportTab(Window, WindUI)
else
    WindUI:Notify({Title = "传送模块加载失败", Content = "请检查网络或仓库地址", Duration = 3, Icon = "alert"})
end

local okPlayer, loadPlayerTab = pcall(function()
    return loadstring(game:HttpGet("https://hygg3795-debug.github.io/1/玩家.txt"))()
end)
if okPlayer and loadPlayerTab then
    loadPlayerTab(Window, WindUI)
else
    WindUI:Notify({Title = "玩家模块加载失败", Content = "请检查网络或仓库地址", Duration = 3, Icon = "alert"})
end

local okBlackhole, loadBlackholeTab = pcall(function()
    return loadstring(game:HttpGet("https://hygg3795-debug.github.io/1/黑洞.txt"))()
end)
if okBlackhole and loadBlackholeTab then
    loadBlackholeTab(Window, WindUI)
else
    WindUI:Notify({Title = "黑洞模块加载失败", Content = "请检查网络或仓库地址", Duration = 3, Icon = "alert"})
end

local okSuper, loadSuperTab = pcall(function()
    return loadstring(game:HttpGet("https://hygg3795-debug.github.io/1/超人.txt"))()
end)
if okSuper and loadSuperTab then
    loadSuperTab(Window, WindUI)
else
    WindUI:Notify({Title = "超人模块加载失败", Content = "请检查网络或仓库地址", Duration = 3, Icon = "alert"})
end

local okAction, loadActionTab = pcall(function()
    return loadstring(game:HttpGet("https://hygg3795-debug.github.io/1/动作.txt"))()
end)
if okAction and loadActionTab then
    loadActionTab(Window, WindUI)
else
    WindUI:Notify({Title = "动作模块加载失败", Content = "请检查网络或仓库地址", Duration = 3, Icon = "alert"})
end

local okLighting, loadLightingTab = pcall(function()
    return loadstring(game:HttpGet("https://hygg3795-debug.github.io/1/光影.lua"))()
end)
if okLighting and loadLightingTab then
    loadLightingTab(Window, WindUI)
else
    WindUI:Notify({Title = "光影模块加载失败", Content = "请检查网络或仓库地址", Duration = 3, Icon = "alert"})
end

_G._LCF_WindUI = WindUI
_G._LCF_TmplWin = Window
print("[" .. BRAND.name .. "] 全新 UI 加载完成 ✅")
