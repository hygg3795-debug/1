local Rayfield = loadstring(game:HttpGet("https://raw.githubusercontent.com/hygg520/hy/refs/heads/main/Rayfield%20UI.txt"))() local Window = Rayfield:CreateWindow({ Name = "hygg脚本 v1.3.4", LoadingTitle = "hygg脚本", LoadingSubtitle = "by hygg", ConfigurationSaving = { Enabled = false, FileName = "hyggConfig" }, Discord = { Enabled = false, Invite = "", RememberJoins = false }, KeySystem = false, Theme = "Dark", Icon = "zap", }) local function Notify(title, content, duration, icon) Rayfield:Notify({ Title = title, Content = content, Duration = duration or 3, Image = icon or 0 }) end (function(Window, Rayfield) local tab = Window:CreateTab("信息", "info") tab:CreateSection("支持的服务器") local serverList = { "鸡场", "自然灾害", "阿拉伯键盘", "超高速跑者", "+1割草冒险", "+1魔法进化", "扔出一枚硬币", "每步+1速度", "每点击+1年龄", "+1斧头挥动逃脱", "+1键盘速度逃脱", "+1速度猴子逃脱", "+1后空翻键盘逃脱", "+1瓶子翻转奥比逃脱", "+1速度银河键盘逃脱", "+1速度忍者键盘逃脱", "+1速度键盘水下逃脱", "+1速度史莱姆键盘逃脱", "+1 High Jump Power Escape", "每次点击增加+1热量" } tab:CreateParagraph({ Title = "支持的服务器", Content = table.concat(serverList, "\n") }) end)(Window, Rayfield) (function(Window, Rayfield) local Players = game:GetService("Players") local LocalPlayer = Players.LocalPlayer local RunService = game:GetService("RunService") local tab = Window:CreateTab("传送", "map-pin") tab:CreateSection("坐标传送") local inputValue = "" local loopConnection = nil tab:CreateInput({ Name = "输入坐标", PlaceholderText = "例如: 100, 200, 300", CurrentValue = "", Callback = function(text) inputValue = text end }) tab:CreateButton({ Name = "传送", Callback = function() if inputValue == "" then Notify("提示", "请先输入坐标", 2, "alert") return end local parts = {} for part in inputValue:gmatch("[^,，%s]+") do local num = tonumber(part) if num then table.insert(parts, num) end end if #parts >= 3 then local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = CFrame.new(Vector3.new(parts[1], parts[2], parts[3])) Notify("传送成功", string.format("已传送到 (%.2f, %.2f, %.2f)", parts[1], parts[2], parts[3]), 2, "check") end else Notify("格式错误", "请输入如: 100,200,300", 2, "alert") end end }) tab:CreateDivider() local coordParagraph = tab:CreateParagraph({ Title = "当前坐标", Content = "X: 0.00  Y: 0.00  Z: 0.00" }) local function updateCoordDisplay() local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then local pos = char.HumanoidRootPart.Position coordParagraph:Set({ Title = "当前坐标", Content = string.format("X: %.2f  Y: %.2f  Z: %.2f", pos.X, pos.Y, pos.Z) }) end end RunService.RenderStepped:Connect(updateCoordDisplay) tab:CreateButton({ Name = "复制当前坐标", Callback = function() local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then local pos = char.HumanoidRootPart.Position local coordText = string.format("%.2f,%.2f,%.2f", pos.X, pos.Y, pos.Z) if setclipboard then setclipboard(coordText) Notify("复制成功", coordText, 2, "check") else Notify("复制失败", "当前环境不支持复制", 2, "x") end else Notify("错误", "未找到角色", 2, "alert") end end }) tab:CreateToggle({ Name = "循环传送", CurrentValue = false, Callback = function(val) if val then if loopConnection then return end loopConnection = RunService.Heartbeat:Connect(function() if inputValue == "" then return end local parts = {} for part in inputValue:gmatch("[^,，%s]+") do local num = tonumber(part) if num then table.insert(parts, num) end end if #parts >= 3 then local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = CFrame.new(Vector3.new(parts[1], parts[2], parts[3])) end end end) else if loopConnection then loopConnection:Disconnect() loopConnection = nil end end end }) tab:CreateSection("快速传送") local function createTeleportToggle(name, coords) tab:CreateToggle({ Name = name, CurrentValue = false, Callback = function(val) if val then local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = CFrame.new(coords) Notify(name, "已传送", 2, "check") end end end }) end createTeleportToggle("传送虚空", Vector3.new(0, 100000000, 0)) end)(Window, Rayfield) (function(Window, Rayfield) local Players = game:GetService("Players") local Workspace = game:GetService("Workspace") local LocalPlayer = Players.LocalPlayer local tab = Window:CreateTab("动作", "sparkles") local active = {} local currentEmoteTrack = nil local LOCAL_ACCESSORY_ATTRIBUTE = "UAC_AssetId" local HAT_LIST = { { id = 90115364269545, name = "香企鹅帽子" }, { id = 114442440891763, name = "贝利亚" }, { id = 74891470, name = "冻犄角" }, { id = 21070012, name = "烈焰之主" }, { id = 48545806, name = "冰寒兜帽" }, { id = 1365767, name = "瓦尔基里头盔" }, } local headlessActive = false local hiddenState = {} local equippedHats = {} local hatToggles = {} local isAdidasActive = false local function clik() local s = Instance.new("Sound") s.SoundId = "rbxassetid://87152549167464" s.Parent = Workspace s.Volume = 1.2 s.TimePosition = 0.1 s:Play() game:GetService("Debris"):AddItem(s, 3) end local ADIDAS_ID_TABLE = { idle = "rbxassetid://126354114956642", walk = "rbxassetid://126354114957006", run = "rbxassetid://82598234841035", jump = "rbxassetid://75290611992385", fall = "rbxassetid://98600215928904", climb = "rbxassetid://126354115004448", swim = "rbxassetid://133308483266208" } local ANIM_NAMES = { idle = {"Animation1", "Animation2"}, walk = {"WalkAnim"}, run = {"RunAnim"}, jump = {"JumpAnim"}, fall = {"FallAnim"}, climb = {"ClimbAnim"}, swim = {"SwimAnim"} } local function applyAdidasPack(char) local hum = char:WaitForChild("Humanoid", 15) if not hum or hum.RigType ~= Enum.HumanoidRigType.R15 then return end local animate = char:WaitForChild("Animate", 15) if not animate then return end animate.Disabled = true local animator = hum:FindFirstChildOfClass("Animator") if animator then for _, track in ipairs(animator:GetPlayingAnimationTracks()) do track:Stop(0) end end for folderName, id in pairs(ADIDAS_ID_TABLE) do local folder = animate:FindFirstChild(folderName) if folder then for _, child in ipairs(folder:GetChildren()) do if child:IsA("Animation") then child:Destroy() end end local names = ANIM_NAMES[folderName] if names then for _, animName in ipairs(names) do local newAnim = Instance.new("Animation") newAnim.Name = animName newAnim.AnimationId = id newAnim.Parent = folder local weight = Instance.new("NumberValue") weight.Name = "Weight" weight.Value = 1 weight.Parent = newAnim end end end end task.wait(0.1) animate.Disabled = false end local function playAnimationPublic(animId, speed, timePos) local player = game.Players.LocalPlayer if not player or not player.Character then return end local character = player.Character local humanoid = character:FindFirstChildOfClass("Humanoid") if not humanoid then return end for _, track in pairs(active) do pcall(function() if track and track.IsPlaying then track:Stop() end end) end active = {} if currentEmoteTrack then pcall(function() if currentEmoteTrack.IsPlaying then currentEmoteTrack:Stop() end end) currentEmoteTrack = nil end local animator = humanoid:FindFirstChildOfClass("Animator") if not animator then animator = Instance.new("Animator") animator.Parent = humanoid end pcall(function() for _, track in ipairs(animator:GetPlayingAnimationTracks()) do track:Stop() end end) local description = humanoid:FindFirstChild("HumanoidDescription") if not description then description = Instance.new("HumanoidDescription") description.Parent = humanoid end local animName = "CustomAnim_" .. tostring(animId) local emoteTable = {} local equipped = {} local existingEquipped = description:GetEquippedEmotes() or {} for _, name in pairs(existingEquipped) do if name ~= animName then table.insert(equipped, name) end end emoteTable[animName] = {animId} table.insert(equipped, animName) description:SetEmotes(emoteTable) description:SetEquippedEmotes(equipped) task.wait(0.05) local ok, track = pcall(function() return humanoid:PlayEmoteAndGetAnimTrackById(animId) end) if ok and track and typeof(track) == "Instance" and track:IsA("AnimationTrack") then currentEmoteTrack = track track.Looped = true track:Play() if speed then track:AdjustSpeed(speed) end if timePos then track.TimePosition = timePos end end local animation = Instance.new("Animation") animation.AnimationId = "rbxassetid://" .. tostring(animId) local track2 = animator:LoadAnimation(animation) if track2 then track2.Looped = true track2.Priority = Enum.AnimationPriority.Action track2:Play() if speed then track2:AdjustSpeed(speed) end if timePos then track2.TimePosition = timePos end active[animId] = track2 end getgenv().LastPlayedAnimation = { id = animId, speed = speed or 1, timePos = timePos or 0 } end local function stopAnimation() clik() if currentEmoteTrack then pcall(function() if currentEmoteTrack.IsPlaying then currentEmoteTrack:Stop() end end) currentEmoteTrack = nil end for animId, track in pairs(active) do pcall(function() if track and track.IsPlaying then track:Stop() end end) end active = {} local player = game.Players.LocalPlayer if player and player.Character then local character = player.Character local humanoid = character:FindFirstChildOfClass("Humanoid") if humanoid then local animator = humanoid:FindFirstChildOfClass("Animator") if animator then pcall(function() for _, track in ipairs(animator:GetPlayingAnimationTracks()) do track:Stop() end end) end pcall(function() humanoid:StopAllAnimations() end) end local animate = character:FindFirstChild("Animate") if animate then animate.Disabled = true task.wait(0.05) animate.Disabled = false end end getgenv().LastPlayedAnimation = nil end local function rememberVisual(instance) if hiddenState[instance] then return end if instance:IsA("BasePart") then hiddenState[instance] = { Transparency = instance.Transparency, LocalTransparencyModifier = instance.LocalTransparencyModifier } elseif instance:IsA("Decal") or instance:IsA("Texture") then hiddenState[instance] = { Transparency = instance.Transparency } end end local function hideVisual(instance) rememberVisual(instance) if instance:IsA("BasePart") then instance.LocalTransparencyModifier = 1 elseif instance:IsA("Decal") or instance:IsA("Texture") then instance.Transparency = 1 end end local function restoreVisual(instance) local state = hiddenState[instance] if not state then return end if instance:IsA("BasePart") then instance.Transparency = state.Transparency instance.LocalTransparencyModifier = state.LocalTransparencyModifier elseif instance:IsA("Decal") or instance:IsA("Texture") then instance.Transparency = state.Transparency end hiddenState[instance] = nil end local function getCharacter() return LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() end local function getHumanoid() local char = getCharacter() return char:FindFirstChildOfClass("Humanoid") or char:WaitForChild("Humanoid", 8) end local function setHeadless(enabled) local character = getCharacter() local head = character:FindFirstChild("Head") if not head then return false end if enabled then hideVisual(head) for _, child in ipairs(head:GetChildren()) do if child:IsA("Decal") or child:IsA("Texture") then hideVisual(child) end end else restoreVisual(head) for _, child in ipairs(head:GetChildren()) do restoreVisual(child) end end return true end local function loadAsset(assetId) local ok, objects = pcall(function() return game:GetObjects("rbxassetid://" .. tostring(assetId)) end) if not ok or not objects or #objects == 0 then return nil end return objects end local function findFirstDescendant(root, predicate) if predicate(root) then return root end for _, child in ipairs(root:GetDescendants()) do if predicate(child) then return child end end return nil end local function findHandleAttachment(handle) for _, child in ipairs(handle:GetChildren()) do if child:IsA("Attachment") then return child end end return nil end local function findCharacterAttachment(character, attachmentName) for _, descendant in ipairs(character:GetDescendants()) do if descendant:IsA("Attachment") and descendant.Name == attachmentName and descendant.Parent:IsA("BasePart") then return descendant end end return nil end local function manualAttachAccessory(accessory) local character = getCharacter() local handle = accessory:FindFirstChild("Handle") if not handle or not handle:IsA("BasePart") then return false end local handleAttachment = findHandleAttachment(handle) if not handleAttachment then return false end local characterAttachment = findCharacterAttachment(character, handleAttachment.Name) if not characterAttachment then return false end accessory.Parent = character handle.Anchored = false handle.CanCollide = false handle.Massless = true handle.LocalTransparencyModifier = 0 handle.CFrame = characterAttachment.Parent.CFrame * characterAttachment.CFrame * handleAttachment.CFrame:Inverse() local oldWeld = handle:FindFirstChild("AccessoryWeld") if oldWeld then oldWeld:Destroy() end local weld = Instance.new("Weld") weld.Name = "AccessoryWeld" weld.Part0 = handle weld.Part1 = characterAttachment.Parent weld.C0 = handleAttachment.CFrame weld.C1 = characterAttachment.CFrame weld.Parent = handle return true end local function addAccessory(assetId) local humanoid = getHumanoid() if not humanoid then return false end local objects = loadAsset(assetId) if not objects then return false end local accessory for _, object in ipairs(objects) do accessory = findFirstDescendant(object, function(candidate) return candidate:IsA("Accessory") end) if accessory then break end end if not accessory then for _, object in ipairs(objects) do object:Destroy() end return false end accessory.Parent = nil accessory:SetAttribute(LOCAL_ACCESSORY_ATTRIBUTE, tostring(assetId)) local ok = pcall(function() humanoid:AddAccessory(accessory) end) local handle = accessory:FindFirstChild("Handle") local hasWeld = handle and handle:FindFirstChild("AccessoryWeld") if ok and hasWeld then return true end local weldOk = manualAttachAccessory(accessory) return weldOk end local function removeAccessory(assetId) local character = getCharacter() local removed = 0 for _, child in ipairs(character:GetChildren()) do if child:IsA("Accessory") and child:GetAttribute(LOCAL_ACCESSORY_ATTRIBUTE) == tostring(assetId) then child:Destroy() removed = removed + 1 end end return removed > 0 end local function toggleHat(assetId) if equippedHats[assetId] then removeAccessory(assetId) equippedHats[assetId] = false return false else local ok = addAccessory(assetId) if ok then equippedHats[assetId] = true return true end return false end end tab:CreateSection("动作列表") local function addAnimButton(name, animId) tab:CreateButton({ Name = name, Callback = function() clik() playAnimationPublic(animId, 1, 0) end }) end addAnimButton("悬浮躺", 77840765435893) addAnimButton("JOJO姿势", 120629563851640) addAnimButton("直升机", 95301257497525) addAnimButton("俄罗斯舞蹈", 97148848007002) addAnimButton("俯卧撑", 108313130500811) addAnimButton("你好臭", 70919402339484) addAnimButton("私人飞行", 120904242187887) addAnimButton("科目三", 125328720114284) addAnimButton("拳击", 76710893141393) tab:CreateButton({ Name = "停止当前动作", Callback = stopAnimation }) tab:CreateSection("R15动画包") tab:CreateButton({ Name = "阿迪达斯动画包", Callback = function() clik() isAdidasActive = true local char = LocalPlayer.Character if char then applyAdidasPack(char) end end }) tab:CreateSection("本地玩家外观") local headlessToggle headlessToggle = tab:CreateToggle({ Name = "无头", CurrentValue = false, Callback = function(val) clik() headlessActive = val setHeadless(val) end }) for _, hat in ipairs(HAT_LIST) do local toggle = tab:CreateToggle({ Name = hat.name, CurrentValue = false, Callback = function(val) clik() local equipped = toggleHat(hat.id) if equipped ~= val then toggle:Set(equipped) end end }) hatToggles[hat.id] = toggle end tab:CreateButton({ Name = "重置所有外观", Callback = function() clik() if headlessActive then headlessActive = false setHeadless(false) headlessToggle:Set(false) end for _, hat in ipairs(HAT_LIST) do if equippedHats[hat.id] then removeAccessory(hat.id) equippedHats[hat.id] = false local toggle = hatToggles[hat.id] if toggle then toggle:Set(false) end end end end }) LocalPlayer.CharacterAdded:Connect(function(char) task.wait(1) if isAdidasActive then applyAdidasPack(char) end if headlessActive then setHeadless(true) end for _, hat in ipairs(HAT_LIST) do if equippedHats[hat.id] then task.wait(0.2) addAccessory(hat.id) end end end) end)(Window, Rayfield) (function(Window, Rayfield) local Players = game:GetService("Players") local RunService = game:GetService("RunService") local ReplicatedStorage = game:GetService("ReplicatedStorage") local Workspace = game:GetService("Workspace") local MarketplaceService = game:GetService("MarketplaceService") local LocalPlayer = Players.LocalPlayer local tab = Window:CreateTab("服务器功能", "server") do local config = { eggEnabled = false, eggRadius = 40, eggAutoDeposit = true, eggDepositInterval = 60, cashEnabled = false, cashDelay = 0.5, sellerEnabled = false, sellerDelay = 0.5, buyEnabled = false, buyAmount = 1, buyDelay = 0.5, mergeEnabled = false, mergeDelay = 0.5, } local processedEggs = {} local eggRunning = false local cashRunning = false local sellerRunning = false local buyRunning = false local mergeRunning = false local lastDepositTime = 0 local lastBuyTime = 0 local lastMergeTime = 0 local isDepositing = false local sellerNoMoney = false local buyNoMoney = false local mergeNoChickens = false local CollectEvent = nil local RemoteFunction = nil local Paper = ReplicatedStorage:FindFirstChild("Paper") if Paper then local Remotes = Paper:FindFirstChild("Remotes") if Remotes then CollectEvent = Remotes:FindFirstChild("__remoteevent") RemoteFunction = Remotes:FindFirstChild("__remotefunction") end end if CollectEvent then local function getRootPart() local char = LocalPlayer.Character return char and char:FindFirstChild("HumanoidRootPart") end local function getPlayerPosition() local root = getRootPart() return root and root.Position end local function findNearbyEggs() local eggs = {} local pos = getPlayerPosition() if not pos then return eggs end local eggsContainer = Workspace:FindFirstChild("Eggs") if not eggsContainer then return eggs end for _, obj in ipairs(eggsContainer:GetChildren()) do if obj:IsA("Model") then local eggId = obj.Name local tier = obj:GetAttribute("Tier") if tier and not processedEggs[eggId] then local eggPos = nil local primary = obj:FindFirstChild("PrimaryPart") if primary then eggPos = primary.Position end if not eggPos then local root = obj:FindFirstChild("HumanoidRootPart") if root then eggPos = root.Position end end if not eggPos then eggPos = obj:GetPivot().Position end if eggPos then local distance = (eggPos - pos).Magnitude if distance <= config.eggRadius then table.insert(eggs, {Object = obj, Id = eggId, Position = eggPos, Distance = distance}) end end end end end table.sort(eggs, function(a,b) return a.Distance < b.Distance end) return eggs end local function teleportToEgg(pos) local root = getRootPart() if not root then return false end pcall(function() root.CFrame = CFrame.new(Vector3.new(pos.X, pos.Y + 3, pos.Z)) end) task.wait(0.2) return true end local function collectEgg(egg) if not egg or not egg.Id or processedEggs[egg.Id] then return false end processedEggs[egg.Id] = true pcall(function() CollectEvent:FireServer("Collect Egg", egg.Id) end) return true end local function depositEggs() if not RemoteFunction or isDepositing then return false end isDepositing = true local success = false pcall(function() local result = RemoteFunction:InvokeServer("Deposit Eggs") success = type(result) == "table" and (result[1] or false) or (result or false) end) isDepositing = false if success then lastDepositTime = tick() end return success end local function collectCash() if not RemoteFunction then return false end local success = false pcall(function() local result = RemoteFunction:InvokeServer("Collect Cash") success = type(result) == "table" and (result[1] or false) or (result or false) end) return success end local function upgradeSeller() if not RemoteFunction then return false end local success = false local message = "" pcall(function() local result = RemoteFunction:InvokeServer("Upgrade Process Level") success = type(result) == "table" and (result[1] or false) or (result or false) message = type(result) == "table" and (result[2] or "") or "" end) if not success and message:find("need") or message:find("afford") then sellerNoMoney = true end return success end local function buyChickens() if not RemoteFunction then return false end local now = tick() if now - lastBuyTime < config.buyDelay then return false end local success = false local message = "" pcall(function() local result = RemoteFunction:InvokeServer("Buy Chickens", config.buyAmount) success = type(result) == "table" and (result[1] or false) or (result or false) message = type(result) == "table" and (result[2] or "") or "" end) lastBuyTime = now if not success and (message:find("need") or message:find("afford") or message:find("money")) then buyNoMoney = true end return success end local function mergeChickens() if not RemoteFunction then return false end local now = tick() if now - lastMergeTime < config.mergeDelay then return false end local success = false local message = "" pcall(function() local result = RemoteFunction:InvokeServer("Merge Chickens") success = type(result) == "table" and (result[1] or false) or (result or false) message = type(result) == "table" and (result[2] or "") or "" end) lastMergeTime = now if not success and (message:find("need") or message:find("enough") or message:find("chicken")) then mergeNoChickens = true end return success end local function eggLoop() local noEggsCount = 0 lastDepositTime = tick() while eggRunning do task.wait(0.1) if not config.eggEnabled then task.wait(0.5); continue end if config.eggAutoDeposit and tick() - lastDepositTime >= config.eggDepositInterval then depositEggs() end local eggs = findNearbyEggs() if #eggs > 0 then noEggsCount = 0 for _, egg in ipairs(eggs) do if not eggRunning or not config.eggEnabled then break end if not egg.Object or not egg.Object.Parent then continue end teleportToEgg(egg.Position) task.wait(0.2) collectEgg(egg) task.wait(0.2) end task.wait(1) else noEggsCount = noEggsCount + 1 if noEggsCount >= 3 and config.eggAutoDeposit then depositEggs() noEggsCount = 0 task.wait(2) else task.wait(2) end end end end local function cashLoop() while cashRunning do task.wait(0.1) if not config.cashEnabled then task.wait(0.5); continue end collectCash() task.wait(config.cashDelay) end end local function sellerLoop() sellerNoMoney = false while sellerRunning do task.wait(0.1) if not config.sellerEnabled or sellerNoMoney then if sellerNoMoney then task.wait(5) end task.wait(0.5); continue end upgradeSeller() task.wait(config.sellerDelay) end end local function buyLoop() buyNoMoney = false lastBuyTime = 0 while buyRunning do task.wait(0.1) if not config.buyEnabled or buyNoMoney then if buyNoMoney then task.wait(5) end task.wait(0.5); continue end buyChickens() task.wait(config.buyDelay) end end local function mergeLoop() mergeNoChickens = false lastMergeTime = 0 while mergeRunning do task.wait(0.1) if not config.mergeEnabled or mergeNoChickens then if mergeNoChickens then task.wait(5) end task.wait(0.5); continue end mergeChickens() task.wait(config.mergeDelay) end end local function toggleEgg(state) config.eggEnabled = state if state then lastDepositTime = tick() if not eggRunning then eggRunning = true; task.spawn(eggLoop) end end end local function toggleCash(state) config.cashEnabled = state if state and not cashRunning then cashRunning = true; task.spawn(cashLoop) end end local function toggleSeller(state) config.sellerEnabled = state if state and not sellerRunning then sellerRunning = true; sellerNoMoney = false; task.spawn(sellerLoop) end end local function toggleBuy(state) config.buyEnabled = state if state then buyNoMoney = false if not buyRunning then buyRunning = true; task.spawn(buyLoop) end end end local function toggleMerge(state) config.mergeEnabled = state if state then mergeNoChickens = false if not mergeRunning then mergeRunning = true; task.spawn(mergeLoop) end end end tab:CreateSection("鸡场") tab:CreateToggle({ Name = "自动捡蛋", CurrentValue = false, Callback = toggleEgg }) tab:CreateSlider({ Name = "捡蛋范围", Range = {10, 80}, CurrentValue = 40, Increment = 1, Callback = function(v) config.eggRadius = math.floor(v) end }) tab:CreateToggle({ Name = "自动存蛋", CurrentValue = true, Callback = function(s) config.eggAutoDeposit = s; if s then lastDepositTime = tick() end end }) tab:CreateSlider({ Name = "存蛋间隔", Range = {10, 300}, CurrentValue = 60, Increment = 1, Callback = function(v) config.eggDepositInterval = math.floor(v) end }) tab:CreateButton({ Name = "手动存蛋", Callback = depositEggs }) tab:CreateDivider() tab:CreateToggle({ Name = "自动收钱", CurrentValue = false, Callback = toggleCash }) tab:CreateSlider({ Name = "收钱间隔", Range = {0.1, 2}, CurrentValue = 0.5, Increment = 0.1, Callback = function(v) config.cashDelay = v end }) tab:CreateDivider() tab:CreateToggle({ Name = "自动升级出售", CurrentValue = false, Callback = toggleSeller }) tab:CreateSlider({ Name = "升级间隔", Range = {0.2, 2}, CurrentValue = 0.5, Increment = 0.1, Callback = function(v) config.sellerDelay = v end }) tab:CreateDivider() tab:CreateToggle({ Name = "自动买鸡", CurrentValue = false, Callback = toggleBuy }) tab:CreateSlider({ Name = "买鸡数量", Range = {1, 100}, CurrentValue = 1, Increment = 1, Callback = function(v) config.buyAmount = math.floor(v) end }) tab:CreateSlider({ Name = "买鸡间隔", Range = {0.2, 2}, CurrentValue = 0.5, Increment = 0.1, Callback = function(v) config.buyDelay = v end }) tab:CreateToggle({ Name = "自动合鸡", CurrentValue = false, Callback = toggleMerge }) tab:CreateSlider({ Name = "合鸡间隔", Range = {0.2, 2}, CurrentValue = 0.5, Increment = 0.1, Callback = function(v) config.mergeDelay = v end }) end end do local disasterFeatures = {waterWalk = false, rocksSolid = false, removeDisasterUI = false, removeFlood = false, mapVote = false, autoSurvive = false} local disasterConnections = {waterFollow = nil, floodFollow = nil, disasterUI = nil, autoTp = nil} local floodPlatform = nil local floodPart = nil local floodEnabled = false local function toggleWaterWalk(val) disasterFeatures.waterWalk = val local water = Workspace:FindFirstChild("WaterLevel") if not water then return end if val then water.CanCollide = true water.Size = Vector3.new(1000, 1, 1000) if disasterConnections.waterFollow then disasterConnections.waterFollow:Disconnect() end disasterConnections.waterFollow = RunService.Heartbeat:Connect(function() local char = LocalPlayer.Character local root = char and char:FindFirstChild("HumanoidRootPart") if root and water then water.Position = Vector3.new(root.Position.X, water.Position.Y, root.Position.Z) end end) else water.Size = Vector3.new(10, 1, 10) water.CanCollide = false if disasterConnections.waterFollow then disasterConnections.waterFollow:Disconnect() disasterConnections.waterFollow = nil end end end local function toggleRocksSolid(val) disasterFeatures.rocksSolid = val for _, v in ipairs(Workspace:GetDescendants()) do if v.Name == "LowerRocks" and v:IsA("BasePart") then v.CanCollide = val end end end local function toggleRemoveDisasterUI(val) disasterFeatures.removeDisasterUI = val local function removeDisasterGuis() local pgui = LocalPlayer:FindFirstChild("PlayerGui") if not pgui then return end local sand = pgui:FindFirstChild("SandStormGui") if sand then sand:Destroy() end local blizzard = pgui:FindFirstChild("BlizzardGui") if blizzard then blizzard:Destroy() end end if val then removeDisasterGuis() local pgui = LocalPlayer:WaitForChild("PlayerGui") disasterConnections.disasterUI = pgui.ChildAdded:Connect(function(child) if child.Name == "SandStormGui" or child.Name == "BlizzardGui" then task.wait() child:Destroy() end end) else if disasterConnections.disasterUI then disasterConnections.disasterUI:Disconnect() disasterConnections.disasterUI = nil end end end local function createFloodPlatform() if floodPlatform then return end floodPlatform = Instance.new("Part") floodPlatform.Name = "FloodPlatform" floodPlatform.Size = Vector3.new(10000, 0.5, 10000) floodPlatform.CanCollide = true floodPlatform.Anchored = true floodPlatform.Transparency = 0.2 floodPlatform.BrickColor = BrickColor.new("Bright blue") floodPlatform.Material = Enum.Material.SmoothPlastic floodPlatform.Parent = Workspace end local function findFlood() local structure = Workspace:FindFirstChild("Structure") if structure then floodPart = structure:FindFirstChild("FloodLevel") end if not floodPart then floodPart = Workspace:FindFirstChild("FloodLevel") end return floodPart end local function updateFloodPlatform() if not floodPlatform or not floodPart then return end local char = LocalPlayer.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end floodPlatform.Position = Vector3.new(root.Position.X, floodPart.Position.Y + 0.5, root.Position.Z) end local function startFlood() if floodEnabled then return end createFloodPlatform() findFlood() if disasterConnections.floodFollow then disasterConnections.floodFollow:Disconnect() end disasterConnections.floodFollow = RunService.Heartbeat:Connect(updateFloodPlatform) floodEnabled = true end local function stopFlood() floodEnabled = false if disasterConnections.floodFollow then disasterConnections.floodFollow:Disconnect() disasterConnections.floodFollow = nil end if floodPlatform then floodPlatform:Destroy() floodPlatform = nil end floodPart = nil end local function toggleRemoveFlood(val) if val then startFlood() else stopFlood() end end local function toggleMapVote(val) disasterFeatures.mapVote = val local playerGui = LocalPlayer:FindFirstChild("PlayerGui") local mainGui = playerGui and playerGui:FindFirstChild("MainGui") local mapPage = mainGui and mainGui:FindFirstChild("MapVotePage") if mapPage then mapPage.Visible = val end end local function toggleAutoSurvive(val) disasterFeatures.autoSurvive = val if val then disasterConnections.autoTp = RunService.Stepped:Connect(function() if LocalPlayer and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-273, 179.5, 394) end end) else if disasterConnections.autoTp then disasterConnections.autoTp:Disconnect() disasterConnections.autoTp = nil end end end tab:CreateSection("自然灾害") tab:CreateToggle({ Name = "水上行走", CurrentValue = false, Callback = toggleWaterWalk }) tab:CreateToggle({ Name = "岩石实体化", CurrentValue = false, Callback = toggleRocksSolid }) tab:CreateToggle({ Name = "移除灾害视角", CurrentValue = false, Callback = toggleRemoveDisasterUI }) tab:CreateToggle({ Name = "去除洪水", CurrentValue = false, Callback = toggleRemoveFlood }) tab:CreateToggle({ Name = "地图投票", CurrentValue = false, Callback = toggleMapVote }) tab:CreateToggle({ Name = "自动存活", CurrentValue = false, Callback = toggleAutoSurvive }) tab:CreateSection("传送") tab:CreateButton({ Name = "传送出生岛", Callback = function() local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = CFrame.new(-247.14, 180.45, 309.40) Notify("传送出生岛", "已传送", 2, "check") end end }) tab:CreateButton({ Name = "传送岛屿", Callback = function() local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = CFrame.new(-104.44, 48.65, 13.03) Notify("传送岛屿", "已传送", 2, "check") end end }) tab:CreateButton({ Name = "传送虚空", Callback = function() local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = CFrame.new(0, 100000000, 0) Notify("传送虚空", "已传送", 2, "check") end end }) end do local autoSellActive = false local removeGrassActive = false tab:CreateSection("+1割草冒险") tab:CreateToggle({ Name = "自动出售", CurrentValue = false, Callback = function(state) autoSellActive = state if state then task.spawn(function() while autoSellActive do task.wait(0.2) for _, container in ipairs({ReplicatedStorage, LocalPlayer:FindFirstChild("PlayerGui"), Workspace}) do if container then for _, remote in ipairs(container:GetDescendants()) do if remote:IsA("RemoteEvent") or remote:IsA("RemoteFunction") then local rname = string.lower(remote.Name) if string.find(rname, "sell") or string.find(rname, "sellall") or string.find(rname, "harvest") then pcall(function() if remote:IsA("RemoteEvent") then remote:FireServer("All") remote:FireServer(true) remote:FireServer() else remote:InvokeServer("All") end end) end end end end end end end) end end }) tab:CreateToggle({ Name = "清除草地", CurrentValue = false, Callback = function(state) removeGrassActive = state if state then task.spawn(function() while removeGrassActive do task.wait(0.1) local char = LocalPlayer.Character local root = char and char:FindFirstChild("HumanoidRootPart") for _, obj in ipairs(Workspace:GetDescendants()) do if obj:IsA("BasePart") then local pname = string.lower(obj.Name) local parentName = string.lower(obj.Parent.Name) if string.find(pname, "grass") or string.find(parentName, "grass") then pcall(function() if obj:FindFirstChildOfClass("TouchTransmitter") and root and firetouchinterest then firetouchinterest(obj, root, 0) task.wait() firetouchinterest(obj, root, 1) end obj:Destroy() end) end end end for _, container in ipairs({ReplicatedStorage, LocalPlayer:FindFirstChild("PlayerGui"), Workspace}) do if container then for _, item in ipairs(container:GetDescendants()) do if item:IsA("RemoteEvent") or item:IsA("RemoteFunction") then local lname = string.lower(item.Name) if string.find(lname, "cut") or string.find(lname, "swing") or string.find(lname, "grass") or string.find(lname, "remove") then pcall(function() if item:IsA("RemoteEvent") then item:FireServer() item:FireServer(true) else item:InvokeServer() end end) end end end end end end end) end end }) end do local autoWinActive = false local autoWinConnection = nil local autoWinCoords = { x = 3568.0078125, y = 1.9449999332427979, z = 8.189765930175781 } local autoRebirthActive = false local autoRebirthConnection = nil local autoClickerActive = false local autoClickerConnection = nil local godModeActive = false local godModeConnection = nil local godModeCharAddedConn = nil local RebirthRemote = nil local GainMagicPower = nil task.spawn(function() local remotesFolder = ReplicatedStorage:FindFirstChild("Remotes") if remotesFolder then RebirthRemote = remotesFolder:FindFirstChild("Rebirth") GainMagicPower = remotesFolder:FindFirstChild("GainMagicPower") end if not RebirthRemote then RebirthRemote = ReplicatedStorage:FindFirstChild("Rebirth", true) end if not GainMagicPower then GainMagicPower = ReplicatedStorage:FindFirstChild("GainMagicPower", true) end end) local function ApplyGodmode(char) if not char or not godModeActive then return end local humanoid = char:FindFirstChild("Humanoid") if not humanoid then return end pcall(function() humanoid.MaxHealth = 1e8 humanoid.Health = humanoid.MaxHealth end) end tab:CreateSection("+1魔法进化") tab:CreateToggle({ Name = "自动点击", CurrentValue = false, Callback = function(val) autoClickerActive = val if autoClickerConnection then autoClickerConnection:Disconnect(); autoClickerConnection = nil end if val then autoClickerConnection = RunService.Heartbeat:Connect(function() if GainMagicPower then GainMagicPower:FireServer() end end) end end }) tab:CreateToggle({ Name = "无限胜利", CurrentValue = false, Callback = function(val) autoWinActive = val if autoWinConnection then autoWinConnection:Disconnect(); autoWinConnection = nil end if val then autoWinConnection = RunService.Heartbeat:Connect(function() local character = LocalPlayer.Character local hrp = character and character:FindFirstChild("HumanoidRootPart") if hrp then hrp.CFrame = CFrame.new(autoWinCoords.x, autoWinCoords.y, autoWinCoords.z) end end) end end }) tab:CreateToggle({ Name = "自动重生", CurrentValue = false, Callback = function(val) autoRebirthActive = val if autoRebirthConnection then autoRebirthConnection = nil end if val then autoRebirthConnection = task.spawn(function() while autoRebirthActive do if RebirthRemote then RebirthRemote:FireServer() end task.wait(1) end end) end end }) tab:CreateToggle({ Name = "无敌模式", CurrentValue = false, Callback = function(val) godModeActive = val if godModeConnection then godModeConnection:Disconnect(); godModeConnection = nil end if godModeCharAddedConn then godModeCharAddedConn:Disconnect(); godModeCharAddedConn = nil end if val then godModeConnection = RunService.Stepped:Connect(function() pcall(function() local character = LocalPlayer.Character if not character then return end local humanoid = character:FindFirstChildOfClass("Humanoid") if humanoid then if humanoid.MaxHealth < 1e8 then humanoid.MaxHealth = 1e8 end if humanoid.Health < humanoid.MaxHealth then humanoid.Health = humanoid.MaxHealth end end end) end) godModeCharAddedConn = LocalPlayer.CharacterAdded:Connect(function(newChar) task.wait(1.2) ApplyGodmode(newChar) end) ApplyGodmode(LocalPlayer.Character) else local character = LocalPlayer.Character local humanoid = character and character:FindFirstChildOfClass("Humanoid") if humanoid then humanoid.MaxHealth = 100 humanoid.Health = 100 end end end }) end do local coinList = {"Paradox Coin", "Lucky Coin", "Golden Coin", "Obsidian Coin", "Platinum Coin", "Ruby Coin", "Emerald Coin", "Amethyst Coin", "Topaz Coin", "Diamond Coin", "Staff Token", "VIP Token", "Developer Token", "Diamond Token"} local throwRemote = nil local sellRemote = nil local upgradeRemote = nil local function getCoinName() local pg = LocalPlayer:FindFirstChild("PlayerGui") if not pg then return nil end local coinLabel = pg:FindFirstChild("UiFolder") and pg.UiFolder:FindFirstChild("Main") and pg.UiFolder.Main:FindFirstChild("HUD") and pg.UiFolder.Main.HUD:FindFirstChild("Coin") and pg.UiFolder.Main.HUD.Coin:FindFirstChild("Main") and pg.UiFolder.Main.HUD.Coin.Main:FindFirstChild("CoinName") return coinLabel and coinLabel:IsA("TextLabel") and coinLabel.Text ~= "" and coinLabel.Text or nil end local function getMultiplier() local pg = LocalPlayer:FindFirstChild("PlayerGui") if not pg then return 0 end local multBar = pg:FindFirstChild("UiFolder") and pg.UiFolder:FindFirstChild("Main") and pg.UiFolder.Main:FindFirstChild("HUD") and pg.UiFolder.Main.HUD:FindFirstChild("ThrowBar") and pg.UiFolder.Main.HUD.ThrowBar:FindFirstChild("CurrentMulti") return multBar and multBar:IsA("Frame") and multBar.Size.X.Scale * 3 or 0 end local function getHRP() local char = LocalPlayer.Character return char and char:FindFirstChild("HumanoidRootPart") end local function scanRemotes() for _, item in ipairs(ReplicatedStorage:GetDescendants()) do if item:IsA("RemoteEvent") or item:IsA("RemoteFunction") then local name = string.lower(item.Name) if string.find(name, "throw") or string.find(name, "toss") or string.find(name, "coin") then if not throwRemote then throwRemote = item end elseif string.find(name, "sell") then if not sellRemote then sellRemote = item end elseif string.find(name, "upgrade") or string.find(name, "buy") then if not upgradeRemote then upgradeRemote = item end end end end end pcall(scanRemotes) local autoThrowActive = false local throwMultiplier = 3 local autoSellActive = false local autoBuyActive = false local autoUpgradeLuck = false local autoUpgradeValue = false task.spawn(function() while true do task.wait(0.2) if autoThrowActive and throwRemote then local currentMult = getMultiplier() if currentMult >= throwMultiplier then local coinName = getCoinName() if not coinName then continue end local hrp = getHRP() if not hrp then continue end local pos = hrp.Position + Vector3.new(0, -0.5, -2) pcall(function() throwRemote:FireServer(throwMultiplier, pos, coinName, nil, nil) end) end end end end) task.spawn(function() while true do task.wait(0.5) if autoSellActive and sellRemote then pcall(function() sellRemote:FireServer() end) task.wait(2) end end end) task.spawn(function() while true do task.wait(1.0) if (not autoUpgradeLuck and not autoUpgradeValue) then continue end if upgradeRemote then if autoUpgradeLuck then pcall(function() upgradeRemote:FireServer("Luck Multiplier") end) end if autoUpgradeValue then pcall(function() upgradeRemote:FireServer("Value Multiplier") end) end task.wait(1.5) end end end) task.spawn(function() while true do task.wait(1.0) if not autoBuyActive then continue end if not getCoinName() then local buyRemote = ReplicatedStorage:FindFirstChild("Assets") and ReplicatedStorage.Assets:FindFirstChild("Events") and ReplicatedStorage.Assets.Events:FindFirstChild("BuyCoin") if buyRemote then for _, name in ipairs(coinList) do pcall(function() buyRemote:FireServer(name) end) task.wait(0.1) end task.wait(3) end end end end) tab:CreateSection("扔出一枚硬币") tab:CreateToggle({ Name = "自动投币", CurrentValue = false, Callback = function(s) autoThrowActive = s end }) tab:CreateSlider({ Name = "投币倍率", Range = {1, 3}, CurrentValue = 3, Increment = 0.1, Callback = function(v) throwMultiplier = v end }) tab:CreateDivider() tab:CreateToggle({ Name = "自动出售", CurrentValue = false, Callback = function(s) autoSellActive = s end }) tab:CreateDivider() tab:CreateToggle({ Name = "自动购买硬币", CurrentValue = false, Callback = function(s) autoBuyActive = s end }) tab:CreateDivider() tab:CreateSection("自动升级") tab:CreateToggle({ Name = "升级（运气）", CurrentValue = false, Callback = function(s) autoUpgradeLuck = s end }) tab:CreateToggle({ Name = "升级（价值）", CurrentValue = false, Callback = function(s) autoUpgradeValue = s end }) end do local spinsLock = false local spinsLockConn = nil tab:CreateSection("每点击+1年龄") tab:CreateToggle({ Name = "锁定转盘次数5", CurrentValue = false, Callback = function(state) spinsLock = state if state then local spins = LocalPlayer:FindFirstChild("Spins") if spins then spins.Value = 5 end spinsLockConn = spins:GetPropertyChangedSignal("Value"):Connect(function() if spinsLock and spins.Value ~= 5 then spins.Value = 5 end end) else if spinsLockConn then spinsLockConn:Disconnect(); spinsLockConn = nil end end end }) end do tab:CreateSection("+1键盘速度逃脱") tab:CreateButton({ Name = "解锁音效", Callback = function() local ids = {1928718370, 1724877043} for _, id in ipairs(ids) do pcall(function() MarketplaceService:SignalPromptGamePassPurchaseFinished(LocalPlayer, id, true) end) pcall(function() MarketplaceService:SignalPromptPurchaseFinished(LocalPlayer, id, true) end) pcall(function() MarketplaceService:SignalPromptProductPurchaseFinished(LocalPlayer, id, true) end) end if hookfunction then local oldOwns oldOwns = hookfunction(MarketplaceService.UserOwnsGamePassAsync, function(self, p, passId) if typeof(passId) == "number" and table.find(ids, passId) and p == LocalPlayer then return true end return oldOwns(self, p, passId) end) end end }) end do local targetPosition1 = Vector3.new(1322, 199, -4761) local targetPosition2 = Vector3.new(-1340.25, 249.68, 6438.55) local delayBetweenTeleports = 0.1 local autoFarmActive1 = false local autoFarmActive2 = false local teleportLoopConnection = nil local function startTeleportLoop() if teleportLoopConnection then return end teleportLoopConnection = RunService.Heartbeat:Connect(function() local char = LocalPlayer.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end if autoFarmActive1 then root.CFrame = CFrame.new(targetPosition1) elseif autoFarmActive2 then root.CFrame = CFrame.new(targetPosition2) end task.wait(delayBetweenTeleports) end) end local function stopTeleportLoop() if teleportLoopConnection then teleportLoopConnection:Disconnect() teleportLoopConnection = nil end end tab:CreateSection("+1速度史莱姆键盘逃脱") local toggle1 = tab:CreateToggle({ Name = "自动胜利世界1", CurrentValue = false, Callback = function(val) if val and autoFarmActive2 then autoFarmActive2 = false toggle2:Set(false) end autoFarmActive1 = val if autoFarmActive1 or autoFarmActive2 then startTeleportLoop() else stopTeleportLoop() end end }) local toggle2 = tab:CreateToggle({ Name = "自动胜利世界2", CurrentValue = false, Callback = function(val) if val and autoFarmActive1 then autoFarmActive1 = false toggle1:Set(false) end autoFarmActive2 = val if autoFarmActive1 or autoFarmActive2 then startTeleportLoop() else stopTeleportLoop() end end }) tab:CreateButton({ Name = "解锁音效", Callback = function() local ids = {1836201958} for _, id in ipairs(ids) do pcall(function() MarketplaceService:SignalPromptGamePassPurchaseFinished(LocalPlayer, id, true) end) pcall(function() MarketplaceService:SignalPromptPurchaseFinished(LocalPlayer, id, true) end) pcall(function() MarketplaceService:SignalPromptProductPurchaseFinished(LocalPlayer, id, true) end) end if hookfunction then local oldOwns oldOwns = hookfunction(MarketplaceService.UserOwnsGamePassAsync, function(self, p, passId) if typeof(passId) == "number" and table.find(ids, passId) and p == LocalPlayer then return true end return oldOwns(self, p, passId) end) end end }) end end)(Window, Rayfield) (function(Window, Rayfield) local Players = game:GetService("Players") local RunService = game:GetService("RunService") local LocalPlayer = Players.LocalPlayer local tab = Window:CreateTab("服务器自动胜利", "trophy") local gameConfigs = { ["+1速度猴子逃脱"] = { type = "multi", positions = { Vector3.new(-9460.94, 389.29, -256.31), Vector3.new(-3604.62, 154.21, -9378.90) }, baseY = 389.29, minOffset = -3, maxOffset = 4, speed = 0.5 }, ["阿拉伯键盘"] = { type = "single", pos = Vector3.new(-7344.6, 1877.7, 1379.7), baseY = 1877.7, minOffset = -3, maxOffset = 4, speed = 0.5 }, ["超高速跑者"] = { type = "single", pos = Vector3.new(0, 6.5, -9076), baseY = 1, minOffset = -3, maxOffset = 4, speed = 0.5 }, ["每步+1速度"] = { type = "single", pos = Vector3.new(8294.85, 201.73, -7598.20), baseY = 1, minOffset = -3, maxOffset = 4, speed = 10 }, ["+1斧头挥动逃脱"] = { type = "single", pos = Vector3.new(1769.53, 6.11, 1222.30), baseY = 1, minOffset = -3, maxOffset = 4, speed = 0.5 }, ["+1速度键盘水下逃脱"] = { type = "multi", positions = { Vector3.new(-4748.62, 21.67, 1521.73), Vector3.new(434.57, -132.81, 6401.16) }, baseY = 21.67, minOffset = -2, maxOffset = 2, speed = 5 }, ["+1速度忍者键盘逃脱"] = { type = "multi", positions = { Vector3.new(392.24, 19.03, -8619.16), Vector3.new(-4.00, 70, -9038.46), Vector3.new(191.00, 475.00, -12475.71) }, baseY = 19.03, minOffset = -3, maxOffset = 4, speed = 0.5 }, ["+1速度银河键盘逃脱"] = { type = "single", pos = Vector3.new(3961.57, -4.96, -3721.73), baseY = -4.96, minOffset = -3, maxOffset = 4, speed = 0.5 }, ["每次点击增加+1热量"] = { type = "single", pos = Vector3.new(-1350.55, 19.98, 1756.15), baseY = 19.98, minOffset = -3, maxOffset = 4, speed = 0.5 }, ["+1后空翻键盘逃脱"] = { type = "multi", positions = { Vector3.new(-6510.57, 276.12, -15755.79), Vector3.new(-32655.05, -1754.57, 50199.60), Vector3.new(-27238.17, 1662.17, 76625.84) }, baseY = 276.12, minOffset = -3, maxOffset = 4, speed = 0.5 }, ["+1 High Jump Power Escape"] = { type = "multi", positions = { Vector3.new(907.21, 9028.27, -41.58), Vector3.new(676.70, 32728.68, -114789.85) }, baseY = 9028.27, minOffset = -3, maxOffset = 4, speed = 0.5 }, ["+1瓶子翻转奥比逃脱"] = { type = "single", pos = Vector3.new(113.79, 13.92, 9502.78), baseY = 13.92, minOffset = -3, maxOffset = 4, speed = 0.5 } } local connections = {} local activeStates = {} local yPositions = {} local dirs = {} local function stopGame(gameName) if connections[gameName] then connections[gameName]:Disconnect() connections[gameName] = nil end activeStates[gameName] = false end local function startSingle(gameName, config) local key = gameName if connections[key] then return end activeStates[key] = true if yPositions[key] == nil then yPositions[key] = config.baseY end if dirs[key] == nil then dirs[key] = 1 end connections[key] = RunService.Heartbeat:Connect(function() if not activeStates[key] then return end local char = LocalPlayer.Character if not char then return end local hrp = char:FindFirstChild("HumanoidRootPart") if hrp then yPositions[key] = yPositions[key] + config.speed * dirs[key] if yPositions[key] <= config.baseY + config.minOffset then dirs[key] = 1 yPositions[key] = config.baseY + config.minOffset end if yPositions[key] >= config.baseY + config.maxOffset then dirs[key] = -1 yPositions[key] = config.baseY + config.maxOffset end hrp.CFrame = CFrame.new(config.pos) + Vector3.new(0, yPositions[key] - config.baseY, 0) end end) end local function startMulti(gameName, config, posIndex) local key = gameName .. "_" .. tostring(posIndex) if connections[key] then return end activeStates[key] = true local pos = config.positions[posIndex] if yPositions[key] == nil then yPositions[key] = config.baseY end if dirs[key] == nil then dirs[key] = 1 end connections[key] = RunService.Heartbeat:Connect(function() if not activeStates[key] then return end local char = LocalPlayer.Character if not char then return end local hrp = char:FindFirstChild("HumanoidRootPart") if hrp then yPositions[key] = yPositions[key] + config.speed * dirs[key] if yPositions[key] <= config.baseY + config.minOffset then dirs[key] = 1 yPositions[key] = config.baseY + config.minOffset end if yPositions[key] >= config.baseY + config.maxOffset then dirs[key] = -1 yPositions[key] = config.baseY + config.maxOffset end hrp.CFrame = CFrame.new(pos) + Vector3.new(0, yPositions[key] - config.baseY, 0) end end) end local function createGameToggle(gameName) local config = gameConfigs[gameName] if not config then return end if config.type == "multi" then for i = 1, #config.positions do tab:CreateToggle({ Name = "自动胜利世界" .. i, CurrentValue = false, Callback = function(val) if val then startMulti(gameName, config, i) else local key = gameName .. "_" .. tostring(i) if connections[key] then connections[key]:Disconnect() connections[key] = nil end activeStates[key] = false end end }) end else tab:CreateToggle({ Name = "自动胜利", CurrentValue = false, Callback = function(val) if val then startSingle(gameName, config) else stopGame(gameName) end end }) end end for name, _ in pairs(gameConfigs) do tab:CreateSection(name) createGameToggle(name) end end)(Window, Rayfield) (function(Window, Rayfield) local Players = game:GetService("Players") local RunService = game:GetService("RunService") local Workspace = game:GetService("Workspace") local LocalPlayer = Players.LocalPlayer local tab = Window:CreateTab("黑洞", "circle") local parts = {} local enabled1 = false local con1 = nil local partCon1 = nil local removeCon1 = nil local config1 = {radius = 50, height = 100, rotationSpeed = 10, attractionStrength = 1000} local hrPart = nil local folder = nil local part = nil local attachment1 = nil local enabled2 = false local heartbeatCon2 = nil local targetPlayer = nil local playerMap = {} local enabledH25 = false local h25Connection = nil local h25DescendantConn = nil local enabledH275 = false local h275Connection = nil local h275DescendantConn = nil local h275Folder = nil local h275Part = nil local h275Attachment = nil local h275HrPart = nil local h275Players = {} local h275CurrentIndex = 1 local h275SwitchTimer = 0 local h275CurrentTarget = nil local function getPlayerNames() local names = {} playerMap = {} for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then local label = p.DisplayName .. " (@" .. p.Name .. ")" table.insert(names, label) playerMap[label] = p end end return names end local function retainPart1(p) if p:IsA("BasePart") and not p.Anchored and p:IsDescendantOf(Workspace) then if p.Parent == LocalPlayer.Character or p:IsDescendantOf(LocalPlayer.Character) then return false end pcall(function() p.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0) p.CanCollide = false end) return true end return false end local function addPart1(p) if retainPart1(p) then local found = false for _, v in pairs(parts) do if v == p then found = true; break end end if not found then table.insert(parts, p) end end end local function removePart1(p) for i, v in pairs(parts) do if v == p then table.remove(parts, i); break end end end local function refreshParts1() parts = {} for _, p in pairs(Workspace:GetDescendants()) do addPart1(p) end end local function startH1() if con1 then return end enabled1 = true refreshParts1() if partCon1 then partCon1:Disconnect() end partCon1 = Workspace.DescendantAdded:Connect(addPart1) if removeCon1 then removeCon1:Disconnect() end removeCon1 = Workspace.DescendantRemoving:Connect(removePart1) con1 = RunService.Heartbeat:Connect(function() if not enabled1 then return end local root = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") if not root then return end local center = root.Position for _, p in pairs(parts) do if p.Parent and not p.Anchored then local pos = p.Position local dist = (Vector3.new(pos.X, center.Y, pos.Z) - center).Magnitude local angle = math.atan2(pos.Z - center.Z, pos.X - center.X) local newAngle = angle + math.rad(config1.rotationSpeed) local targetPos = Vector3.new( center.X + math.cos(newAngle) * math.min(config1.radius, dist), center.Y + (config1.height * (math.abs(math.sin((pos.Y - center.Y) / config1.height)))), center.Z + math.sin(newAngle) * math.min(config1.radius, dist) ) local dir = (targetPos - p.Position).unit p.Velocity = dir * config1.attractionStrength end end end) end local function stopH1() enabled1 = false if con1 then con1:Disconnect(); con1 = nil end if partCon1 then partCon1:Disconnect(); partCon1 = nil end if removeCon1 then removeCon1:Disconnect(); removeCon1 = nil end parts = {} end local function setupH2() local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() hrPart = character:WaitForChild("HumanoidRootPart") folder = Instance.new("Folder", Workspace) part = Instance.new("Part", folder) attachment1 = Instance.new("Attachment", part) part.Anchored = true part.CanCollide = false part.Transparency = 1 if not getgenv().Network then getgenv().Network = {BaseParts = {}, Velocity = Vector3.new(14.46262424, 14.46262424, 14.46262424)} Network.RetainPart = function(p) if typeof(p) == "Instance" and p:IsA("BasePart") and p:IsDescendantOf(Workspace) then table.insert(Network.BaseParts, p) p.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0) p.CanCollide = false end end local function EnablePartControl() LocalPlayer.ReplicationFocus = Workspace RunService.Heartbeat:Connect(function() sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge) for _, p in pairs(Network.BaseParts) do if p:IsDescendantOf(Workspace) then p.Velocity = Network.Velocity end end end) end EnablePartControl() end end local function ForcePart(v) if v:IsA("Part") and not v.Anchored and not v.Parent:FindFirstChild("Humanoid") and not v.Parent:FindFirstChild("Head") and v.Name ~= "Handle" then for _, x in next, v:GetChildren() do if x:IsA("BodyAngularVelocity") or x:IsA("BodyForce") or x:IsA("BodyGyro") or x:IsA("BodyPosition") or x:IsA("BodyThrust") or x:IsA("BodyVelocity") or x:IsA("RocketPropulsion") then x:Destroy() end end if v:FindFirstChild("Attachment") then v:FindFirstChild("Attachment"):Destroy() end if v:FindFirstChild("AlignPosition") then v:FindFirstChild("AlignPosition"):Destroy() end if v:FindFirstChild("Torque") then v:FindFirstChild("Torque"):Destroy() end v.CanCollide = false local Torque = Instance.new("Torque", v) Torque.Torque = Vector3.new(100000, 100000, 100000) local AlignPosition = Instance.new("AlignPosition", v) local Attachment2 = Instance.new("Attachment", v) Torque.Attachment0 = Attachment2 AlignPosition.MaxForce = 9999999999999999 AlignPosition.MaxVelocity = math.huge AlignPosition.Responsiveness = 200 AlignPosition.Attachment0 = Attachment2 AlignPosition.Attachment1 = attachment1 end end local function startH2() if enabled2 then return end enabled2 = true setupH2() for _, v in next, Workspace:GetDescendants() do ForcePart(v) end local conn = Workspace.DescendantAdded:Connect(function(v) if enabled2 then ForcePart(v) end end) _G.BlackholeDescendantConn = conn heartbeatCon2 = RunService.Heartbeat:Connect(function() if enabled2 and attachment1 and hrPart then attachment1.WorldCFrame = hrPart.CFrame end end) end local function stopH2() enabled2 = false if heartbeatCon2 then heartbeatCon2:Disconnect(); heartbeatCon2 = nil end if _G.BlackholeDescendantConn then _G.BlackholeDescendantConn:Disconnect() _G.BlackholeDescendantConn = nil end if folder then folder:Destroy(); folder = nil end hrPart = nil attachment1 = nil part = nil end local function setupH25() local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() hrPart = character:WaitForChild("HumanoidRootPart") folder = Instance.new("Folder", Workspace) part = Instance.new("Part", folder) attachment1 = Instance.new("Attachment", part) part.Anchored = true part.CanCollide = false part.Transparency = 1 if not getgenv().Network then getgenv().Network = {BaseParts = {}, Velocity = Vector3.new(14.46262424, 14.46262424, 14.46262424)} Network.RetainPart = function(p) if typeof(p) == "Instance" and p:IsA("BasePart") and p:IsDescendantOf(Workspace) then table.insert(Network.BaseParts, p) p.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0) p.CanCollide = false end end local function EnablePartControl() LocalPlayer.ReplicationFocus = Workspace RunService.Heartbeat:Connect(function() sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge) for _, p in pairs(Network.BaseParts) do if p:IsDescendantOf(Workspace) then p.Velocity = Network.Velocity end end end) end EnablePartControl() end end local function ForcePartH25(v, targetPos) if v:IsA("BasePart") and not v.Anchored and not v.Parent:FindFirstChild("Humanoid") and not v.Parent:FindFirstChild("Head") and v.Name ~= "Handle" then local distance = (v.Position - targetPos).Magnitude if distance > 2000 then return end for _, x in next, v:GetChildren() do if x:IsA("BodyAngularVelocity") or x:IsA("BodyForce") or x:IsA("BodyGyro") or x:IsA("BodyPosition") or x:IsA("BodyThrust") or x:IsA("BodyVelocity") or x:IsA("RocketPropulsion") then x:Destroy() end end if v:FindFirstChild("Attachment") then v:FindFirstChild("Attachment"):Destroy() end if v:FindFirstChild("AlignPosition") then v:FindFirstChild("AlignPosition"):Destroy() end if v:FindFirstChild("Torque") then v:FindFirstChild("Torque"):Destroy() end v.CanCollide = false local Torque = Instance.new("Torque", v) Torque.Torque = Vector3.new(100000, 100000, 100000) local AlignPosition = Instance.new("AlignPosition", v) local Attachment2 = Instance.new("Attachment", v) Torque.Attachment0 = Attachment2 AlignPosition.MaxForce = 9999999999999999 AlignPosition.MaxVelocity = math.huge AlignPosition.Responsiveness = 200 AlignPosition.Attachment0 = Attachment2 AlignPosition.Attachment1 = attachment1 end end local function startH25() if enabledH25 then return end if not targetPlayer then Notify("提示", "请先选择目标玩家", 2, "alert") return end enabledH25 = true setupH25() local targetPos = targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") if targetPos then for _, v in next, Workspace:GetDescendants() do if v:IsA("BasePart") then local distance = (v.Position - targetPos.Position).Magnitude if distance <= 2000 then ForcePartH25(v, targetPos.Position) end end end end h25DescendantConn = Workspace.DescendantAdded:Connect(function(v) if enabledH25 and v:IsA("BasePart") and targetPlayer and targetPlayer.Character then local targetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart") if targetRoot then local distance = (v.Position - targetRoot.Position).Magnitude if distance <= 2000 then ForcePartH25(v, targetRoot.Position) end end end end) h25Connection = RunService.Heartbeat:Connect(function() if enabledH25 and attachment1 and targetPlayer and targetPlayer.Character then local targetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart") if targetRoot then attachment1.WorldCFrame = targetRoot.CFrame end elseif not enabledH25 then if h25Connection then h25Connection:Disconnect(); h25Connection = nil end end end) end local function stopH25() enabledH25 = false if h25Connection then h25Connection:Disconnect(); h25Connection = nil end if h25DescendantConn then h25DescendantConn:Disconnect(); h25DescendantConn = nil end if folder then folder:Destroy(); folder = nil end hrPart = nil attachment1 = nil part = nil end local function setupH275() local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() h275HrPart = character:WaitForChild("HumanoidRootPart") h275Folder = Instance.new("Folder", Workspace) h275Part = Instance.new("Part", h275Folder) h275Attachment = Instance.new("Attachment", h275Part) h275Part.Anchored = true h275Part.CanCollide = false h275Part.Transparency = 1 if not getgenv().Network then getgenv().Network = {BaseParts = {}, Velocity = Vector3.new(14.46262424, 14.46262424, 14.46262424)} Network.RetainPart = function(p) if typeof(p) == "Instance" and p:IsA("BasePart") and p:IsDescendantOf(Workspace) then table.insert(Network.BaseParts, p) p.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0) p.CanCollide = false end end local function EnablePartControl() LocalPlayer.ReplicationFocus = Workspace RunService.Heartbeat:Connect(function() sethiddenproperty(LocalPlayer, "SimulationRadius", math.huge) for _, p in pairs(Network.BaseParts) do if p:IsDescendantOf(Workspace) then p.Velocity = Network.Velocity end end end) end EnablePartControl() end end local function ForcePartH275(v, targetPos) if v:IsA("BasePart") and not v.Anchored and not v.Parent:FindFirstChild("Humanoid") and not v.Parent:FindFirstChild("Head") and v.Name ~= "Handle" then local distance = (v.Position - targetPos).Magnitude if distance > 2000 then return end for _, x in next, v:GetChildren() do if x:IsA("BodyAngularVelocity") or x:IsA("BodyForce") or x:IsA("BodyGyro") or x:IsA("BodyPosition") or x:IsA("BodyThrust") or x:IsA("BodyVelocity") or x:IsA("RocketPropulsion") then x:Destroy() end end if v:FindFirstChild("Attachment") then v:FindFirstChild("Attachment"):Destroy() end if v:FindFirstChild("AlignPosition") then v:FindFirstChild("AlignPosition"):Destroy() end if v:FindFirstChild("Torque") then v:FindFirstChild("Torque"):Destroy() end v.CanCollide = false local Torque = Instance.new("Torque", v) Torque.Torque = Vector3.new(100000, 100000, 100000) local AlignPosition = Instance.new("AlignPosition", v) local Attachment2 = Instance.new("Attachment", v) Torque.Attachment0 = Attachment2 AlignPosition.MaxForce = 9999999999999999 AlignPosition.MaxVelocity = math.huge AlignPosition.Responsiveness = 200 AlignPosition.Attachment0 = Attachment2 AlignPosition.Attachment1 = h275Attachment end end local function getAlivePlayers() local alive = {} for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then local char = p.Character if char then local hum = char:FindFirstChildOfClass("Humanoid") if hum and hum.Health > 0 then table.insert(alive, p) end end end end return alive end local function startH275() if enabledH275 then return end enabledH275 = true setupH275() h275Players = getAlivePlayers() h275CurrentIndex = 1 h275SwitchTimer = 0 if #h275Players > 0 then h275CurrentTarget = h275Players[h275CurrentIndex] local targetRoot = h275CurrentTarget.Character and h275CurrentTarget.Character:FindFirstChild("HumanoidRootPart") if targetRoot then for _, v in next, Workspace:GetDescendants() do if v:IsA("BasePart") then local distance = (v.Position - targetRoot.Position).Magnitude if distance <= 2000 then ForcePartH275(v, targetRoot.Position) end end end end else enabledH275 = false return end h275DescendantConn = Workspace.DescendantAdded:Connect(function(v) if enabledH275 and v:IsA("BasePart") and h275CurrentTarget and h275CurrentTarget.Character then local targetRoot = h275CurrentTarget.Character:FindFirstChild("HumanoidRootPart") if targetRoot then local distance = (v.Position - targetRoot.Position).Magnitude if distance <= 2000 then ForcePartH275(v, targetRoot.Position) end end end end) h275Connection = RunService.Heartbeat:Connect(function() if not enabledH275 then if h275Connection then h275Connection:Disconnect(); h275Connection = nil end return end h275Players = getAlivePlayers() if #h275Players == 0 then stopH275() return end if h275CurrentIndex > #h275Players then h275CurrentIndex = 1 end h275CurrentTarget = h275Players[h275CurrentIndex] if h275CurrentTarget and h275CurrentTarget.Character then local targetRoot = h275CurrentTarget.Character:FindFirstChild("HumanoidRootPart") if targetRoot and h275Attachment then h275Attachment.WorldCFrame = targetRoot.CFrame end end h275SwitchTimer = h275SwitchTimer + 1 if h275SwitchTimer >= 15 then h275SwitchTimer = 0 h275CurrentIndex = h275CurrentIndex + 1 if h275CurrentIndex > #h275Players then h275CurrentIndex = 1 end h275CurrentTarget = h275Players[h275CurrentIndex] end end) end local function stopH275() enabledH275 = false if h275Connection then h275Connection:Disconnect(); h275Connection = nil end if h275DescendantConn then h275DescendantConn:Disconnect(); h275DescendantConn = nil end if h275Folder then h275Folder:Destroy(); h275Folder = nil end h275HrPart = nil h275Attachment = nil h275Part = nil h275CurrentTarget = nil h275Players = {} end tab:CreateSection("h1") tab:CreateToggle({ Name = "开启h1", CurrentValue = false, Callback = function(val) if val then startH1() else stopH1() end end }) tab:CreateSection("h2") tab:CreateToggle({ Name = "开启h2", CurrentValue = false, Callback = function(val) if val then startH2() else stopH2() end end }) tab:CreateSection("h2.5") local playerDropdown local function refreshDropdown() local newNames = getPlayerNames() if playerDropdown then playerDropdown:Refresh(newNames) else playerDropdown = tab:CreateDropdown({ Name = "选择目标玩家", Options = newNames, CurrentOption = {}, Callback = function(selected) if selected and #selected > 0 then targetPlayer = playerMap[selected[1]] end end }) end end refreshDropdown() tab:CreateButton({ Name = "刷新玩家列表", Callback = function() refreshDropdown() Notify("刷新成功", "玩家列表已更新", 2, "check") end }) Players.PlayerAdded:Connect(refreshDropdown) Players.PlayerRemoving:Connect(refreshDropdown) tab:CreateToggle({ Name = "开启h2.5", CurrentValue = false, Callback = function(val) if val then startH25() else stopH25() end end }) tab:CreateSection("h2.75") tab:CreateToggle({ Name = "开启h2.75", CurrentValue = false, Callback = function(val) if val then startH275() else stopH275() end end }) end)(Window, Rayfield) (function(Window, Rayfield) local Players = game:GetService("Players") local RunService = game:GetService("RunService") local TweenService = game:GetService("TweenService") local Workspace = game:GetService("Workspace") local LocalPlayer = Players.LocalPlayer local tab = Window:CreateTab("玩家", "users") local targetPlayer = nil local playerMap = {} local function getPlayerNames() local names = {} playerMap = {} for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then local label = p.DisplayName .. " (@" .. p.Name .. ")" table.insert(names, label) playerMap[label] = p end end return names end tab:CreateSection("玩家选项") local playerDropdown local function refreshDropdown() local newNames = getPlayerNames() if playerDropdown then playerDropdown:Refresh(newNames) else playerDropdown = tab:CreateDropdown({ Name = "选择目标玩家", Options = newNames, CurrentOption = {}, Callback = function(selected) if selected and #selected > 0 then targetPlayer = playerMap[selected[1]] end end }) end end refreshDropdown() tab:CreateButton({ Name = "刷新玩家列表", Callback = function() refreshDropdown() Notify("刷新成功", "玩家列表已更新", 2, "check") end }) Players.PlayerAdded:Connect(refreshDropdown) Players.PlayerRemoving:Connect(refreshDropdown) local teleportSection = tab:CreateSection("传送") teleportSection:CreateToggle({ Name = "平滑跟随", CurrentValue = false, Callback = function(val) if val then if not targetPlayer then Notify("提示", "请先选择目标玩家", 2, "alert") return end _G.SmoothFollowCon = RunService.Heartbeat:Connect(function() local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") if not myRoot then return end local target = targetPlayer if not target or not target.Character then return end local targetRoot = target.Character:FindFirstChild("HumanoidRootPart") if not targetRoot then return end local offset = targetRoot.CFrame.LookVector * -2.5 myRoot.CFrame = CFrame.new(targetRoot.Position + offset, targetRoot.Position) end) else if _G.SmoothFollowCon then _G.SmoothFollowCon:Disconnect() _G.SmoothFollowCon = nil end end end }) teleportSection:CreateButton({ Name = "传送至选中玩家", Callback = function() if not targetPlayer then Notify("提示", "请先选择目标玩家", 2, "alert") return end local target = targetPlayer if not target.Character or not target.Character:FindFirstChild("HumanoidRootPart") then return end local myChar = LocalPlayer.Character if not myChar or not myChar:FindFirstChild("HumanoidRootPart") then return end local targetPos = target.Character.HumanoidRootPart.Position myChar.HumanoidRootPart.CFrame = CFrame.new(targetPos + Vector3.new(0, 0, 3)) end }) teleportSection:CreateButton({ Name = "传送最近玩家", Callback = function() local nearest = nil local minDist = math.huge local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") if not myRoot then return end local myPos = myRoot.Position for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then local pChar = p.Character if pChar then local pRoot = pChar:FindFirstChild("HumanoidRootPart") if pRoot then local dist = (pRoot.Position - myPos).Magnitude if dist < minDist then minDist = dist nearest = p end end end end end if nearest then local targetRoot = nearest.Character and nearest.Character:FindFirstChild("HumanoidRootPart") if targetRoot and myRoot then myRoot.CFrame = targetRoot.CFrame + Vector3.new(0, 0, 3) Notify("传送成功", "已传送至 " .. nearest.DisplayName, 2, "check") end else Notify("提示", "未找到可传送的玩家", 2, "alert") end end }) local teleportAllRunning = false teleportSection:CreateToggle({ Name = "传送所有人 (0.45s间隔)", CurrentValue = false, Callback = function(val) teleportAllRunning = val if val then task.spawn(function() local players = Players:GetPlayers() local count = 0 for _, p in ipairs(players) do if not teleportAllRunning then break end if p ~= LocalPlayer then local char = p.Character if char and char:FindFirstChild("HumanoidRootPart") then local myChar = LocalPlayer.Character if myChar and myChar:FindFirstChild("HumanoidRootPart") then myChar.HumanoidRootPart.CFrame = char.HumanoidRootPart.CFrame + Vector3.new(0, 0, 3) count = count + 1 task.wait(0.45) end end end end if count > 0 then Notify("传送所有人", "已传送至 " .. count .. " 名玩家", 2, "check") else Notify("传送所有人", "没有可传送的玩家", 2, "alert") end end) end end }) local trollingSection = tab:CreateSection("恶搞") local suckConnection = nil local suckAnimation = nil local originalGravity = nil trollingSection:CreateToggle({ Name = "口人", CurrentValue = false, Callback = function(val) if val then if not targetPlayer then Notify("提示", "请先选择目标玩家", 2, "alert") return end local target = targetPlayer local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") local targetChar = target.Character if not targetChar then return end local targetRoot = targetChar:FindFirstChild("HumanoidRootPart") if not myRoot or not targetRoot then return end originalGravity = Workspace.Gravity Workspace.Gravity = 0 local animation = Instance.new("Animation") animation.AnimationId = "rbxassetid://5918726674" local humanoid = myChar:FindFirstChildOfClass("Humanoid") if humanoid then suckAnimation = humanoid:LoadAnimation(animation) suckAnimation:Play() suckAnimation:AdjustSpeed(1) end local targetTorso = targetChar:FindFirstChild("LowerTorso") or targetChar:FindFirstChild("UpperTorso") if targetTorso then suckConnection = RunService.Heartbeat:Connect(function() if myRoot and targetTorso then myRoot.CFrame = targetTorso.CFrame * CFrame.new(0, -2.3, -1.0) * CFrame.Angles(0, math.pi, 0) end end) end else if suckConnection then suckConnection:Disconnect(); suckConnection = nil end if suckAnimation then suckAnimation:Stop(); suckAnimation = nil end if originalGravity then Workspace.Gravity = originalGravity; originalGravity = nil end end end }) local bangConnection = nil local bangAnimation = nil trollingSection:CreateToggle({ Name = "配人", CurrentValue = false, Callback = function(val) if val then if not targetPlayer then Notify("提示", "请先选择目标玩家", 2, "alert") return end local target = targetPlayer local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") if not myRoot then return end local animation = Instance.new("Animation") animation.AnimationId = "rbxassetid://10714068222" local humanoid = myChar:FindFirstChildOfClass("Humanoid") if humanoid then bangAnimation = humanoid:LoadAnimation(animation) bangAnimation:Play() bangAnimation:AdjustSpeed(2) end bangConnection = RunService.Heartbeat:Connect(function() if not val then return end local targetChar = target.Character if not targetChar then return end local targetHRP = targetChar:FindFirstChild("HumanoidRootPart") if not targetHRP or not myRoot then return end local forwardCFrame = targetHRP.CFrame * CFrame.new(0, 0, 1) local backwardCFrame = targetHRP.CFrame * CFrame.new(0, 0, 2.5) local tweenForward = TweenService:Create(myRoot, TweenInfo.new(0.2, Enum.EasingStyle.Linear, Enum.EasingDirection.Out), {CFrame = forwardCFrame}) tweenForward:Play() tweenForward.Completed:Wait() local tweenBackward = TweenService:Create(myRoot, TweenInfo.new(0.2, Enum.EasingStyle.Linear, Enum.EasingDirection.Out), {CFrame = backwardCFrame}) tweenBackward:Play() tweenBackward.Completed:Wait() end) else if bangConnection then bangConnection:Disconnect(); bangConnection = nil end if bangAnimation then bangAnimation:Stop(); bangAnimation = nil end end end }) local susConnection = nil local susAnimation = nil trollingSection:CreateToggle({ Name = "被配", CurrentValue = false, Callback = function(val) if val then if not targetPlayer then Notify("提示", "请先选择目标玩家", 2, "alert") return end local target = targetPlayer local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") if not myRoot then return end local animation = Instance.new("Animation") animation.AnimationId = "rbxassetid://10714360343" local humanoid = myChar:FindFirstChildOfClass("Humanoid") if humanoid then susAnimation = humanoid:LoadAnimation(animation) susAnimation:Play() end susConnection = RunService.Heartbeat:Connect(function() if not val then return end local targetChar = target.Character if not targetChar then return end local targetHRP = targetChar:FindFirstChild("HumanoidRootPart") if not targetHRP or not myRoot then return end local forwardCFrame = targetHRP.CFrame * CFrame.new(0, 0, -1.5) local backwardCFrame = targetHRP.CFrame * CFrame.new(0, 0, -1.1) local tweenForward = TweenService:Create(myRoot, TweenInfo.new(0.15, Enum.EasingStyle.Linear, Enum.EasingDirection.Out), {CFrame = forwardCFrame}) tweenForward:Play() tweenForward.Completed:Wait() local tweenBackward = TweenService:Create(myRoot, TweenInfo.new(0.15, Enum.EasingStyle.Linear, Enum.EasingDirection.Out), {CFrame = backwardCFrame}) tweenBackward:Play() tweenBackward.Completed:Wait() end) else if susConnection then susConnection:Disconnect(); susConnection = nil end if susAnimation then susAnimation:Stop(); susAnimation = nil end end end }) local getSuckedConnection = nil local getSuckedAnimation = nil local getSuckedGravity = nil local getSuckedRunning = false trollingSection:CreateToggle({ Name = "人口", CurrentValue = false, Callback = function(val) if val then if not targetPlayer then Notify("提示", "请先选择目标玩家", 2, "alert") return end getSuckedRunning = true local target = targetPlayer local myChar = LocalPlayer.Character if not myChar then return end local myRoot = myChar:FindFirstChild("HumanoidRootPart") local targetChar = target.Character if not targetChar then return end local targetRoot = targetChar:FindFirstChild("HumanoidRootPart") if not myRoot or not targetRoot then return end getSuckedGravity = Workspace.Gravity Workspace.Gravity = 0 coroutine.wrap(function() while getSuckedRunning and myRoot and targetRoot and myRoot.Position.Y <= 44 do task.wait() myRoot.CFrame = myRoot.CFrame * CFrame.new(0, 1.5, 0) end task.wait(1) if getSuckedRunning then local animation = Instance.new("Animation") animation.AnimationId = "rbxassetid://5918726674" local humanoid = myChar:FindFirstChildOfClass("Humanoid") if humanoid then getSuckedAnimation = humanoid:LoadAnimation(animation) getSuckedAnimation:Play() getSuckedAnimation:AdjustSpeed(1) end getSuckedConnection = RunService.Stepped:Connect(function() if getSuckedRunning and myRoot and targetRoot then myRoot.CFrame = targetRoot.CFrame * CFrame.new(0, 2.3, -1.1) * CFrame.Angles(0, math.pi, 0) myRoot.Velocity = Vector3.new(0, 0, 0) end end) end end)() else getSuckedRunning = false if getSuckedConnection then getSuckedConnection:Disconnect(); getSuckedConnection = nil end if getSuckedAnimation then getSuckedAnimation:Stop(); getSuckedAnimation = nil end if getSuckedGravity then Workspace.Gravity = getSuckedGravity; getSuckedGravity = nil end end end }) local cameraFollowConnection = nil local isCameraFollowing = false trollingSection:CreateToggle({ Name = "镜头跟随", CurrentValue = false, Callback = function(val) isCameraFollowing = val if val then if not targetPlayer then Notify("提示", "请先选择目标玩家", 2, "alert") return end local target = targetPlayer local targetChar = target.Character if not targetChar then Notify("提示", "目标玩家没有角色", 2, "alert") return end local targetHumanoid = targetChar:FindFirstChildOfClass("Humanoid") if not targetHumanoid then Notify("提示", "目标玩家没有Humanoid", 2, "alert") return end workspace.CurrentCamera.CameraSubject = targetHumanoid cameraFollowConnection = RunService.Heartbeat:Connect(function() if not isCameraFollowing then return end local currentTarget = targetPlayer if not currentTarget or not currentTarget.Character then return end local currentHumanoid = currentTarget.Character:FindFirstChildOfClass("Humanoid") if currentHumanoid then workspace.CurrentCamera.CameraSubject = currentHumanoid end end) Notify("镜头跟随", "已锁定 " .. target.DisplayName, 2, "check") else if cameraFollowConnection then cameraFollowConnection:Disconnect() cameraFollowConnection = nil end local myChar = LocalPlayer.Character if myChar then local myHumanoid = myChar:FindFirstChildOfClass("Humanoid") if myHumanoid then workspace.CurrentCamera.CameraSubject = myHumanoid end end Notify("镜头跟随", "已关闭", 2, "check") end end }) local flingSection = tab:CreateSection("甩飞") local function silentFling(targetPlayer) if not targetPlayer then return end local targetChar = targetPlayer.Character if not targetChar then return end local myChar = LocalPlayer.Character if not myChar then return end local myHumanoid = myChar:FindFirstChildOfClass("Humanoid") local myRoot = myHumanoid and myHumanoid.RootPart if not myRoot then return end local targetHumanoid = targetChar:FindFirstChildOfClass("Humanoid") local targetRoot = targetHumanoid and targetHumanoid.RootPart if not targetRoot then Notify("甩飞失败", "目标没有RootPart", 2, "alert") return end local originalSubject = workspace.CurrentCamera.CameraSubject if myHumanoid then workspace.CurrentCamera.CameraSubject = myHumanoid end local oldPos = myRoot.CFrame local oldVel = myRoot.AssemblyLinearVelocity local oldRotVel = myRoot.AssemblyAngularVelocity myHumanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false) for _, otherPlayer in pairs(Players:GetPlayers()) do if otherPlayer ~= LocalPlayer then local otherChar = otherPlayer.Character if otherChar then for _, part in pairs(otherChar:GetDescendants()) do if part:IsA("BasePart") then pcall(function() part.CanCollide = false end) end end end end end local bv = Instance.new("BodyVelocity") bv.Name = "FlingVelocity" bv.Parent = myRoot bv.Velocity = Vector3.new(9e8, 9e8, 9e8) bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge) local startTime = tick() local direction = 1 local lastSwitch = tick() while tick() - startTime < 2.5 do if not targetPlayer.Character then break end local currentTargetRoot = targetPlayer.Character:FindFirstChild("HumanoidRootPart") if not currentTargetRoot then break end if tick() - lastSwitch > 0.1 then direction = direction * -1 lastSwitch = tick() end local myChar2 = LocalPlayer.Character local myRoot2 = myChar2 and myChar2:FindFirstChild("HumanoidRootPart") if myRoot2 then local offset = direction * 2 local targetPos = currentTargetRoot.Position + currentTargetRoot.CFrame.LookVector * offset myRoot2.CFrame = CFrame.new(targetPos) myRoot2.AssemblyAngularVelocity = Vector3.new(50000, 50000, 50000) myRoot2.AssemblyLinearVelocity = Vector3.new(9e7, 9e7 * 10, 9e7) end task.wait(0.05) end bv:Destroy() myHumanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true) local myChar3 = LocalPlayer.Character local myRoot3 = myChar3 and myChar3:FindFirstChild("HumanoidRootPart") if myRoot3 then myRoot3.CFrame = oldPos myRoot3.AssemblyLinearVelocity = oldVel myRoot3.AssemblyAngularVelocity = oldRotVel end if originalSubject then workspace.CurrentCamera.CameraSubject = originalSubject elseif myHumanoid then workspace.CurrentCamera.CameraSubject = myHumanoid end task.wait(0.05) if myHumanoid then myHumanoid:ChangeState(Enum.HumanoidStateType.GettingUp) end end flingSection:CreateButton({ Name = "甩飞当前选中", Callback = function() if not targetPlayer then Notify("提示", "请先选择目标玩家", 2, "alert") return end local target = targetPlayer if not target or not target.Character then Notify("提示", "目标玩家不在游戏中", 2, "alert") return end local targetHum = target.Character:FindFirstChildOfClass("Humanoid") if not targetHum or targetHum.Health <= 0 then Notify("提示", "目标已死亡", 2, "alert") return end silentFling(target) Notify("甩飞成功", "已弹飞 " .. target.DisplayName, 1.5, "check") end }) local loopFlingRunning = false local loopFlingConnection = nil flingSection:CreateToggle({ Name = "循环甩飞当前选中", CurrentValue = false, Callback = function(val) if val then if not targetPlayer then Notify("提示", "请先选择目标玩家", 2, "alert") return end loopFlingRunning = true local target = targetPlayer Notify("循环甩飞", "已开启，目标: " .. target.DisplayName, 2, "check") loopFlingConnection = RunService.Heartbeat:Connect(function() if not loopFlingRunning then loopFlingConnection:Disconnect() loopFlingConnection = nil return end if not targetPlayer or not targetPlayer.Character then return end local targetHum = targetPlayer.Character:FindFirstChildOfClass("Humanoid") if not targetHum or targetHum.Health <= 0 then return end silentFling(targetPlayer) task.wait(0.3) end) else loopFlingRunning = false if loopFlingConnection then loopFlingConnection:Disconnect() loopFlingConnection = nil end Notify("循环甩飞", "已关闭", 1.5, "check") end end }) flingSection:CreateButton({ Name = "甩飞所有玩家", Callback = function() local count = 0 for _, p in ipairs(Players:GetPlayers()) do if p ~= LocalPlayer then local char = p.Character if char then local hum = char:FindFirstChildOfClass("Humanoid") if hum and hum.Health > 0 then silentFling(p) count = count + 1 task.wait(0.2) end end end end Notify("甩飞所有玩家", "已甩飞 " .. count .. " 名玩家", 2, "check") end }) local loopAllFlingRunning = false local loopAllFlingConnection = nil flingSection:CreateToggle({ Name = "循环甩飞所有玩家", CurrentValue = false, Callback = function(val) if val then loopAllFlingRunning = true Notify("循环甩飞所有玩家", "已开启", 2, "check") loopAllFlingConnection = RunService.Heartbeat:Connect(function() if not loopAllFlingRunning then loopAllFlingConnection:Disconnect() loopAllFlingConnection = nil return end local players = Players:GetPlayers() for _, p in ipairs(players) do if not loopAllFlingRunning then break end if p ~= LocalPlayer then local char = p.Character if char then local hum = char:FindFirstChildOfClass("Humanoid") if hum and hum.Health > 0 then silentFling(p) task.wait(0.2) end end end end task.wait(0.3) end) else loopAllFlingRunning = false if loopAllFlingConnection then loopAllFlingConnection:Disconnect() loopAllFlingConnection = nil end Notify("循环甩飞所有玩家", "已关闭", 1.5, "check") end end }) end)(Window, Rayfield) (function(Window, Rayfield) local Players = game:GetService("Players") local RunService = game:GetService("RunService") local UserInputService = game:GetService("UserInputService") local VirtualUser = game:GetService("VirtualUser") local VirtualInput = game:GetService("VirtualInputManager") local Lighting = game:GetService("Lighting") local Workspace = game:GetService("Workspace") local TweenService = game:GetService("TweenService") local ReplicatedStorage = game:GetService("ReplicatedStorage") local LocalPlayer = Players.LocalPlayer local tab = Window:CreateTab("通用", "settings") tab:CreateSection("建议开") local function createSuggestToggle(title, callback) return tab:CreateToggle({ Name = title, CurrentValue = false, Callback = callback }) end createSuggestToggle("防踢", function(val) if val then local con = LocalPlayer.Idled:Connect(function() VirtualUser:CaptureController() VirtualUser:ClickButton2(Vector2.new()) end) _G.AntiAFKCon = con else if _G.AntiAFKCon then _G.AntiAFKCon:Disconnect(); _G.AntiAFKCon = nil end end end) local function createNoFall() local con = nil local charCon = nil local z = Vector3.zero local function bind(c) if not c then return end local r = c:WaitForChild("HumanoidRootPart", 5) if r then if con then con:Disconnect(); con = nil end con = RunService.Heartbeat:Connect(function() if not r.Parent then if con then con:Disconnect(); con = nil end return end local v = r.AssemblyLinearVelocity r.AssemblyLinearVelocity = z RunService.RenderStepped:Wait() r.AssemblyLinearVelocity = v end) end end return { start = function() if charCon then charCon:Disconnect() end bind(LocalPlayer.Character) charCon = LocalPlayer.CharacterAdded:Connect(bind) end, stop = function() if con then con:Disconnect(); con = nil end if charCon then charCon:Disconnect(); charCon = nil end end } end local noFallApi = nil createSuggestToggle("防摔", function(val) if not noFallApi then noFallApi = createNoFall() end if val then noFallApi.start() else noFallApi.stop() end end) local originalLighting = {} local originalAtmosphere = {} createSuggestToggle("去雾", function(enable) if enable then originalLighting.FogStart = Lighting.FogStart originalLighting.FogEnd = Lighting.FogEnd originalLighting.FogColor = Lighting.FogColor Lighting.FogStart = 0 Lighting.FogEnd = math.huge Lighting.FogColor = Color3.fromRGB(200, 200, 220) for _, obj in pairs(Lighting:GetChildren()) do if obj:IsA("Atmosphere") then if not originalAtmosphere[obj] then originalAtmosphere[obj] = { Density = obj.Density, Offset = obj.Offset, Glare = obj.Glare, Haze = obj.Haze } end obj.Density = 0 obj.Offset = 0 obj.Glare = 0 obj.Haze = 0 end end else if originalLighting.FogStart then Lighting.FogStart = originalLighting.FogStart end if originalLighting.FogEnd then Lighting.FogEnd = originalLighting.FogEnd end if originalLighting.FogColor then Lighting.FogColor = originalLighting.FogColor end for obj, props in pairs(originalAtmosphere) do if obj and obj.Parent then obj.Density = props.Density obj.Offset = props.Offset obj.Glare = props.Glare obj.Haze = props.Haze end end originalAtmosphere = {} end end) local antiPushConn = nil createSuggestToggle("防甩飞", function(val) if val then if antiPushConn then antiPushConn:Disconnect() end antiPushConn = RunService.Stepped:Connect(function() local char = LocalPlayer.Character if char then local root = char:FindFirstChild("HumanoidRootPart") if root then root.CanCollide = false end end end) else if antiPushConn then antiPushConn:Disconnect(); antiPushConn = nil end local char = LocalPlayer.Character if char then local root = char:FindFirstChild("HumanoidRootPart") if root then root.CanCollide = true end end end end) local antiPullbackConn = nil local antiPullbackSpeed = 100 createSuggestToggle("防回拉", function(val) if val then if antiPullbackConn then antiPullbackConn:Disconnect() end antiPullbackConn = RunService.Heartbeat:Connect(function() local char = LocalPlayer.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") local root = char:FindFirstChild("HumanoidRootPart") if not hum or not root then return end if hum.MoveDirection.Magnitude > 0 then char:TranslateBy(hum.MoveDirection * antiPullbackSpeed / 20) end end) else if antiPullbackConn then antiPullbackConn:Disconnect(); antiPullbackConn = nil end end end) createSuggestToggle("无限跳", function(val) if val then local conn = UserInputService.JumpRequest:Connect(function() if LocalPlayer and LocalPlayer.Character then local hum = LocalPlayer.Character:FindFirstChildOfClass("Humanoid") if hum then hum:ChangeState(Enum.HumanoidStateType.Jumping) end end end) _G.InfJumpCon = conn else if _G.InfJumpCon then _G.InfJumpCon:Disconnect(); _G.InfJumpCon = nil end end end) local instantRespawnEnabled = false local instantRespawnLoop = nil createSuggestToggle("秒复活", function(val) instantRespawnEnabled = val if val then if instantRespawnLoop then task.cancel(instantRespawnLoop) end instantRespawnLoop = task.spawn(function() while instantRespawnEnabled do local character = LocalPlayer.Character if character then local humanoid = character:FindFirstChildOfClass("Humanoid") if humanoid and humanoid.Health <= 0 then LocalPlayer:LoadCharacter() task.wait(0.5) end end task.wait(0.1) end end) else if instantRespawnLoop then task.cancel(instantRespawnLoop); instantRespawnLoop = nil end end end) local instantBuyEnabled = false local instantBuyConnection = nil local instantBuyAddedConnection = nil local function setupInstantBuy() for _, v in pairs(workspace:GetDescendants()) do if v:IsA("ProximityPrompt") then v.HoldDuration = 0 end end end createSuggestToggle("瞬间交互", function(val) instantBuyEnabled = val if val then setupInstantBuy() if instantBuyConnection then instantBuyConnection:Disconnect() end instantBuyConnection = RunService.Heartbeat:Connect(function() if instantBuyEnabled then setupInstantBuy() end end) if instantBuyAddedConnection then instantBuyAddedConnection:Disconnect() end instantBuyAddedConnection = workspace.DescendantAdded:Connect(function(v) if v:IsA("ProximityPrompt") and instantBuyEnabled then v.HoldDuration = 0 end end) else if instantBuyConnection then instantBuyConnection:Disconnect(); instantBuyConnection = nil end if instantBuyAddedConnection then instantBuyAddedConnection:Disconnect(); instantBuyAddedConnection = nil end end end) local unbreakLimbsEnabled = false local unbreakLimbsConn = nil createSuggestToggle("肢体不碎", function(val) unbreakLimbsEnabled = val if val then local function setup() if type(CharStats) == "function" and me then local limbsfolder = CharStats(me).HealthValues if not limbsfolder then return end for _, a in pairs(limbsfolder:GetChildren()) do for _, i in pairs(a:GetChildren()) do if i and i.Name == "Broken" then i.Value = false if not unbreakLimbsConn then unbreakLimbsConn = i:GetPropertyChangedSignal("Value"):Connect(function() if unbreakLimbsEnabled then i.Value = false end end) end end end end end end setup() else if unbreakLimbsConn then unbreakLimbsConn:Disconnect(); unbreakLimbsConn = nil end end end) local teleportToolEnabled = false local teleportToolConnection = nil local teleportTool = nil local function createTeleportTool() if teleportTool then return end local Mouse = LocalPlayer:GetMouse() local Camera = workspace.CurrentCamera local Tool = Instance.new("Tool") Tool.Name = "点击传送" Tool.RequiresHandle = false Tool.Activated:Connect(function() local Character = LocalPlayer.Character if not Character then return end local HRP = Character:FindFirstChild("HumanoidRootPart") if not HRP then return end local params = RaycastParams.new() params.FilterDescendantsInstances = {Character} params.FilterType = Enum.RaycastFilterType.Blacklist local unitRay = Camera:ScreenPointToRay(Mouse.X, Mouse.Y) local result = workspace:Raycast(unitRay.Origin, unitRay.Direction * 100000, params) if not result then return end local hitPos = result.Position + Vector3.new(0, 3, 0) HRP.CFrame = CFrame.new(hitPos) end) teleportTool = Tool local function equipTool() if teleportToolEnabled and teleportTool then local backpack = LocalPlayer:FindFirstChild("Backpack") if backpack and teleportTool.Parent ~= backpack then teleportTool.Parent = backpack end end end equipTool() if teleportToolConnection then teleportToolConnection:Disconnect() end teleportToolConnection = LocalPlayer.CharacterAdded:Connect(function() task.wait(0.5) equipTool() end) end local function destroyTeleportTool() if teleportToolConnection then teleportToolConnection:Disconnect(); teleportToolConnection = nil end if teleportTool then teleportTool:Destroy(); teleportTool = nil end end createSuggestToggle("点击传送工具", function(val) teleportToolEnabled = val if val then createTeleportTool() else destroyTeleportTool() end end) local groupBypassEnabled = false local groupBypassHook = nil createSuggestToggle("群组验证绕过", function(val) groupBypassEnabled = val if val then if not groupBypassHook then local OldNameCall OldNameCall = hookmetamethod(game, "__namecall", function(self, ...) if self ~= LocalPlayer or getnamecallmethod() ~= "IsInGroup" then return OldNameCall(self, ...) end return true end) hookfunction(LocalPlayer.IsInGroup, function(self, ...) return true end) groupBypassHook = {OldNameCall = OldNameCall} end end end) createSuggestToggle("一键清屏", function(val) if val then local CoreGui = game:GetService("CoreGui") local PlayerGui = LocalPlayer:WaitForChild("PlayerGui") if PlayerGui:FindFirstChild("FullUIToggle") then PlayerGui.FullUIToggle:Destroy() end local Hidden = false local Stored = {} local ScreenGui = Instance.new("ScreenGui") ScreenGui.Name = "FullUIToggle" ScreenGui.ResetOnSpawn = false ScreenGui.Parent = PlayerGui local Button = Instance.new("TextButton") Button.Size = UDim2.new(0, 120, 0, 30) Button.Position = UDim2.new(1, -130, 0, 10) Button.BackgroundColor3 = Color3.fromRGB(20, 20, 20) Button.TextColor3 = Color3.new(1, 1, 1) Button.Text = "隐藏" Button.Parent = ScreenGui Instance.new("UICorner", Button) local function IsUI(obj) return obj:IsA("GuiObject") end local function ShouldSkip(obj) return obj:IsDescendantOf(ScreenGui) end local function Process(container, hide) for _, obj in ipairs(container:GetDescendants()) do if IsUI(obj) and not ShouldSkip(obj) then if hide then if not Stored[obj] then Stored[obj] = obj.Visible end obj.Visible = false else if Stored[obj] ~= nil then obj.Visible = Stored[obj] end end end end end local function ToggleUI(state) if state then Stored = {} Process(PlayerGui, true) Process(CoreGui, true) else Process(PlayerGui, false) Process(CoreGui, false) Stored = {} end end Button.MouseButton1Click:Connect(function() Hidden = not Hidden ToggleUI(Hidden) Button.Text = Hidden and "显示" or "隐藏" end) local function Hook(container) container.DescendantAdded:Connect(function(obj) if Hidden and obj:IsA("GuiObject") and not ShouldSkip(obj) then Stored[obj] = obj.Visible obj.Visible = false end end) end Hook(PlayerGui) Hook(CoreGui) else local PlayerGui = LocalPlayer:FindFirstChild("PlayerGui") if PlayerGui and PlayerGui:FindFirstChild("FullUIToggle") then PlayerGui.FullUIToggle:Destroy() end end end) tab:CreateDivider() tab:CreateSection("独立功能") local speedEnabled = false local speedSliderValue = 1 local speedCFrameConn = nil local speedWalkSpeedMonitor = nil local gameDefaultWalkSpeed = 16 local function monitorWalkSpeed() if speedWalkSpeedMonitor then speedWalkSpeedMonitor:Disconnect(); speedWalkSpeedMonitor = nil end local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if not hum then return end speedWalkSpeedMonitor = hum:GetPropertyChangedSignal("WalkSpeed"):Connect(function() if not speedEnabled then gameDefaultWalkSpeed = hum.WalkSpeed end end) gameDefaultWalkSpeed = hum.WalkSpeed end LocalPlayer.CharacterAdded:Connect(function() task.wait(0.5) if not speedEnabled then monitorWalkSpeed() end end) tab:CreateToggle({ Name = "奔跑速度开关", CurrentValue = false, Callback = function(val) speedEnabled = val if val then if speedCFrameConn then speedCFrameConn:Disconnect() end local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if hum then gameDefaultWalkSpeed = hum.WalkSpeed end if speedWalkSpeedMonitor then speedWalkSpeedMonitor:Disconnect(); speedWalkSpeedMonitor = nil end speedCFrameConn = RunService.RenderStepped:Connect(function() local char = LocalPlayer.Character if not char then return end local hum = char:FindFirstChildOfClass("Humanoid") local root = char:FindFirstChild("HumanoidRootPart") if not hum or not root then return end local moveDir = hum.MoveDirection if moveDir.Magnitude > 0 then local actualSpeed = speedSliderValue * 32 root.CFrame = root.CFrame + moveDir * (actualSpeed * 0.05) end end) else if speedCFrameConn then speedCFrameConn:Disconnect(); speedCFrameConn = nil end local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if hum then hum.WalkSpeed = gameDefaultWalkSpeed end monitorWalkSpeed() end end }) tab:CreateSlider({ Name = "奔跑速度", Range = {1, 40}, CurrentValue = 1, Increment = 1, Callback = function(value) speedSliderValue = math.floor(value) end }) local jumpEnabled = false local jumpSliderValue = 1 local jumpLockConnection = nil local gameDefaultJumpPower = 50 local jumpPowerMonitor = nil local function monitorJumpPower() if jumpPowerMonitor then jumpPowerMonitor:Disconnect(); jumpPowerMonitor = nil end local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if not hum then return end jumpPowerMonitor = hum:GetPropertyChangedSignal("JumpPower"):Connect(function() if not jumpEnabled then gameDefaultJumpPower = hum.JumpPower end end) gameDefaultJumpPower = hum.JumpPower end local function stopJumpLock() if jumpLockConnection then jumpLockConnection:Disconnect(); jumpLockConnection = nil end end local function startJumpLock(value) stopJumpLock() if not jumpEnabled then return end local actualJumpPower = value * 50 jumpLockConnection = RunService.Heartbeat:Connect(function() local char = LocalPlayer.Character if char then local hum = char:FindFirstChildOfClass("Humanoid") if hum then hum.UseJumpPower = true if hum.JumpPower ~= actualJumpPower then hum.JumpPower = actualJumpPower end end end end) end LocalPlayer.CharacterAdded:Connect(function() task.wait(0.5) if jumpEnabled then startJumpLock(jumpSliderValue) else monitorJumpPower() end end) tab:CreateToggle({ Name = "跳跃高度开关", CurrentValue = false, Callback = function(val) jumpEnabled = val if val then local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if hum then gameDefaultJumpPower = hum.JumpPower end if jumpPowerMonitor then jumpPowerMonitor:Disconnect(); jumpPowerMonitor = nil end startJumpLock(jumpSliderValue) else stopJumpLock() local char = LocalPlayer.Character local hum = char and char:FindFirstChildOfClass("Humanoid") if hum then hum.JumpPower = gameDefaultJumpPower end monitorJumpPower() end end }) tab:CreateSlider({ Name = "跳跃高度", Range = {1, 60}, CurrentValue = 1, Increment = 1, Callback = function(value) jumpSliderValue = math.floor(value) if jumpEnabled then startJumpLock(jumpSliderValue) end end }) local gravityEnabled = false local gravitySliderValue = 1 local gravityHeartbeat = nil local gameDefaultGravity = 196.2 local gravityMonitor = nil local function monitorGravity() if gravityMonitor then gravityMonitor:Disconnect(); gravityMonitor = nil end gravityMonitor = RunService.Stepped:Connect(function() if not gravityEnabled then gameDefaultGravity = workspace.Gravity end end) gameDefaultGravity = workspace.Gravity end local function startGravityLock(ratio) if gravityHeartbeat then gravityHeartbeat:Disconnect(); gravityHeartbeat = nil end if not gravityEnabled then return end local targetGravity = ratio * 196.2 gravityHeartbeat = RunService.Heartbeat:Connect(function() workspace.Gravity = targetGravity end) end tab:CreateToggle({ Name = "重力开关", CurrentValue = false, Callback = function(val) gravityEnabled = val if val then gameDefaultGravity = workspace.Gravity if gravityMonitor then gravityMonitor:Disconnect(); gravityMonitor = nil end startGravityLock(gravitySliderValue) else if gravityHeartbeat then gravityHeartbeat:Disconnect(); gravityHeartbeat = nil end workspace.Gravity = gameDefaultGravity monitorGravity() end end }) tab:CreateSlider({ Name = "重力", Range = {1, 40}, CurrentValue = 1, Increment = 1, Callback = function(value) gravitySliderValue = math.floor(value) if gravityEnabled then startGravityLock(gravitySliderValue) end end }) tab:CreateToggle({ Name = "穿墙", CurrentValue = false, Callback = function(val) if val then _G.NoclipCon = RunService.Stepped:Connect(function() local char = LocalPlayer.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end root.CanCollide = false for _, v in ipairs(char:GetDescendants()) do if v:IsA("BasePart") and v.CanCollide then v.CanCollide = false end end end) else if _G.NoclipCon then _G.NoclipCon:Disconnect(); _G.NoclipCon = nil end local char = LocalPlayer.Character if char then local root = char:FindFirstChild("HumanoidRootPart") if root then root.CanCollide = true end for _, v in ipairs(char:GetDescendants()) do if v:IsA("BasePart") and not v.CanCollide then v.CanCollide = true end end end end end }) local superNoclipState = false local tpWalkConn = nil local noclipConn = nil local tpWalkSpeed = 1 tab:CreateToggle({ Name = "穿墙2.0", CurrentValue = false, Callback = function(val) superNoclipState = val if val then if not tpWalkConn then tpWalkConn = RunService.RenderStepped:Connect(function(delta) local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") and char:FindFirstChild("Humanoid") then local hum = char.Humanoid if hum.MoveDirection.Magnitude > 0 then char.HumanoidRootPart.CFrame = char.HumanoidRootPart.CFrame + hum.MoveDirection * (tpWalkSpeed * delta * 40) end end end) end if not noclipConn then noclipConn = RunService.Stepped:Connect(function() if LocalPlayer.Character then for _, part in ipairs(LocalPlayer.Character:GetDescendants()) do if part:IsA("BasePart") then part.CanCollide = false end end end end) end else if tpWalkConn then tpWalkConn:Disconnect(); tpWalkConn = nil end if noclipConn then noclipConn:Disconnect(); noclipConn = nil end local char = LocalPlayer.Character if char then for _, part in ipairs(char:GetDescendants()) do if part:IsA("BasePart") then part.CanCollide = true end end end end end }) tab:CreateSlider({ Name = "穿墙行走速度", Range = {1, 50}, CurrentValue = 1, Increment = 1, Callback = function(value) tpWalkSpeed = math.floor(value) end }) tab:CreateToggle({ Name = "锁定视角", CurrentValue = false, Callback = function(val) if val then _G.LockViewCon = RunService:BindToRenderStep("LockView", Enum.RenderPriority.Camera.Value + 1, function() local char = LocalPlayer.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end local camDir = workspace.CurrentCamera.CFrame.LookVector local flatDir = Vector3.new(camDir.X, 0, camDir.Z) if flatDir.Magnitude > 0.01 then root.CFrame = CFrame.new(root.Position, root.Position + flatDir) end end) else if _G.LockViewCon then RunService:UnbindFromRenderStep("LockView") _G.LockViewCon = nil end end end }) local espPlayerCons = {} local function addESP(character) if not character then return end local oldHL = character:FindFirstChild("NH_ESP") if oldHL then oldHL:Destroy() end local highlight = Instance.new("Highlight") highlight.Name = "NH_ESP" highlight.FillTransparency = 0.7 highlight.OutlineTransparency = 0 highlight.FillColor = Color3.fromRGB(255, 0, 0) highlight.OutlineColor = Color3.fromRGB(255, 255, 255) highlight.Adornee = character highlight.Parent = character end local function hookPlayer(player) if player == LocalPlayer then return end if player.Character then addESP(player.Character) end local con = player.CharacterAdded:Connect(function(char) task.wait(0.2) addESP(char) end) table.insert(espPlayerCons, con) end tab:CreateToggle({ Name = "透视玩家", CurrentValue = false, Callback = function(enabled) _G.ESPEnabled = enabled if enabled then for _, player in ipairs(Players:GetPlayers()) do hookPlayer(player) end _G.ESPAddedCon = Players.PlayerAdded:Connect(function(player) if _G.ESPEnabled then hookPlayer(player) end end) else if _G.ESPAddedCon then _G.ESPAddedCon:Disconnect(); _G.ESPAddedCon = nil end for _, con in ipairs(espPlayerCons) do con:Disconnect() end espPlayerCons = {} for _, player in ipairs(Players:GetPlayers()) do if player.Character then for _, desc in pairs(player.Character:GetDescendants()) do if desc:IsA("Highlight") and desc.Name == "NH_ESP" then desc:Destroy() end end end end for _, hl in pairs(workspace:GetDescendants()) do if hl:IsA("Highlight") and hl.Name == "NH_ESP" then hl:Destroy() end end end end }) tab:CreateToggle({ Name = "透视", CurrentValue = false, Callback = function(val) if val then _G.XrayCon = RunService.Heartbeat:Connect(function() for _, v in pairs(workspace:GetDescendants()) do if v:IsA("BasePart") and not v.Parent:FindFirstChildWhichIsA("Humanoid") and not (v.Parent and v.Parent.Parent and v.Parent.Parent:FindFirstChildWhichIsA("Humanoid")) then v.LocalTransparencyModifier = 0.5 end end end) else if _G.XrayCon then _G.XrayCon:Disconnect(); _G.XrayCon = nil end for _, v in pairs(workspace:GetDescendants()) do if v:IsA("BasePart") then v.LocalTransparencyModifier = 0 end end end end }) tab:CreateToggle({ Name = "飞行", CurrentValue = false, Callback = function(val) local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() local hum = char:FindFirstChildOfClass("Humanoid") local root = char:FindFirstChild("HumanoidRootPart") if val then if hum then for _, state in pairs(Enum.HumanoidStateType:GetEnumItems()) do hum:SetStateEnabled(state, false) end hum:ChangeState(Enum.HumanoidStateType.Swimming) end if char.Animate then char.Animate.Disabled = true end local bg = Instance.new("BodyGyro", root) bg.P = 9e4 bg.maxTorque = Vector3.new(9e9, 9e9, 9e9) bg.CFrame = root.CFrame local bv = Instance.new("BodyVelocity", root) bv.Velocity = Vector3.new(0, 0, 0) bv.MaxForce = Vector3.new(9e9, 9e9, 9e9) if hum then hum.PlatformStand = true end _G.FlyBody = {bg = bg, bv = bv} _G.FlyEnabled = true _G.FlyCon1 = RunService.RenderStepped:Connect(function() if not _G.FlyEnabled or not char or not hum or hum.Health <= 0 then if _G.FlyCon1 then _G.FlyCon1:Disconnect() end return end if root then bg.CFrame = workspace.CurrentCamera.CoordinateFrame end end) _G.FlyCon2 = RunService.Heartbeat:Connect(function() if not _G.FlyEnabled or not char or not hum then return end if hum.MoveDirection.Magnitude > 0 then char:TranslateBy(hum.MoveDirection * 1) end end) else if hum then for _, state in pairs(Enum.HumanoidStateType:GetEnumItems()) do hum:SetStateEnabled(state, true) end hum:ChangeState(Enum.HumanoidStateType.Running) hum.PlatformStand = false end if char.Animate then char.Animate.Disabled = false end if _G.FlyBody and _G.FlyBody.bg then _G.FlyBody.bg:Destroy() end if _G.FlyBody and _G.FlyBody.bv then _G.FlyBody.bv:Destroy() end _G.FlyBody = {} _G.FlyEnabled = false if _G.FlyCon1 then _G.FlyCon1:Disconnect(); _G.FlyCon1 = nil end if _G.FlyCon2 then _G.FlyCon2:Disconnect(); _G.FlyCon2 = nil end end end }) local function setTransparency(character, transparency) for _, part in pairs(character:GetDescendants()) do if part:IsA("BasePart") or part:IsA("Decal") then part.Transparency = transparency end end end tab:CreateToggle({ Name = "隐身", CurrentValue = false, Callback = function(val) if val then local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then local savedpos = char.HumanoidRootPart.CFrame task.wait() char:MoveTo(Vector3.new(-25.95, 84, 3537.55)) task.wait(0.15) local Seat = Instance.new('Seat', workspace) Seat.Anchored = false Seat.CanCollide = false Seat.Name = 'invischair' Seat.Transparency = 1 Seat.Position = Vector3.new(-25.95, 84, 3537.55) local Weld = Instance.new("Weld", Seat) Weld.Part0 = Seat Weld.Part1 = char:FindFirstChild("Torso") or char:FindFirstChild("UpperTorso") task.wait() Seat.CFrame = savedpos setTransparency(char, 0.5) end else local invisChair = workspace:FindFirstChild('invischair') if invisChair then invisChair:Destroy() end if LocalPlayer.Character then setTransparency(LocalPlayer.Character, 0) end end end }) tab:CreateToggle({ Name = "偷取所有物品", CurrentValue = false, Callback = function(val) _G.StealLoop = val if val then task.spawn(function() while _G.StealLoop do local myBackpack = LocalPlayer:FindFirstChild("Backpack") if not myBackpack then myBackpack = Instance.new("Backpack") myBackpack.Parent = LocalPlayer end for _, targetPlayer in ipairs(Players:GetPlayers()) do if targetPlayer == LocalPlayer or not targetPlayer.Character then continue end local containers = { targetPlayer:FindFirstChild("Backpack"), targetPlayer:FindFirstChild("Inventory"), targetPlayer:FindFirstChild("Storage"), targetPlayer:FindFirstChild("Bag"), targetPlayer.Character:FindFirstChild("Backpack") } for _, container in ipairs(containers) do if not container then continue end for _, item in ipairs(container:GetChildren()) do if item:IsA("Tool") or item:IsA("Model") or item:IsA("Part") or item:IsA("Accessory") then item.Parent = myBackpack task.wait(0.05) end end end end task.wait(1) end end) end end }) tab:CreateToggle({ Name = "防坐下", CurrentValue = false, Callback = function(val) local character = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() local humanoid = character:WaitForChild("Humanoid") if val then humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, false) humanoid.Sit = false else humanoid:SetStateEnabled(Enum.HumanoidStateType.Seated, true) humanoid.Sit = false end end }) local blockGui = nil local function placeOneBlock() local char = LocalPlayer.Character local root = char and char:FindFirstChild("HumanoidRootPart") if not char or not root then return end local pos = root.Position + Vector3.new(0, -3, 0) local block = Instance.new("Part") block.Name = "LocalBlock" block.Size = Vector3.new(6, 1, 6) block.Position = pos block.Anchored = true block.CanCollide = true block.BrickColor = BrickColor.new("White") block.Parent = workspace end tab:CreateToggle({ Name = "放置方块", CurrentValue = false, Callback = function(val) if val then if blockGui then blockGui:Destroy() end local sg = Instance.new("ScreenGui") sg.Name = "BlockPlaceGui" sg.Parent = LocalPlayer.PlayerGui sg.ResetOnSpawn = false _G.BlockGui = sg local mainFrame = Instance.new("Frame") mainFrame.Parent = sg mainFrame.Size = UDim2.new(0, 160, 0, 100) mainFrame.Position = UDim2.new(0.1, 0, 0.18, 0) mainFrame.BackgroundTransparency = 1 mainFrame.ZIndex = 9999 local btnClear = Instance.new("TextButton") btnClear.Parent = mainFrame btnClear.Size = UDim2.new(0, 50, 0, 50) btnClear.Position = UDim2.new(0, 0, 0, 0) btnClear.BackgroundColor3 = Color3.new(1, 0, 0) btnClear.TextColor3 = Color3.new(1, 1, 1) btnClear.Text = "清除" btnClear.Font = Enum.Font.GothamBold btnClear.TextSize = 14 btnClear.AutoButtonColor = false btnClear.ZIndex = 10000 Instance.new("UICorner", btnClear).CornerRadius = UDim.new(0, 12) btnClear.MouseButton1Click:Connect(function() for _, v in pairs(workspace:GetChildren()) do if v.Name == "LocalBlock" then v:Destroy() end end end) local closeBtn = Instance.new("TextButton") closeBtn.Parent = mainFrame closeBtn.Size = UDim2.new(0, 50, 0, 50) closeBtn.Position = UDim2.new(0, 55, 0, 0) closeBtn.BackgroundColor3 = Color3.new(0, 0, 0) closeBtn.Text = "X" closeBtn.TextColor3 = Color3.new(1, 1, 1) closeBtn.TextSize = 18 closeBtn.ZIndex = 10002 Instance.new("UICorner", closeBtn).CornerRadius = UDim.new(0, 8) closeBtn.MouseButton1Click:Connect(function() _G.AutoPlace = false if _G.BlockGui then _G.BlockGui:Destroy(); _G.BlockGui = nil end end) local autoBtn = Instance.new("TextButton") autoBtn.Parent = mainFrame autoBtn.Size = UDim2.new(0, 160, 0, 40) autoBtn.Position = UDim2.new(0, 0, 0, 55) autoBtn.BackgroundColor3 = Color3.new(0, 0, 0) autoBtn.Text = "自动放置" autoBtn.TextColor3 = Color3.new(1, 1, 1) autoBtn.Font = Enum.Font.GothamBold autoBtn.TextSize = 14 autoBtn.AutoButtonColor = false autoBtn.ZIndex = 10000 Instance.new("UICorner", autoBtn).CornerRadius = UDim.new(0, 12) autoBtn.MouseButton1Click:Connect(function() _G.AutoPlace = not _G.AutoPlace if _G.AutoPlace then autoBtn.BackgroundColor3 = Color3.new(0.2, 0.8, 0.2) task.spawn(function() while _G.AutoPlace do placeOneBlock() task.wait(0.1) end end) else autoBtn.BackgroundColor3 = Color3.new(0, 0, 0) end end) local function makeDraggable(frame) local dragging = false local dragStart, startPos frame.InputBegan:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = true dragStart = input.Position startPos = frame.Position end end) frame.InputEnded:Connect(function(input) if input.UserInputType == Enum.UserInputType.MouseButton1 then dragging = false end end) frame.InputChanged:Connect(function(input) if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then local delta = input.Position - dragStart frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y) end end) end makeDraggable(mainFrame) else if _G.BlockGui then _G.BlockGui:Destroy(); _G.BlockGui = nil end _G.AutoPlace = false end end }) local respawnProtection = { _enabled = false, _lastPosition = nil, _deathConnection = nil, _charAddedConn = nil } tab:CreateToggle({ Name = "原地复活", CurrentValue = false, Callback = function(val) respawnProtection._enabled = val if val then if respawnProtection._charAddedConn then respawnProtection._charAddedConn:Disconnect() end respawnProtection._charAddedConn = LocalPlayer.CharacterAdded:Connect(function(character) local humanoid = character:WaitForChild("Humanoid") if respawnProtection._deathConnection then respawnProtection._deathConnection:Disconnect() end respawnProtection._deathConnection = humanoid.Died:Connect(function() if respawnProtection._enabled and character:FindFirstChild("HumanoidRootPart") then respawnProtection._lastPosition = character.HumanoidRootPart.Position end end) if respawnProtection._enabled and respawnProtection._lastPosition then local hrp = character:WaitForChild("HumanoidRootPart") if hrp then hrp.CFrame = CFrame.new(respawnProtection._lastPosition) end end end) local char = LocalPlayer.Character if char then local humanoid = char:FindFirstChildOfClass("Humanoid") if humanoid then if respawnProtection._deathConnection then respawnProtection._deathConnection:Disconnect() end respawnProtection._deathConnection = humanoid.Died:Connect(function() if respawnProtection._enabled and char:FindFirstChild("HumanoidRootPart") then respawnProtection._lastPosition = char.HumanoidRootPart.Position end end) end end else if respawnProtection._deathConnection then respawnProtection._deathConnection:Disconnect(); respawnProtection._deathConnection = nil end if respawnProtection._charAddedConn then respawnProtection._charAddedConn:Disconnect(); respawnProtection._charAddedConn = nil end respawnProtection._lastPosition = nil end end }) local waterWalkEnabled = false local waterWalkConn = nil tab:CreateToggle({ Name = "水上行走", CurrentValue = false, Callback = function(val) waterWalkEnabled = val if val then if waterWalkConn then waterWalkConn:Disconnect() end waterWalkConn = RunService.Heartbeat:Connect(function() local character = LocalPlayer.Character if character and character:FindFirstChild("Humanoid") then character.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Swimming, false) end end) else if waterWalkConn then waterWalkConn:Disconnect(); waterWalkConn = nil end local character = LocalPlayer.Character if character and character:FindFirstChild("Humanoid") then character.Humanoid:SetStateEnabled(Enum.HumanoidStateType.Swimming, true) end end end }) local carFlyEnabled = false local carFlyBodyVelocity = nil local carFlyBodyGyro = nil local carFlyMoveConnection = nil local carFlySpeed = 50 local function startCarFly() local char = LocalPlayer.Character local root = char and char:FindFirstChild("HumanoidRootPart") if not root then return end if carFlyBodyVelocity then carFlyBodyVelocity:Destroy() end if carFlyBodyGyro then carFlyBodyGyro:Destroy() end if carFlyMoveConnection then carFlyMoveConnection:Disconnect() end carFlyBodyVelocity = Instance.new("BodyVelocity", root) carFlyBodyVelocity.MaxForce = Vector3.new(math.huge, math.huge, math.huge) carFlyBodyVelocity.Velocity = Vector3.new(0, 0, 0) carFlyBodyGyro = Instance.new("BodyGyro", root) carFlyBodyGyro.MaxTorque = Vector3.new(math.huge, math.huge, math.huge) carFlyBodyGyro.D = 5000 carFlyBodyGyro.P = 50000 carFlyBodyGyro.CFrame = workspace.CurrentCamera.CFrame carFlyMoveConnection = RunService.RenderStepped:Connect(function() if not carFlyEnabled then return end local charNow = LocalPlayer.Character local rootNow = charNow and charNow:FindFirstChild("HumanoidRootPart") local humNow = charNow and charNow:FindFirstChildOfClass("Humanoid") if not rootNow or not humNow then return end local camCF = workspace.CurrentCamera.CFrame local direction = Vector3.zero if UserInputService:IsKeyDown(Enum.KeyCode.W) then direction = direction + camCF.LookVector end if UserInputService:IsKeyDown(Enum.KeyCode.S) then direction = direction - camCF.LookVector end if UserInputService:IsKeyDown(Enum.KeyCode.A) then direction = direction - camCF.RightVector end if UserInputService:IsKeyDown(Enum.KeyCode.D) then direction = direction + camCF.RightVector end if UserInputService:IsKeyDown(Enum.KeyCode.Space) then direction = direction + Vector3.new(0, 1, 0) end if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then direction = direction - Vector3.new(0, 1, 0) end if direction.Magnitude > 0 then carFlyBodyVelocity.Velocity = direction.Unit * carFlySpeed else carFlyBodyVelocity.Velocity = Vector3.zero end carFlyBodyGyro.CFrame = camCF end) end local function stopCarFly() if carFlyBodyVelocity then carFlyBodyVelocity:Destroy(); carFlyBodyVelocity = nil end if carFlyBodyGyro then carFlyBodyGyro:Destroy(); carFlyBodyGyro = nil end if carFlyMoveConnection then carFlyMoveConnection:Disconnect(); carFlyMoveConnection = nil end local char = LocalPlayer.Character local root = char and char:FindFirstChild("HumanoidRootPart") if root then root.Anchored = false end end tab:CreateToggle({ Name = "飞车", CurrentValue = false, Callback = function(val) carFlyEnabled = val if val then startCarFly() else stopCarFly() end end }) local autoRebirthTask = nil tab:CreateToggle({ Name = "自动重生", CurrentValue = false, Callback = function(val) if val then if autoRebirthTask then task.cancel(autoRebirthTask) end autoRebirthTask = task.spawn(function() while true do pcall(function() local remotes = ReplicatedStorage:FindFirstChild("Remotes") or ReplicatedStorage for _, r in ipairs(remotes:GetDescendants()) do if r:IsA("RemoteEvent") and (r.Name:find("Rebirth") or r.Name:find("Prestige")) then r:FireServer() end end end) task.wait(1) end end) else if autoRebirthTask then task.cancel(autoRebirthTask); autoRebirthTask = nil end end end }) local autoGainRunning = false local autoGainConnection = nil local autoGainCharAddedCon = nil local function startAutoGain() local char = LocalPlayer.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") local hum = char:FindFirstChildOfClass("Humanoid") if not root or not hum then return end local parkSpot = root.Position local angle = 0 local radius = 3 local pace = 0.01 autoGainRunning = true autoGainConnection = RunService.Heartbeat:Connect(function(deltaTime) if not autoGainRunning then autoGainConnection:Disconnect() autoGainConnection = nil return end local char2 = LocalPlayer.Character if not char2 then autoGainRunning = false; return end root = char2:FindFirstChild("HumanoidRootPart") hum = char2:FindFirstChildOfClass("Humanoid") if not root or not hum then return end angle = angle + (pace * deltaTime) / radius if angle > 2 * math.pi then angle = angle - 2 * math.pi end local px = parkSpot.X + radius * math.cos(angle) local pz = parkSpot.Z + radius * math.sin(angle) root.CFrame = CFrame.lookAt( Vector3.new(px, parkSpot.Y, pz), Vector3.new(px - math.sin(angle), parkSpot.Y, pz + math.cos(angle)) ) hum:Move(Vector3.new(-math.sin(angle), 0, math.cos(angle))) end) end local function stopAutoGain() autoGainRunning = false if autoGainConnection then autoGainConnection:Disconnect(); autoGainConnection = nil end if autoGainCharAddedCon then autoGainCharAddedCon:Disconnect(); autoGainCharAddedCon = nil end end tab:CreateToggle({ Name = "自动获取速度", CurrentValue = false, Callback = function(val) if val then startAutoGain() if autoGainCharAddedCon then autoGainCharAddedCon:Disconnect() end autoGainCharAddedCon = LocalPlayer.CharacterAdded:Connect(function() task.wait(0.5) if autoGainRunning then startAutoGain() end end) else stopAutoGain() end end }) local deleteEffectsEnabled = false local deleteEffectsConn = nil local disabledEffects = {} tab:CreateToggle({ Name = "删除所有特效", CurrentValue = false, Callback = function(val) deleteEffectsEnabled = val if val then local function clean(obj) pcall(function() if obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam") then if obj.Enabled then disabledEffects[obj] = true; obj.Enabled = false end end if obj:IsA("Fire") or obj:IsA("Smoke") or obj:IsA("Explosion") or obj:IsA("Sparkles") then if obj.Enabled then disabledEffects[obj] = true; obj.Enabled = false end end if obj:IsA("Light") or obj:IsA("PointLight") or obj:IsA("SpotLight") or obj:IsA("SurfaceLight") then if obj.Enabled then disabledEffects[obj] = true; obj.Enabled = false end end if obj:IsA("BloomEffect") or obj:IsA("BlurEffect") or obj:IsA("ColorCorrectionEffect") or obj:IsA("SunRaysEffect") or obj:IsA("DepthOfFieldEffect") or obj:IsA("Atmosphere") then if obj.Enabled then disabledEffects[obj] = true; obj.Enabled = false end end if obj:IsA("Highlight") or obj:IsA("SelectionBox") or obj:IsA("SelectionSphere") then if obj.Enabled then disabledEffects[obj] = true; obj.Enabled = false end end if obj:IsA("Decal") then if obj.Enabled then disabledEffects[obj] = true; obj.Enabled = false end end end) end for _, obj in pairs(game:GetDescendants()) do clean(obj) end deleteEffectsConn = game.DescendantAdded:Connect(clean) else for obj, _ in pairs(disabledEffects) do pcall(function() if obj and obj.Parent then obj.Enabled = true end end) end disabledEffects = {} if deleteEffectsConn then deleteEffectsConn:Disconnect(); deleteEffectsConn = nil end end end }) tab:CreateButton({ Name = "自动翻译", Callback = function() Notify("自动翻译", "此功能因Rayfield迁移已简化，可自行加载独立翻译脚本", 3, "info") end }) tab:CreateButton({ Name = "无敌", Callback = function() if _G.Unkillable then return end _G.Unkillable = true local function isKillish(inst) local name = string.lower(inst.Name) local KILL_KEYWORDS = {"kill","damage","hurt","lava","spike","poison","acid","death","saw","blade"} for _, kw in ipairs(KILL_KEYWORDS) do if string.find(name, kw, 1, true) then return true end end for _, child in ipairs(inst:GetChildren()) do if child:IsA("LuaSourceContainer") then local cname = string.lower(child.Name) if string.find(cname, "kill", 1, true) or string.find(cname, "damage", 1, true) or string.find(cname, "hurt", 1, true) or string.find(cname, "death", 1, true) then return true end end end return false end local function isKillNPC(inst) if not inst:IsA("Model") then return false end if Players:GetPlayerFromCharacter(inst) then return false end if not inst:FindFirstChildOfClass("Humanoid") then return false end local name = string.lower(inst.Name) local NPC_KEYWORDS = {"killer","monster","zombie","npc","boss","guard","enemy","chase","horror","demon","evil"} for _, kw in ipairs(NPC_KEYWORDS) do if string.find(name, kw, 1, true) then return true end end return false end local function destroyKillTargets() local queue = { Workspace } local idx = 1 while idx <= #queue do if not _G.Unkillable then break end local node = queue[idx] idx = idx + 1 if node and node.Parent then local children = node:GetChildren() for _, child in ipairs(children) do if LocalPlayer.Character and child:IsDescendantOf(LocalPlayer.Character) then elseif isKillNPC(child) then child:Destroy() elseif isKillish(child) then child:Destroy() else if child:IsA("Model") or child:IsA("Folder") or not child:IsA("BasePart") then table.insert(queue, child) end end end end if idx % 200 == 0 then RunService.Heartbeat:Wait() end end end local conn = Workspace.DescendantAdded:Connect(function(inst) if not _G.Unkillable then return end if LocalPlayer.Character and inst:IsDescendantOf(LocalPlayer.Character) then return end if isKillNPC(inst) then inst:Destroy() end if isKillish(inst) then inst:Destroy() end end) task.spawn(destroyKillTargets) _G.UnkillableConn = conn end }) tab:CreateButton({ Name = "去除所有NPC", Callback = function() if _G.RemoveAllNPC then return end _G.RemoveAllNPC = true local function isRemovableNPC(inst) if not inst:IsA("Model") then return false end if Players:GetPlayerFromCharacter(inst) then return false end if not inst:FindFirstChildOfClass("Humanoid") then return false end return true end local function destroyNPCs() local queue = { Workspace } local idx = 1 while idx <= #queue do if not _G.RemoveAllNPC then break end local node = queue[idx] idx = idx + 1 if node and node.Parent then local children = node:GetChildren() for _, child in ipairs(children) do if LocalPlayer.Character and child:IsDescendantOf(LocalPlayer.Character) then elseif isRemovableNPC(child) then child:Destroy() else table.insert(queue, child) end end end if idx % 150 == 0 then RunService.Heartbeat:Wait() end end end local conn = Workspace.DescendantAdded:Connect(function(inst) if not _G.RemoveAllNPC then return end if LocalPlayer.Character and inst:IsDescendantOf(LocalPlayer.Character) then return end if isRemovableNPC(inst) then inst:Destroy() end end) task.spawn(destroyNPCs) _G.RemoveAllNPCConn = conn end }) local isRagdolled = false local motorBackup = {} local function getCharacter() return LocalPlayer.Character end tab:CreateButton({ Name = "布娃娃状态", Callback = function() local character = getCharacter() if not character then return end local humanoid = character:FindFirstChildOfClass("Humanoid") local root = character:FindFirstChild("HumanoidRootPart") if not humanoid or not root then return end if not isRagdolled then humanoid:ChangeState(Enum.HumanoidStateType.Physics) humanoid.AutoRotate = false motorBackup = {} for _, joint in ipairs(character:GetDescendants()) do if joint:IsA("Motor6D") then table.insert(motorBackup, {Part0 = joint.Part0, Part1 = joint.Part1, C0 = joint.C0, C1 = joint.C1, Parent = joint.Parent}) local socket = Instance.new("BallSocketConstraint") local a1 = Instance.new("Attachment") local a2 = Instance.new("Attachment") a1.CFrame = joint.C0 a2.CFrame = joint.C1 a1.Parent = joint.Part0 a2.Parent = joint.Part1 socket.Attachment0 = a1 socket.Attachment1 = a2 socket.Parent = joint.Parent socket.LimitsEnabled = true joint:Destroy() end end isRagdolled = true else for _, data in pairs(motorBackup) do local motor = Instance.new("Motor6D") motor.Part0 = data.Part0 motor.Part1 = data.Part1 motor.C0 = data.C0 motor.C1 = data.C1 motor.Parent = data.Parent end humanoid:ChangeState(Enum.HumanoidStateType.GettingUp) humanoid.AutoRotate = true isRagdolled = false motorBackup = {} end end }) tab:CreateButton({ Name = "自杀", Callback = function() local char = LocalPlayer.Character if char then local hum = char:FindFirstChildOfClass("Humanoid") if hum then hum.Health = 0 end end end }) tab:CreateDivider() tab:CreateSection("连点器") local clickerActive = false local clickerConnection = nil local clickerCPS = 20 tab:CreateToggle({ Name = "连点器", CurrentValue = false, Callback = function(val) clickerActive = val if val then if clickerConnection then task.cancel(clickerConnection); clickerConnection = nil end local interval = 1000 / clickerCPS clickerConnection = task.spawn(function() while clickerActive do VirtualInput:SendMouseButtonEvent(0, 0, 0, true, game, 0) task.wait(0.02) VirtualInput:SendMouseButtonEvent(0, 0, 0, false, game, 0) task.wait(0.02) task.wait(interval / 1000) end end) else if clickerConnection then task.cancel(clickerConnection); clickerConnection = nil end end end }) tab:CreateInput({ Name = "每秒点击次数", PlaceholderText = "默认: 20", CurrentValue = "20", Callback = function(text) local num = tonumber(text) if num and num > 0 then clickerCPS = num if clickerActive then if clickerConnection then task.cancel(clickerConnection); clickerConnection = nil end local interval = 1000 / clickerCPS clickerConnection = task.spawn(function() while clickerActive do VirtualInput:SendMouseButtonEvent(0, 0, 0, true, game, 0) task.wait(0.02) VirtualInput:SendMouseButtonEvent(0, 0, 0, false, game, 0) task.wait(0.02) task.wait(interval / 1000) end end) end end end }) tab:CreateDivider() tab:CreateSection("光影效果") local originalLighting2 = {} local originalTerrain = {} local createdEffects = {} local runningConnections = {} local function clearEffects() for _, effect in ipairs(createdEffects) do effect:Destroy() end createdEffects = {} for _, conn in ipairs(runningConnections) do conn:Disconnect() end runningConnections = {} end local function saveLighting() originalLighting2 = { Brightness = Lighting.Brightness, Ambient = Lighting.Ambient, OutdoorAmbient = Lighting.OutdoorAmbient, GlobalShadows = Lighting.GlobalShadows, ShadowSoftness = Lighting.ShadowSoftness, FogColor = Lighting.FogColor, FogEnd = Lighting.FogEnd, FogStart = Lighting.FogStart, ClockTime = Lighting.ClockTime, GeographicLatitude = Lighting.GeographicLatitude, ExposureCompensation = Lighting.ExposureCompensation, EnvironmentDiffuseScale = Lighting.EnvironmentDiffuseScale, EnvironmentSpecularScale = Lighting.EnvironmentSpecularScale } local terrain = Workspace:FindFirstChildOfClass("Terrain") if terrain then originalTerrain = { WaterReflectance = terrain.WaterReflectance, WaterTransparency = terrain.WaterTransparency, WaterWaveSize = terrain.WaterWaveSize, WaterWaveSpeed = terrain.WaterWaveSpeed, WaterColor = terrain.WaterColor } end end local function restoreLighting() for key, value in pairs(originalLighting2) do Lighting[key] = value end local terrain = Workspace:FindFirstChildOfClass("Terrain") if terrain then for key, value in pairs(originalTerrain) do terrain[key] = value end end end local function createEffect(effectType, properties) local effect = Instance.new(effectType) for prop, value in pairs(properties) do if effect[prop] ~= nil then effect[prop] = value end end effect.Parent = Lighting table.insert(createdEffects, effect) return effect end tab:CreateToggle({ Name = "真实光影", CurrentValue = false, Callback = function(val) if val then saveLighting() clearEffects() Lighting.Brightness = 0.2 Lighting.Ambient = Color3.new(0, 0, 0) Lighting.OutdoorAmbient = Color3.new(0, 0, 0) Lighting.GlobalShadows = false Lighting.EnvironmentDiffuseScale = 0 Lighting.EnvironmentSpecularScale = 0 else clearEffects() restoreLighting() end end }) tab:CreateToggle({ Name = "明亮光影", CurrentValue = false, Callback = function(val) if val then saveLighting() clearEffects() Lighting.ClockTime = 12 Lighting.GeographicLatitude = 0 Lighting.GlobalShadows = true Lighting.ShadowSoftness = 0.3 Lighting.Brightness = 3 Lighting.OutdoorAmbient = Color3.new(0.8, 0.8, 0.8) Lighting.Ambient = Color3.new(0.6, 0.6, 0.6) Lighting.Technology = Enum.Technology.ShadowMap createEffect("SunRaysEffect", {Intensity = 0.2, Spread = 0.5}) createEffect("BloomEffect", {Intensity = 0.3, Size = 10}) createEffect("Atmosphere", {Density = 0.1, Offset = 0.5, Color = Color3.new(0.9, 0.9, 0.9), Decay = Color3.new(0.9, 0.9, 0.9), Glare = 0, Haze = 0}) createEffect("ColorCorrectionEffect", {Saturation = -0.1, Contrast = 0.1}) createEffect("BlurEffect", {Size = 1}) else clearEffects() restoreLighting() end end }) tab:CreateToggle({ Name = "画质增强Pro Max", CurrentValue = false, Callback = function(val) if val then saveLighting() clearEffects() for _, child in ipairs(Lighting:GetChildren()) do if child:IsA("PostEffect") or child:IsA("Sky") then child:Destroy() end end createEffect("ColorCorrectionEffect", {Contrast = 0.18, Brightness = 0.05, Saturation = 0.3, TintColor = Color3.fromRGB(255, 242, 230)}) createEffect("BloomEffect", {Intensity = 0.25, Size = 48, Threshold = 0.85}) createEffect("SunRaysEffect", {Intensity = 0.25, Spread = 1.0}) createEffect("Atmosphere", {Density = 0.4, Offset = 0.25, Color = Color3.fromRGB(220, 220, 255), Decay = Color3.fromRGB(20, 25, 45), Glare = 0.15, Haze = 0.25}) local sky = Instance.new("Sky") sky.Parent = Lighting sky.SkyboxBk = "rbxassetid://6444337006" sky.SkyboxDn = "rbxassetid://6444336728" sky.SkyboxFt = "rbxassetid://6444337006" sky.SkyboxLf = "rbxassetid://6444337006" sky.SkyboxRt = "rbxassetid://6444337006" sky.SkyboxUp = "rbxassetid://6444336728" sky.StarCount = 3000 table.insert(createdEffects, sky) Lighting.GlobalShadows = true Lighting.ShadowSoftness = 0.05 Lighting.Brightness = 2.8 Lighting.ExposureCompensation = 0.7 Lighting.FogColor = Color3.fromRGB(140, 158, 178) Lighting.FogEnd = 3000 local terrain = Workspace:FindFirstChildOfClass("Terrain") if terrain then terrain.WaterReflectance = 0.35 terrain.WaterTransparency = 0.88 terrain.WaterWaveSize = 0.15 terrain.WaterWaveSpeed = 25 terrain.WaterColor = Color3.fromRGB(72, 141, 202) end else clearEffects() restoreLighting() end end }) tab:CreateToggle({ Name = "固定黄昏光影", CurrentValue = false, Callback = function(val) if val then saveLighting() clearEffects() for _, child in ipairs(Lighting:GetChildren()) do if child:IsA("PostEffect") or child:IsA("Atmosphere") then child:Destroy() end end Lighting.GlobalShadows = true Lighting.ShadowSoftness = 0.06 Lighting.Brightness = 3.5 Lighting.ExposureCompensation = 0.5 Lighting.EnvironmentSpecularScale = 1.2 Lighting.EnvironmentDiffuseScale = 0.5 Lighting.FogColor = Color3.fromRGB(130, 140, 160) Lighting.FogEnd = 8000 Lighting.FogStart = 50 Lighting.OutdoorAmbient = Color3.fromRGB(120, 130, 150) Lighting.GeographicLatitude = 40.0 Lighting.ClockTime = 16.5 createEffect("ColorCorrectionEffect", {Contrast = 0.15, Brightness = 0.04, Saturation = 0.25, TintColor = Color3.fromRGB(255, 245, 235)}) createEffect("BloomEffect", {Intensity = 0.08, Size = 40, Threshold = 0.95}) createEffect("SunRaysEffect", {Intensity = 0.22, Spread = 0.8}) createEffect("DepthOfFieldEffect", {FarIntensity = 0.1, FocusDistance = 35, InFocusRadius = 22, NearIntensity = 0.3}) createEffect("Atmosphere", {Density = 0.25, Offset = 0.25, Color = Color3.fromRGB(180, 190, 210), Decay = Color3.fromRGB(35, 40, 50), Haze = 0.12}) local terrain = Workspace:FindFirstChildOfClass("Terrain") if terrain then terrain.WaterReflectance = 0.2 terrain.WaterTransparency = 0.93 terrain.WaterWaveSize = 0.12 terrain.WaterWaveSpeed = 16 terrain.WaterColor = Color3.fromRGB(75, 135, 200) end local sky = Lighting:FindFirstChildOfClass("Sky") or Instance.new("Sky") sky.Parent = Lighting sky.CelestialBodiesShown = true sky.StarCount = 3500 sky.SkyboxBk = "rbxassetid://2711140172" sky.SkyboxDn = "rbxassetid://2711140372" sky.SkyboxFt = "rbxassetid://2711140592" sky.SkyboxLf = "rbxassetid://2711140792" sky.SkyboxRt = "rbxassetid://2711141042" sky.SkyboxUp = "rbxassetid://2711141282" table.insert(createdEffects, sky) Workspace.CurrentCamera.FieldOfView = 70 else clearEffects() restoreLighting() end end }) end)(Window, Rayfield) Notify("加载完成", "所有功能已就绪，祝您使用愉快！", 3, "check")