return function(Window, WindUI) local Players = game:GetService("Players") local RunService = game:GetService("RunService") local ReplicatedStorage = game:GetService("ReplicatedStorage") local Workspace = game:GetService("Workspace") local MarketplaceService = game:GetService("MarketplaceService") local LocalPlayer = Players.LocalPlayer local tab = Window:Tab({Title = "服务器功能", Icon = "server"}) local chickenSection = tab:Section({ Title = "鸡场", Collapsible = true, Box = true, DefaultOpen = false }) do local config = { eggEnabled = false, eggRadius = 40, eggAutoDeposit = true, eggDepositInterval = 60, cashEnabled = false, cashDelay = 0.5, sellerEnabled = false, sellerDelay = 0.5, buyEnabled = false, buyAmount = 1, buyDelay = 0.5, mergeEnabled = false, mergeDelay = 0.5, } local processedEggs = {} local eggRunning = false local cashRunning = false local sellerRunning = false local buyRunning = false local mergeRunning = false local lastDepositTime = 0 local lastBuyTime = 0 local lastMergeTime = 0 local isDepositing = false local sellerNoMoney = false local buyNoMoney = false local mergeNoChickens = false local CollectEvent = nil local RemoteFunction = nil local Paper = ReplicatedStorage:FindFirstChild("Paper") if Paper then local Remotes = Paper:FindFirstChild("Remotes") if Remotes then CollectEvent = Remotes:FindFirstChild("__remoteevent") RemoteFunction = Remotes:FindFirstChild("__remotefunction") end end if CollectEvent then local function getRootPart() local char = LocalPlayer.Character return char and char:FindFirstChild("HumanoidRootPart") end local function getPlayerPosition() local root = getRootPart() return root and root.Position end local function findNearbyEggs() local eggs = {} local pos = getPlayerPosition() if not pos then return eggs end local eggsContainer = Workspace:FindFirstChild("Eggs") if not eggsContainer then return eggs end for _, obj in ipairs(eggsContainer:GetChildren()) do if obj:IsA("Model") then local eggId = obj.Name local tier = obj:GetAttribute("Tier") if tier and not processedEggs[eggId] then local eggPos = nil local primary = obj:FindFirstChild("PrimaryPart") if primary then eggPos = primary.Position end if not eggPos then local root = obj:FindFirstChild("HumanoidRootPart") if root then eggPos = root.Position end end if not eggPos then eggPos = obj:GetPivot().Position end if eggPos then local distance = (eggPos - pos).Magnitude if distance <= config.eggRadius then table.insert(eggs, {Object = obj, Id = eggId, Position = eggPos, Distance = distance}) end end end end end table.sort(eggs, function(a, b) return a.Distance < b.Distance end) return eggs end local function teleportToEgg(pos) local root = getRootPart() if not root then return false end pcall(function() root.CFrame = CFrame.new(Vector3.new(pos.X, pos.Y + 3, pos.Z)) end) task.wait(0.2) return true end local function collectEgg(egg) if not egg or not egg.Id or processedEggs[egg.Id] then return false end processedEggs[egg.Id] = true pcall(function() CollectEvent:FireServer("Collect Egg", egg.Id) end) return true end local function depositEggs() if not RemoteFunction or isDepositing then return false end isDepositing = true local success = false pcall(function() local result = RemoteFunction:InvokeServer("Deposit Eggs") success = type(result) == "table" and (result[1] or false) or (result or false) end) isDepositing = false if success then lastDepositTime = tick() end return success end local function collectCash() if not RemoteFunction then return false end local success = false pcall(function() local result = RemoteFunction:InvokeServer("Collect Cash") success = type(result) == "table" and (result[1] or false) or (result or false) end) return success end local function upgradeSeller() if not RemoteFunction then return false end local success = false local message = "" pcall(function() local result = RemoteFunction:InvokeServer("Upgrade Process Level") success = type(result) == "table" and (result[1] or false) or (result or false) message = type(result) == "table" and (result[2] or "") or "" end) if not success and message:find("need") or message:find("afford") then sellerNoMoney = true end return success end local function buyChickens() if not RemoteFunction then return false end local now = tick() if now - lastBuyTime < config.buyDelay then return false end local success = false local message = "" pcall(function() local result = RemoteFunction:InvokeServer("Buy Chickens", config.buyAmount) success = type(result) == "table" and (result[1] or false) or (result or false) message = type(result) == "table" and (result[2] or "") or "" end) lastBuyTime = now if not success and (message:find("need") or message:find("afford") or message:find("money")) then buyNoMoney = true end return success end local function mergeChickens() if not RemoteFunction then return false end local now = tick() if now - lastMergeTime < config.mergeDelay then return false end local success = false local message = "" pcall(function() local result = RemoteFunction:InvokeServer("Merge Chickens") success = type(result) == "table" and (result[1] or false) or (result or false) message = type(result) == "table" and (result[2] or "") or "" end) lastMergeTime = now if not success and (message:find("need") or message:find("enough") or message:find("chicken")) then mergeNoChickens = true end return success end local function eggLoop() local noEggsCount = 0 lastDepositTime = tick() while eggRunning do task.wait(0.1) if not config.eggEnabled then task.wait(0.5); continue end if config.eggAutoDeposit and tick() - lastDepositTime >= config.eggDepositInterval then depositEggs() end local eggs = findNearbyEggs() if #eggs > 0 then noEggsCount = 0 for _, egg in ipairs(eggs) do if not eggRunning or not config.eggEnabled then break end if not egg.Object or not egg.Object.Parent then continue end teleportToEgg(egg.Position) task.wait(0.2) collectEgg(egg) task.wait(0.2) end task.wait(1) else noEggsCount = noEggsCount + 1 if noEggsCount >= 3 and config.eggAutoDeposit then depositEggs() noEggsCount = 0 task.wait(2) else task.wait(2) end end end end local function cashLoop() while cashRunning do task.wait(0.1) if not config.cashEnabled then task.wait(0.5); continue end collectCash() task.wait(config.cashDelay) end end local function sellerLoop() sellerNoMoney = false while sellerRunning do task.wait(0.1) if not config.sellerEnabled or sellerNoMoney then if sellerNoMoney then task.wait(5) end task.wait(0.5); continue end upgradeSeller() task.wait(config.sellerDelay) end end local function buyLoop() buyNoMoney = false lastBuyTime = 0 while buyRunning do task.wait(0.1) if not config.buyEnabled or buyNoMoney then if buyNoMoney then task.wait(5) end task.wait(0.5); continue end buyChickens() task.wait(config.buyDelay) end end local function mergeLoop() mergeNoChickens = false lastMergeTime = 0 while mergeRunning do task.wait(0.1) if not config.mergeEnabled or mergeNoChickens then if mergeNoChickens then task.wait(5) end task.wait(0.5); continue end mergeChickens() task.wait(config.mergeDelay) end end local function toggleEgg(state) config.eggEnabled = state if state then lastDepositTime = tick() if not eggRunning then eggRunning = true; task.spawn(eggLoop) end end end local function toggleCash(state) config.cashEnabled = state if state and not cashRunning then cashRunning = true; task.spawn(cashLoop) end end local function toggleSeller(state) config.sellerEnabled = state if state and not sellerRunning then sellerRunning = true; sellerNoMoney = false; task.spawn(sellerLoop) end end local function toggleBuy(state) config.buyEnabled = state if state then buyNoMoney = false if not buyRunning then buyRunning = true; task.spawn(buyLoop) end end end local function toggleMerge(state) config.mergeEnabled = state if state then mergeNoChickens = false if not mergeRunning then mergeRunning = true; task.spawn(mergeLoop) end end end chickenSection:Toggle({Title = "自动捡蛋", Value = false, Callback = toggleEgg}) chickenSection:Slider({Title = "捡蛋范围", Value = {Min = 10, Max = 80, Default = 40}, Callback = function(v) config.eggRadius = math.floor(v) end}) chickenSection:Toggle({Title = "自动存蛋", Value = true, Callback = function(s) config.eggAutoDeposit = s; if s then lastDepositTime = tick() end end}) chickenSection:Slider({Title = "存蛋间隔", Value = {Min = 10, Max = 300, Default = 60}, Callback = function(v) config.eggDepositInterval = math.floor(v) end}) chickenSection:Button({Title = "手动存蛋", Callback = depositEggs}) chickenSection:Divider() chickenSection:Toggle({Title = "自动收钱", Value = false, Callback = toggleCash}) chickenSection:Slider({Title = "收钱间隔", Value = {Min = 0.1, Max = 2, Default = 0.5}, Callback = function(v) config.cashDelay = v end}) chickenSection:Divider() chickenSection:Toggle({Title = "自动升级出售", Value = false, Callback = toggleSeller}) chickenSection:Slider({Title = "升级间隔", Value = {Min = 0.2, Max = 2, Default = 0.5}, Callback = function(v) config.sellerDelay = v end}) chickenSection:Divider() chickenSection:Toggle({Title = "自动买鸡", Value = false, Callback = toggleBuy}) chickenSection:Slider({Title = "买鸡数量", Value = {Min = 1, Max = 100, Default = 1}, Callback = function(v) config.buyAmount = math.floor(v) end}) chickenSection:Slider({Title = "买鸡间隔", Value = {Min = 0.2, Max = 2, Default = 0.5}, Callback = function(v) config.buyDelay = v end}) chickenSection:Toggle({Title = "自动合鸡", Value = false, Callback = toggleMerge}) chickenSection:Slider({Title = "合鸡间隔", Value = {Min = 0.2, Max = 2, Default = 0.5}, Callback = function(v) config.mergeDelay = v end}) end end local disasterSection = tab:Section({ Title = "自然灾害", Collapsible = true, Box = true, DefaultOpen = false }) do local disasterFeatures = {waterWalk = false, rocksSolid = false, removeDisasterUI = false, removeFlood = false, mapVote = false, autoSurvive = false} local disasterConnections = {waterFollow = nil, floodFollow = nil, disasterUI = nil, autoTp = nil} local floodPlatform = nil local floodPart = nil local floodEnabled = false local function toggleWaterWalk(val) disasterFeatures.waterWalk = val local water = Workspace:FindFirstChild("WaterLevel") if not water then return end if val then water.CanCollide = true water.Size = Vector3.new(1000, 1, 1000) if disasterConnections.waterFollow then disasterConnections.waterFollow:Disconnect() end disasterConnections.waterFollow = RunService.Heartbeat:Connect(function() local char = LocalPlayer.Character local root = char and char:FindFirstChild("HumanoidRootPart") if root and water then water.Position = Vector3.new(root.Position.X, water.Position.Y, root.Position.Z) end end) else water.Size = Vector3.new(10, 1, 10) water.CanCollide = false if disasterConnections.waterFollow then disasterConnections.waterFollow:Disconnect(); disasterConnections.waterFollow = nil end end end local function toggleRocksSolid(val) disasterFeatures.rocksSolid = val for _, v in ipairs(Workspace:GetDescendants()) do if v.Name == "LowerRocks" and v:IsA("BasePart") then v.CanCollide = val end end end local function toggleRemoveDisasterUI(val) disasterFeatures.removeDisasterUI = val local function removeDisasterGuis() local pgui = LocalPlayer:FindFirstChild("PlayerGui") if not pgui then return end local sand = pgui:FindFirstChild("SandStormGui") if sand then sand:Destroy() end local blizzard = pgui:FindFirstChild("BlizzardGui") if blizzard then blizzard:Destroy() end end if val then removeDisasterGuis() local pgui = LocalPlayer:WaitForChild("PlayerGui") disasterConnections.disasterUI = pgui.ChildAdded:Connect(function(child) if child.Name == "SandStormGui" or child.Name == "BlizzardGui" then task.wait(); child:Destroy() end end) else if disasterConnections.disasterUI then disasterConnections.disasterUI:Disconnect(); disasterConnections.disasterUI = nil end end end local function createFloodPlatform() if floodPlatform then return end floodPlatform = Instance.new("Part") floodPlatform.Name = "FloodPlatform" floodPlatform.Size = Vector3.new(10000, 0.5, 10000) floodPlatform.CanCollide = true floodPlatform.Anchored = true floodPlatform.Transparency = 0.2 floodPlatform.BrickColor = BrickColor.new("Bright blue") floodPlatform.Material = Enum.Material.SmoothPlastic floodPlatform.Parent = Workspace end local function findFlood() local structure = Workspace:FindFirstChild("Structure") if structure then floodPart = structure:FindFirstChild("FloodLevel") end if not floodPart then floodPart = Workspace:FindFirstChild("FloodLevel") end return floodPart end local function updateFloodPlatform() if not floodPlatform or not floodPart then return end local char = LocalPlayer.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end floodPlatform.Position = Vector3.new(root.Position.X, floodPart.Position.Y + 0.5, root.Position.Z) end local function startFlood() if floodEnabled then return end createFloodPlatform() findFlood() if disasterConnections.floodFollow then disasterConnections.floodFollow:Disconnect() end disasterConnections.floodFollow = RunService.Heartbeat:Connect(updateFloodPlatform) floodEnabled = true end local function stopFlood() floodEnabled = false if disasterConnections.floodFollow then disasterConnections.floodFollow:Disconnect(); disasterConnections.floodFollow = nil end if floodPlatform then floodPlatform:Destroy(); floodPlatform = nil end floodPart = nil end local function toggleRemoveFlood(val) if val then startFlood() else stopFlood() end end local function toggleMapVote(val) disasterFeatures.mapVote = val local playerGui = LocalPlayer:FindFirstChild("PlayerGui") local mainGui = playerGui and playerGui:FindFirstChild("MainGui") local mapPage = mainGui and mainGui:FindFirstChild("MapVotePage") if mapPage then mapPage.Visible = val end end local function toggleAutoSurvive(val) disasterFeatures.autoSurvive = val if val then disasterConnections.autoTp = RunService.Stepped:Connect(function() if LocalPlayer and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(-273, 179.5, 394) end end) else if disasterConnections.autoTp then disasterConnections.autoTp:Disconnect(); disasterConnections.autoTp = nil end end end disasterSection:Toggle({Title = "水上行走", Value = false, Callback = toggleWaterWalk}) disasterSection:Toggle({Title = "岩石实体化", Value = false, Callback = toggleRocksSolid}) disasterSection:Toggle({Title = "移除灾害视角", Value = false, Callback = toggleRemoveDisasterUI}) disasterSection:Toggle({Title = "去除洪水", Value = false, Callback = toggleRemoveFlood}) disasterSection:Toggle({Title = "地图投票", Value = false, Callback = toggleMapVote}) disasterSection:Toggle({Title = "自动存活", Value = false, Callback = toggleAutoSurvive}) local teleportSection = disasterSection:Section({Title = "传送", Collapsible = true, Box = true, DefaultOpen = false}) teleportSection:Button({Title = "传送出生岛", Callback = function() local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = CFrame.new(-247.14, 180.45, 309.40) WindUI:Notify({Title = "传送出生岛", Content = "已传送", Duration = 2, Icon = "check"}) end end}) teleportSection:Button({Title = "传送岛屿", Callback = function() local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = CFrame.new(-104.44, 48.65, 13.03) WindUI:Notify({Title = "传送岛屿", Content = "已传送", Duration = 2, Icon = "check"}) end end}) teleportSection:Button({Title = "传送虚空", Callback = function() local char = LocalPlayer.Character if char and char:FindFirstChild("HumanoidRootPart") then char.HumanoidRootPart.CFrame = CFrame.new(0, 100000000, 0) WindUI:Notify({Title = "传送虚空", Content = "已传送", Duration = 2, Icon = "check"}) end end}) end local grassSection = tab:Section({ Title = "+1割草冒险", Collapsible = true, Box = true, DefaultOpen = false }) do local autoSellActive = false local removeGrassActive = false grassSection:Toggle({Title = "自动出售", Value = false, Callback = function(state) autoSellActive = state if state then task.spawn(function() while autoSellActive do task.wait(0.2) for _, container in ipairs({ReplicatedStorage, LocalPlayer:FindFirstChild("PlayerGui"), Workspace}) do if container then for _, remote in ipairs(container:GetDescendants()) do if remote:IsA("RemoteEvent") or remote:IsA("RemoteFunction") then local rname = string.lower(remote.Name) if string.find(rname, "sell") or string.find(rname, "sellall") or string.find(rname, "harvest") then pcall(function() if remote:IsA("RemoteEvent") then remote:FireServer("All") remote:FireServer(true) remote:FireServer() else remote:InvokeServer("All") end end) end end end end end end end) end end}) grassSection:Toggle({Title = "清除草地", Value = false, Callback = function(state) removeGrassActive = state if state then task.spawn(function() while removeGrassActive do task.wait(0.1) local char = LocalPlayer.Character local root = char and char:FindFirstChild("HumanoidRootPart") for _, obj in ipairs(Workspace:GetDescendants()) do if obj:IsA("BasePart") then local pname = string.lower(obj.Name) local parentName = string.lower(obj.Parent.Name) if string.find(pname, "grass") or string.find(parentName, "grass") then pcall(function() if obj:FindFirstChildOfClass("TouchTransmitter") and root and firetouchinterest then firetouchinterest(obj, root, 0) task.wait() firetouchinterest(obj, root, 1) end obj:Destroy() end) end end end for _, container in ipairs({ReplicatedStorage, LocalPlayer:FindFirstChild("PlayerGui"), Workspace}) do if container then for _, item in ipairs(container:GetDescendants()) do if item:IsA("RemoteEvent") or item:IsA("RemoteFunction") then local lname = string.lower(item.Name) if string.find(lname, "cut") or string.find(lname, "swing") or string.find(lname, "grass") or string.find(lname, "remove") then pcall(function() if item:IsA("RemoteEvent") then item:FireServer() item:FireServer(true) else item:InvokeServer() end end) end end end end end end end) end end}) end local magicSection = tab:Section({ Title = "+1魔法进化", Collapsible = true, Box = true, DefaultOpen = false }) do local autoWinActive = false local autoWinConnection = nil local autoWinCoords = { x = 3568.0078125, y = 1.9449999332427979, z = 8.189765930175781 } local autoRebirthActive = false local autoRebirthConnection = nil local autoClickerActive = false local autoClickerConnection = nil local godModeActive = false local godModeConnection = nil local godModeCharAddedConn = nil local RebirthRemote = nil local GainMagicPower = nil task.spawn(function() local remotesFolder = ReplicatedStorage:FindFirstChild("Remotes") if remotesFolder then RebirthRemote = remotesFolder:FindFirstChild("Rebirth") GainMagicPower = remotesFolder:FindFirstChild("GainMagicPower") end if not RebirthRemote then RebirthRemote = ReplicatedStorage:FindFirstChild("Rebirth", true) end if not GainMagicPower then GainMagicPower = ReplicatedStorage:FindFirstChild("GainMagicPower", true) end end) local function ApplyGodmode(char) if not char or not godModeActive then return end local humanoid = char:FindFirstChild("Humanoid") if not humanoid then return end pcall(function() humanoid.MaxHealth = 1e8; humanoid.Health = humanoid.MaxHealth end) end magicSection:Toggle({Title = "自动点击", Value = false, Callback = function(val) autoClickerActive = val if autoClickerConnection then autoClickerConnection:Disconnect(); autoClickerConnection = nil end if val then autoClickerConnection = RunService.Heartbeat:Connect(function() if GainMagicPower then GainMagicPower:FireServer() end end) end end}) magicSection:Toggle({Title = "无限胜利", Value = false, Callback = function(val) autoWinActive = val if autoWinConnection then autoWinConnection:Disconnect(); autoWinConnection = nil end if val then autoWinConnection = RunService.Heartbeat:Connect(function() local character = LocalPlayer.Character local hrp = character and character:FindFirstChild("HumanoidRootPart") if hrp then hrp.CFrame = CFrame.new(autoWinCoords.x, autoWinCoords.y, autoWinCoords.z) end end) end end}) magicSection:Toggle({Title = "自动重生", Value = false, Callback = function(val) autoRebirthActive = val if autoRebirthConnection then autoRebirthConnection = nil end if val then autoRebirthConnection = task.spawn(function() while autoRebirthActive do if RebirthRemote then RebirthRemote:FireServer() end task.wait(1) end end) end end}) magicSection:Toggle({Title = "无敌模式", Value = false, Callback = function(val) godModeActive = val if godModeConnection then godModeConnection:Disconnect(); godModeConnection = nil end if godModeCharAddedConn then godModeCharAddedConn:Disconnect(); godModeCharAddedConn = nil end if val then godModeConnection = RunService.Stepped:Connect(function() pcall(function() local character = LocalPlayer.Character if not character then return end local humanoid = character:FindFirstChildOfClass("Humanoid") if humanoid then if humanoid.MaxHealth < 1e8 then humanoid.MaxHealth = 1e8 end if humanoid.Health < humanoid.MaxHealth then humanoid.Health = humanoid.MaxHealth end end end) end) godModeCharAddedConn = LocalPlayer.CharacterAdded:Connect(function(newChar) task.wait(1.2) ApplyGodmode(newChar) end) ApplyGodmode(LocalPlayer.Character) else local character = LocalPlayer.Character local humanoid = character and character:FindFirstChildOfClass("Humanoid") if humanoid then humanoid.MaxHealth = 100; humanoid.Health = 100 end end end}) end local coinSection = tab:Section({ Title = "扔出一枚硬币", Collapsible = true, Box = true, DefaultOpen = false }) do local coinList = {"Paradox Coin", "Lucky Coin", "Golden Coin", "Obsidian Coin", "Platinum Coin", "Ruby Coin", "Emerald Coin", "Amethyst Coin", "Topaz Coin", "Diamond Coin", "Staff Token", "VIP Token", "Developer Token", "Diamond Token"} local throwRemote = nil local sellRemote = nil local upgradeRemote = nil local function getCoinName() local pg = LocalPlayer:FindFirstChild("PlayerGui") if not pg then return nil end local coinLabel = pg:FindFirstChild("UiFolder") and pg.UiFolder:FindFirstChild("Main") and pg.UiFolder.Main:FindFirstChild("HUD") and pg.UiFolder.Main.HUD:FindFirstChild("Coin") and pg.UiFolder.Main.HUD.Coin:FindFirstChild("Main") and pg.UiFolder.Main.HUD.Coin.Main:FindFirstChild("CoinName") return coinLabel and coinLabel:IsA("TextLabel") and coinLabel.Text ~= "" and coinLabel.Text or nil end local function getMultiplier() local pg = LocalPlayer:FindFirstChild("PlayerGui") if not pg then return 0 end local multBar = pg:FindFirstChild("UiFolder") and pg.UiFolder:FindFirstChild("Main") and pg.UiFolder.Main:FindFirstChild("HUD") and pg.UiFolder.Main.HUD:FindFirstChild("ThrowBar") and pg.UiFolder.Main.HUD.ThrowBar:FindFirstChild("CurrentMulti") return multBar and multBar:IsA("Frame") and multBar.Size.X.Scale * 3 or 0 end local function getHRP() local char = LocalPlayer.Character return char and char:FindFirstChild("HumanoidRootPart") end local function scanRemotes() for _, item in ipairs(ReplicatedStorage:GetDescendants()) do if item:IsA("RemoteEvent") or item:IsA("RemoteFunction") then local name = string.lower(item.Name) if string.find(name, "throw") or string.find(name, "toss") or string.find(name, "coin") then if not throwRemote then throwRemote = item end elseif string.find(name, "sell") then if not sellRemote then sellRemote = item end elseif string.find(name, "upgrade") or string.find(name, "buy") then if not upgradeRemote then upgradeRemote = item end end end end end pcall(scanRemotes) local autoThrowActive = false local throwMultiplier = 3 local autoSellActive = false local autoBuyActive = false local autoUpgradeLuck = false local autoUpgradeValue = false task.spawn(function() while true do task.wait(0.2) if autoThrowActive and throwRemote then local currentMult = getMultiplier() if currentMult >= throwMultiplier then local coinName = getCoinName() if not coinName then continue end local hrp = getHRP() if not hrp then continue end local pos = hrp.Position + Vector3.new(0, -0.5, -2) pcall(function() throwRemote:FireServer(throwMultiplier, pos, coinName, nil, nil) end) end end end end) task.spawn(function() while true do task.wait(0.5) if autoSellActive and sellRemote then pcall(function() sellRemote:FireServer() end) task.wait(2) end end end) task.spawn(function() while true do task.wait(1.0) if (not autoUpgradeLuck and not autoUpgradeValue) then continue end if upgradeRemote then if autoUpgradeLuck then pcall(function() upgradeRemote:FireServer("Luck Multiplier") end) end if autoUpgradeValue then pcall(function() upgradeRemote:FireServer("Value Multiplier") end) end task.wait(1.5) end end end) task.spawn(function() while true do task.wait(1.0) if not autoBuyActive then continue end if not getCoinName() then local buyRemote = ReplicatedStorage:FindFirstChild("Assets") and ReplicatedStorage.Assets:FindFirstChild("Events") and ReplicatedStorage.Assets.Events:FindFirstChild("BuyCoin") if buyRemote then for _, name in ipairs(coinList) do pcall(function() buyRemote:FireServer(name) end) task.wait(0.1) end task.wait(3) end end end end) coinSection:Toggle({Title = "自动投币", Value = false, Callback = function(s) autoThrowActive = s end}) coinSection:Slider({Title = "投币倍率", Step = 0.1, Value = {Min = 1, Max = 3, Default = 3}, Width = 200, IsTextbox = true, Callback = function(v) throwMultiplier = v end}) coinSection:Divider() coinSection:Toggle({Title = "自动出售", Value = false, Callback = function(s) autoSellActive = s end}) coinSection:Divider() coinSection:Toggle({Title = "自动购买硬币", Value = false, Callback = function(s) autoBuyActive = s end}) coinSection:Divider() local upgradeSubSection = coinSection:Section({Title = "自动升级", Collapsible = true, Box = true, DefaultOpen = false}) upgradeSubSection:Toggle({Title = "升级（运气）", Value = false, Callback = function(s) autoUpgradeLuck = s end}) upgradeSubSection:Toggle({Title = "升级（价值）", Value = false, Callback = function(s) autoUpgradeValue = s end}) end local ageSection = tab:Section({ Title = "每点击+1年龄", Collapsible = true, Box = true, DefaultOpen = false }) do local spinsLock = false local spinsLockConn = nil ageSection:Toggle({Title = "锁定转盘次数5", Value = false, Callback = function(state) spinsLock = state if state then local spins = LocalPlayer:FindFirstChild("Spins") if spins then spins.Value = 5 end spinsLockConn = spins:GetPropertyChangedSignal("Value"):Connect(function() if spinsLock and spins.Value ~= 5 then spins.Value = 5 end end) else if spinsLockConn then spinsLockConn:Disconnect(); spinsLockConn = nil end end end}) end local spaceBallSection = tab:Section({ Title = "将球踢向外太空", Collapsible = true, Box = true, DefaultOpen = false }) do local spaceClickActive = false local spaceKickActive = false local spaceClickConnection = nil local spaceKickConnection = nil local function getSpaceRemotes() local remotes = {} local paper = ReplicatedStorage:FindFirstChild("Paper") if paper then local remotesFolder = paper:FindFirstChild("Remotes") if remotesFolder then for _, child in ipairs(remotesFolder:GetChildren()) do if child:IsA("RemoteEvent") or child:IsA("RemoteFunction") then table.insert(remotes, child) end end end end for _, child in ipairs(ReplicatedStorage:GetChildren()) do if child:IsA("RemoteEvent") or child:IsA("RemoteFunction") then table.insert(remotes, child) end end return remotes end spaceBallSection:Toggle({ Title = "自动点击", Value = false, Callback = function(state) spaceClickActive = state if spaceClickConnection then spaceClickConnection:Disconnect() spaceClickConnection = nil end if state then spaceClickConnection = RunService.Heartbeat:Connect(function() if not spaceClickActive then return end local remotes = getSpaceRemotes() for _, remote in ipairs(remotes) do local name = string.lower(remote.Name) if string.find(name, "click") or string.find(name, "power") or string.find(name, "train") or string.find(name, "addpower") or string.find(name, "tap") or string.find(name, "gain") then pcall(function() if remote:IsA("RemoteEvent") then remote:FireServer() elseif remote:IsA("RemoteFunction") then remote:InvokeServer() end end) end end end) end end }) spaceBallSection:Toggle({ Title = "自动踢球", Value = false, Callback = function(state) spaceKickActive = state if spaceKickConnection then spaceKickConnection:Disconnect() spaceKickConnection = nil end if state then spaceKickConnection = RunService.Heartbeat:Connect(function() if not spaceKickActive then return end local remotes = getSpaceRemotes() for _, remote in ipairs(remotes) do local name = string.lower(remote.Name) if string.find(name, "kick") or string.find(name, "shoot") or string.find(name, "ball") or string.find(name, "launch") or string.find(name, "win") or string.find(name, "claim") or string.find(name, "cash") or string.find(name, "score") then pcall(function() if remote:IsA("RemoteEvent") then remote:FireServer() elseif remote:IsA("RemoteFunction") then remote:InvokeServer() end end) end end end) end end }) end local keyboardSection = tab:Section({ Title = "+1键盘速度逃脱", Collapsible = true, Box = true, DefaultOpen = false }) do keyboardSection:Button({Title = "解锁音效", Callback = function() local ids = {1928718370, 1724877043} for _, id in ipairs(ids) do pcall(function() MarketplaceService:SignalPromptGamePassPurchaseFinished(LocalPlayer, id, true) end) pcall(function() MarketplaceService:SignalPromptPurchaseFinished(LocalPlayer, id, true) end) pcall(function() MarketplaceService:SignalPromptProductPurchaseFinished(LocalPlayer, id, true) end) end if hookfunction then local oldOwns oldOwns = hookfunction(MarketplaceService.UserOwnsGamePassAsync, function(self, p, passId) if typeof(passId) == "number" and table.find(ids, passId) and p == LocalPlayer then return true end return oldOwns(self, p, passId) end) end end}) end local slimeSection = tab:Section({ Title = "+1速度史莱姆键盘逃脱", Collapsible = true, Box = true, DefaultOpen = false }) do local targetPosition1 = Vector3.new(1322, 199, -4761) local targetPosition2 = Vector3.new(-1340.25, 249.68, 6438.55) local delayBetweenTeleports = 0.1 local autoFarmActive1 = false local autoFarmActive2 = false local teleportLoopConnection = nil local function startTeleportLoop() if teleportLoopConnection then return end teleportLoopConnection = RunService.Heartbeat:Connect(function() local char = LocalPlayer.Character if not char then return end local root = char:FindFirstChild("HumanoidRootPart") if not root then return end if autoFarmActive1 then root.CFrame = CFrame.new(targetPosition1) elseif autoFarmActive2 then root.CFrame = CFrame.new(targetPosition2) end task.wait(delayBetweenTeleports) end) end local function stopTeleportLoop() if teleportLoopConnection then teleportLoopConnection:Disconnect(); teleportLoopConnection = nil end end local toggle1 = slimeSection:Toggle({Title = "自动胜利世界1", Value = false, Callback = function(val) if val and autoFarmActive2 then autoFarmActive2 = false; toggle2:SetValue(false) end autoFarmActive1 = val if autoFarmActive1 or autoFarmActive2 then startTeleportLoop() else stopTeleportLoop() end end}) local toggle2 = slimeSection:Toggle({Title = "自动胜利世界2", Value = false, Callback = function(val) if val and autoFarmActive1 then autoFarmActive1 = false; toggle1:SetValue(false) end autoFarmActive2 = val if autoFarmActive1 or autoFarmActive2 then startTeleportLoop() else stopTeleportLoop() end end}) slimeSection:Button({Title = "解锁音效", Callback = function() local ids = {1836201958} for _, id in ipairs(ids) do pcall(function() MarketplaceService:SignalPromptGamePassPurchaseFinished(LocalPlayer, id, true) end) pcall(function() MarketplaceService:SignalPromptPurchaseFinished(LocalPlayer, id, true) end) pcall(function() MarketplaceService:SignalPromptProductPurchaseFinished(LocalPlayer, id, true) end) end if hookfunction then local oldOwns oldOwns = hookfunction(MarketplaceService.UserOwnsGamePassAsync, function(self, p, passId) if typeof(passId) == "number" and table.find(ids, passId) and p == LocalPlayer then return true end return oldOwns(self, p, passId) end) end end}) end return tab end
