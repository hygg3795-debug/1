return function(Window, WindUI)
    local GameName = "扔出一枚硬币"
    local CreatorName = "Badshah"

    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local Players = game:GetService("Players")
    local LocalPlayer = Players.LocalPlayer
    local UserInputService = game:GetService("UserInputService")

    local tab = Window:Tab({Title = GameName, Icon = "coins"})

    local autoThrowKey = "T"
    local autoSellKey = "Y"
    local autoUpgradeKey = "U"

    _G.AutoThrowActive = false
    _G.AutoSellActive = false
    _G.AutoUpgradeActive = false

    _G.ThrowRemote = nil
    _G.ThrowArgs = {}

    _G.SellRemote = nil
    _G.SellArgs = {}

    _G.UpgradeRemote = nil
    _G.UpgradeArgs = {}

    local function scanRemotes()
        for _, item in ipairs(ReplicatedStorage:GetDescendants()) do
            if item:IsA("RemoteEvent") or item:IsA("RemoteFunction") then
                local name = string.lower(item.Name)
                if string.find(name, "throw") or string.find(name, "toss") or string.find(name, "coin") then
                    if not _G.ThrowRemote then
                        _G.ThrowRemote = item
                        print("[自动挂机] 找到投币远程事件:", item:GetFullName())
                        _G.ThrowArgs = {}
                    end
                elseif string.find(name, "sell") then
                    if not _G.SellRemote then
                        _G.SellRemote = item
                        print("[自动挂机] 找到出售远程事件:", item:GetFullName())
                        _G.SellArgs = {}
                    end
                elseif string.find(name, "upgrade") or string.find(name, "buy") then
                    if not _G.UpgradeRemote then
                        _G.UpgradeRemote = item
                        print("[自动挂机] 找到升级远程事件:", item:GetFullName())
                        _G.UpgradeArgs = {}
                    end
                end
            end
        end
    end
    pcall(scanRemotes)

    local hookSuccess, hookError = pcall(function()
        local oldNamecall
        oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
            local method = getnamecallmethod()
            local args = {...}
            
            if method == "FireServer" or method == "InvokeServer" then
                local name = string.lower(self.Name)
                if string.find(name, "throw") or string.find(name, "toss") or string.find(name, "coin") then
                    _G.ThrowRemote = self
                    _G.ThrowArgs = args
                elseif string.find(name, "sell") then
                    _G.SellRemote = self
                    _G.SellArgs = args
                elseif string.find(name, "upgrade") or string.find(name, "buy") then
                    _G.UpgradeRemote = self
                    _G.UpgradeArgs = args
                end
            end
            return oldNamecall(self, ...)
        end)
        print("[自动挂机] 方法拦截钩子注入成功!")
    end)

    if not hookSuccess then
        warn("[自动挂机] 方法拦截钩子注入失败 (在Roblox Studio中为正常现象):", hookError)
    end

    -- 自动投币循环
    task.spawn(function()
        while true do
            task.wait(0.1)
            if _G.AutoThrowActive and _G.ThrowRemote then
                local args = _G.ThrowArgs or {}
                pcall(function()
                    if _G.ThrowRemote:IsA("RemoteEvent") then
                        _G.ThrowRemote:FireServer(unpack(args))
                    elseif _G.ThrowRemote:IsA("RemoteFunction") then
                        _G.ThrowRemote:InvokeServer(unpack(args))
                    end
                end)
            end
        end
    end)

    -- 自动出售循环
    task.spawn(function()
        while true do
            task.wait(1.0)
            if _G.AutoSellActive and _G.SellRemote then
                local args = _G.SellArgs or {}
                pcall(function()
                    if _G.SellRemote:IsA("RemoteEvent") then
                        _G.SellRemote:FireServer(unpack(args))
                    elseif _G.SellRemote:IsA("RemoteFunction") then
                        _G.SellRemote:InvokeServer(unpack(args))
                    end
                end)
            end
        end
    end)

    -- 自动升级循环
    task.spawn(function()
        while true do
            task.wait(1.5)
            if _G.AutoUpgradeActive and _G.UpgradeRemote then
                local args = _G.UpgradeArgs or {}
                pcall(function()
                    if _G.UpgradeRemote:IsA("RemoteEvent") then
                        _G.UpgradeRemote:FireServer(unpack(args))
                    elseif _G.UpgradeRemote:IsA("RemoteFunction") then
                        _G.UpgradeRemote:InvokeServer(unpack(args))
                    end
                end)
            end
        end
    end)

    -- ========== UI控件 ==========

    -- 自动投币
    tab:Toggle({
        Title = "自动投币",
        Value = false,
        Callback = function(state)
            _G.AutoThrowActive = state
            print("[扔出一枚硬币] 自动投币设置为:", state)
        end
    })

    tab:Keybind({
        Title = "自动投币快捷键",
        Value = "T",
        Callback = function()
            _G.AutoThrowActive = not _G.AutoThrowActive
            print("[扔出一枚硬币] 自动投币切换为:", _G.AutoThrowActive)
        end
    })

    tab:Divider()

    -- 自动出售
    tab:Toggle({
        Title = "自动出售",
        Value = false,
        Callback = function(state)
            _G.AutoSellActive = state
            print("[扔出一枚硬币] 自动出售设置为:", state)
        end
    })

    tab:Keybind({
        Title = "自动出售快捷键",
        Value = "Y",
        Callback = function()
            _G.AutoSellActive = not _G.AutoSellActive
            print("[扔出一枚硬币] 自动出售切换为:", _G.AutoSellActive)
        end
    })

    tab:Divider()

    -- 自动升级
    tab:Toggle({
        Title = "自动升级",
        Value = false,
        Callback = function(state)
            _G.AutoUpgradeActive = state
            print("[扔出一枚硬币] 自动升级设置为:", state)
        end
    })

    tab:Keybind({
        Title = "自动升级快捷键",
        Value = "U",
        Callback = function()
            _G.AutoUpgradeActive = not _G.AutoUpgradeActive
            print("[扔出一枚硬币] 自动升级切换为:", _G.AutoUpgradeActive)
        end
    })

    tab:Divider()

    -- 状态显示
    local statusPara = tab:Paragraph({
        Title = "状态",
        Desc = "自动投币: 关闭 | 自动出售: 关闭 | 自动升级: 关闭"
    })

    -- 更新状态
    task.spawn(function()
        while true do
            task.wait(0.5)
            local status = string.format(
                "自动投币: %s | 自动出售: %s | 自动升级: %s\n快捷键: [%s] 投币 [%s] 出售 [%s] 升级",
                _G.AutoThrowActive and "开启" or "关闭",
                _G.AutoSellActive and "开启" or "关闭",
                _G.AutoUpgradeActive and "开启" or "关闭",
                autoThrowKey, autoSellKey, autoUpgradeKey
            )
            pcall(function() statusPara:setDesc(status) end)
        end
    end)

    -- 底部信息
    tab:Paragraph({
        Title = "作者",
        Desc = CreatorName
    })

    return tab
end