return function(Window, WindUI)
    local GameName = "扔出一枚硬币"
    local ReplicatedStorage = game:GetService("ReplicatedStorage")
    local tab = Window:Tab({Title = GameName, Icon = "coins"})

    _G.AutoThrowActive = false
    _G.AutoSellActive = false
    _G.ThrowMultiplier = 1.0
    _G.UpgradeCoinActive = false
    _G.UpgradeValueActive = false
    _G.UpgradeLuckActive = false

    _G.ThrowRemote = nil
    _G.ThrowArgs = {}
    _G.SellRemote = nil
    _G.SellArgs = {}
    _G.UpgradeRemote = nil
    _G.UpgradeArgs = {}

    local function scanRemotes()
        for _, item in ipairs(ReplicatedStorage:GetDescendants()) do
            if item:IsA("RemoteEvent") or item:IsA("RemoteFunction") then
                local name = string.lower(item.Name)
                if string.find(name, "throw") or string.find(name, "toss") or string.find(name, "coin") then
                    if not _G.ThrowRemote then
                        _G.ThrowRemote = item
                        _G.ThrowArgs = {}
                    end
                elseif string.find(name, "sell") then
                    if not _G.SellRemote then
                        _G.SellRemote = item
                        _G.SellArgs = {}
                    end
                elseif string.find(name, "upgrade") or string.find(name, "buy") then
                    if not _G.UpgradeRemote then
                        _G.UpgradeRemote = item
                        _G.UpgradeArgs = {}
                    end
                end
            end
        end
    end
    pcall(scanRemotes)

    pcall(function()
        local oldNamecall
        oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
            local method = getnamecallmethod()
            local args = {...}
            
            if method == "FireServer" or method == "InvokeServer" then
                local name = string.lower(self.Name)
                if string.find(name, "throw") or string.find(name, "toss") or string.find(name, "coin") then
                    _G.ThrowRemote = self
                    _G.ThrowArgs = args
                elseif string.find(name, "sell") then
                    _G.SellRemote = self
                    _G.SellArgs = args
                elseif string.find(name, "upgrade") or string.find(name, "buy") then
                    _G.UpgradeRemote = self
                    _G.UpgradeArgs = args
                end
            end
            return oldNamecall(self, ...)
        end)
    end)

    task.spawn(function()
        while true do
            task.wait(0.1)
            if _G.AutoThrowActive and _G.ThrowRemote then
                local args = _G.ThrowArgs or {}
                if #args >= 1 and type(args[1]) == "number" then
                    args[1] = _G.ThrowMultiplier
                end
                pcall(function()
                    if _G.ThrowRemote:IsA("RemoteEvent") then
                        _G.ThrowRemote:FireServer(unpack(args))
                    elseif _G.ThrowRemote:IsA("RemoteFunction") then
                        _G.ThrowRemote:InvokeServer(unpack(args))
                    end
                end)
            end
        end
    end)

    task.spawn(function()
        while true do
            task.wait(1.0)
            if _G.AutoSellActive and _G.SellRemote then
                local args = _G.SellArgs or {}
                pcall(function()
                    if _G.SellRemote:IsA("RemoteEvent") then
                        _G.SellRemote:FireServer(unpack(args))
                    elseif _G.SellRemote:IsA("RemoteFunction") then
                        _G.SellRemote:InvokeServer(unpack(args))
                    end
                end)
            end
        end
    end)

    task.spawn(function()
        while true do
            task.wait(1.5)
            if _G.UpgradeRemote then
                if _G.UpgradeCoinActive then
                    local args = _G.UpgradeArgs or {}
                    pcall(function()
                        if _G.UpgradeRemote:IsA("RemoteEvent") then
                            _G.UpgradeRemote:FireServer(unpack(args))
                        elseif _G.UpgradeRemote:IsA("RemoteFunction") then
                            _G.UpgradeRemote:InvokeServer(unpack(args))
                        end
                    end)
                end
                if _G.UpgradeValueActive then
                    local args = _G.UpgradeArgs or {}
                    pcall(function()
                        if _G.UpgradeRemote:IsA("RemoteEvent") then
                            _G.UpgradeRemote:FireServer(unpack(args))
                        elseif _G.UpgradeRemote:IsA("RemoteFunction") then
                            _G.UpgradeRemote:InvokeServer(unpack(args))
                        end
                    end)
                end
                if _G.UpgradeLuckActive then
                    local args = _G.UpgradeArgs or {}
                    pcall(function()
                        if _G.UpgradeRemote:IsA("RemoteEvent") then
                            _G.UpgradeRemote:FireServer(unpack(args))
                        elseif _G.UpgradeRemote:IsA("RemoteFunction") then
                            _G.UpgradeRemote:InvokeServer(unpack(args))
                        end
                    end)
                end
            end
            task.wait(0.5)
        end
    end)

    local throwSection = tab:Section({
        Title = "投币设置",
        Icon = "coins",
        Collapsible = true,
        DefaultCollapsed = false
    })

    throwSection:Toggle({
        Title = "自动投币",
        Value = false,
        Callback = function(state)
            _G.AutoThrowActive = state
        end
    })

    throwSection:Slider({
        Title = "投币倍率",
        Value = {Min = 1, Max = 3, Default = 1},
        Step = 0.1,
        Width = 200,
        IsTextbox = true,
        Callback = function(value)
            _G.ThrowMultiplier = value
        end
    })

    throwSection:Toggle({
        Title = "自动出售",
        Value = false,
        Callback = function(state)
            _G.AutoSellActive = state
        end
    })

    local upgradeSection = tab:Section({
        Title = "升级",
        Icon = "arrow-up",
        Collapsible = true,
        DefaultCollapsed = true
    })

    upgradeSection:Toggle({
        Title = "升级硬币",
        Value = false,
        Callback = function(state)
            _G.UpgradeCoinActive = state
        end
    })

    upgradeSection:Toggle({
        Title = "升级价值倍率",
        Value = false,
        Callback = function(state)
            _G.UpgradeValueActive = state
        end
    })

    upgradeSection:Toggle({
        Title = "升级运气倍率",
        Value = false,
        Callback = function(state)
            _G.UpgradeLuckActive = state
        end
    })

    tab:Paragraph({
        Title = "状态",
        Desc = "自动投币: 关闭 | 自动出售: 关闭 | 倍率: 1.0x"
    })

    task.spawn(function()
        while true do
            task.wait(1)
            local status = string.format(
                "自动投币: %s | 自动出售: %s | 倍率: %.1fx",
                _G.AutoThrowActive and "开启" or "关闭",
                _G.AutoSellActive and "开启" or "关闭",
                _G.ThrowMultiplier
            )
            for _, child in pairs(tab:GetChildren()) do
                if child:IsA("Paragraph") or (child.setDesc and child.Title == "状态") then
                    pcall(function() child:setDesc(status) end)
                end
            end
        end
    end)

    return tab
end