-- 黑洞 Tab 模块（完全合并版：黑洞1/黑洞2的实现代码已直接内置，不再需要单独的 5.txt / 9.txt）
-- 用法（在主脚本 hygg 中）：
--   local loadBlackholeTab = loadstring(game:HttpGet("https://你的仓库地址/黑洞"))()
--   loadBlackholeTab(Window, WindUI)
--
-- 这个模块会创建"黑洞"Tab，内含一个下拉选择器（黑洞1/黑洞2）和一个总开关。
-- 两个黑洞互斥（同时只能开一个），各自的悬浮窗仍然独立生成、互不影响。

return function(Window, WindUI)

-- ================= 黑洞1 实现（原 5.txt） =================
local function createBlackhole1()
    local plr = game.Players.LocalPlayer
    local enabled = false
    local parts = {}
    local config = {radius = 50, height = 100, rotationSpeed = 10, attractionStrength = 1000}
    local con = nil
    local partCon = nil
    local removeCon = nil
    local screenGui = nil
    local mainFrame = nil
    local minimized = false
    local controls = {}

    local function retainPart(part)
        if part:IsA("BasePart") and not part.Anchored and part:IsDescendantOf(workspace) then
            if part.Parent == plr.Character or part:IsDescendantOf(plr.Character) then return false end
            pcall(function()
                part.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
                part.CanCollide = false
            end)
            return true
        end
        return false
    end

    local function addPart(part)
        if retainPart(part) and not table.find(parts, part) then
            table.insert(parts, part)
        end
    end

    local function removePart(part)
        local idx = table.find(parts, part)
        if idx then table.remove(parts, idx) end
    end

    for _, part in pairs(workspace:GetDescendants()) do addPart(part) end
    if partCon then partCon:Disconnect() end
    partCon = workspace.DescendantAdded:Connect(addPart)
    if removeCon then removeCon:Disconnect() end
    removeCon = workspace.DescendantRemoving:Connect(removePart)

    local function toggleMinimize()
        minimized = not minimized
        for _, c in pairs(controls) do
            c.Visible = not minimized
        end
        if minimized then
            mainFrame.Size = UDim2.new(0, 300, 0, 40)
        else
            mainFrame.Size = UDim2.new(0, 300, 0, 500)
        end
    end

    local function createGUI()
        if screenGui then return end
        screenGui = Instance.new("ScreenGui")
        screenGui.Name = "SuperRingPartsGUI"
        screenGui.ResetOnSpawn = false
        screenGui.Parent = plr:WaitForChild("PlayerGui")

        mainFrame = Instance.new("Frame")
        mainFrame.Size = UDim2.new(0, 300, 0, 500)
        mainFrame.Position = UDim2.new(0.5, -150, 0.5, -250)
        mainFrame.BorderSizePixel = 0
        mainFrame.Parent = screenGui
        mainFrame.Active = true
        mainFrame.Draggable = true

        local uiCorner = Instance.new("UICorner")
        uiCorner.CornerRadius = UDim.new(0, 20)
        uiCorner.Parent = mainFrame

        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 40)
        title.Text = "黑洞1"
        title.TextColor3 = Color3.fromRGB(0, 0, 0)
        title.BackgroundColor3 = Color3.fromRGB(0, 204, 204)
        title.Font = Enum.Font.Fondamento
        title.TextSize = 22
        title.Parent = mainFrame
        local titleCorner = Instance.new("UICorner")
        titleCorner.CornerRadius = UDim.new(0, 20)
        titleCorner.Parent = title

        local closeBtn = Instance.new("TextButton")
        closeBtn.Size = UDim2.new(0, 30, 0, 30)
        closeBtn.Position = UDim2.new(1, -35, 0, 5)
        closeBtn.Text = "✕"
        closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        closeBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        closeBtn.Font = Enum.Font.Fondamento
        closeBtn.TextSize = 15
        closeBtn.Parent = mainFrame
        local closeCorner = Instance.new("UICorner")
        closeCorner.CornerRadius = UDim.new(0, 15)
        closeCorner.Parent = closeBtn
        closeBtn.MouseButton1Click:Connect(function()
            if screenGui then screenGui:Destroy() screenGui = nil end
            _G.CloseBlackhole1 = true
            if _G.HyggUI and _G.HyggUI.toggleBlackhole then
                _G.HyggUI.toggleBlackhole(false)
            end
        end)

        local minBtn = Instance.new("TextButton")
        minBtn.Size = UDim2.new(0, 30, 0, 30)
        minBtn.Position = UDim2.new(1, -70, 0, 5)
        minBtn.Text = "－"
        minBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
        minBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 200)
        minBtn.Font = Enum.Font.Fondamento
        minBtn.TextSize = 20
        minBtn.Parent = mainFrame
        local minCorner = Instance.new("UICorner")
        minCorner.CornerRadius = UDim.new(0, 15)
        minCorner.Parent = minBtn
        minBtn.MouseButton1Click:Connect(toggleMinimize)

        local function createControl(labelText, configKey, posY, color, step, minVal, maxVal)
            step = step or 10
            minVal = minVal or 0
            maxVal = maxVal or 10000

            local bg = Instance.new("Frame")
            bg.Size = UDim2.new(0.9, 0, 0, 42)
            bg.Position = UDim2.new(0.05, 0, posY, 0)
            bg.BackgroundColor3 = color
            bg.BorderSizePixel = 0
            bg.Parent = mainFrame
            table.insert(controls, bg)
            local bgCorner = Instance.new("UICorner")
            bgCorner.CornerRadius = UDim.new(0, 8)
            bgCorner.Parent = bg

            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(0.45, 0, 1, 0)
            label.Position = UDim2.new(0.02, 0, 0, 0)
            label.Text = labelText
            label.TextColor3 = Color3.fromRGB(0, 0, 0)
            label.BackgroundTransparency = 1
            label.Font = Enum.Font.Fondamento
            label.TextSize = 18
            label.TextXAlignment = Enum.TextXAlignment.Left
            label.Parent = bg

            local display = Instance.new("TextLabel")
            display.Size = UDim2.new(0.2, 0, 1, 0)
            display.Position = UDim2.new(0.42, 0, 0, 0)
            display.Text = tostring(tonumber(config[configKey]) or 0)
            display.TextColor3 = Color3.fromRGB(0, 0, 0)
            display.BackgroundTransparency = 1
            display.Font = Enum.Font.Fondamento
            display.TextSize = 18
            display.Parent = bg

            local minusBtn = Instance.new("TextButton")
            minusBtn.Size = UDim2.new(0, 30, 0, 30)
            minusBtn.Position = UDim2.new(0.7, 0, 0.5, -15)
            minusBtn.Text = "-"
            minusBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
            minusBtn.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
            minusBtn.Font = Enum.Font.Fondamento
            minusBtn.TextSize = 20
            minusBtn.Parent = bg
            local mc = Instance.new("UICorner")
            mc.CornerRadius = UDim.new(0, 8)
            mc.Parent = minusBtn

            local plusBtn = Instance.new("TextButton")
            plusBtn.Size = UDim2.new(0, 30, 0, 30)
            plusBtn.Position = UDim2.new(0.82, 0, 0.5, -15)
            plusBtn.Text = "+"
            plusBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
            plusBtn.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
            plusBtn.Font = Enum.Font.Fondamento
            plusBtn.TextSize = 20
            plusBtn.Parent = bg
            local pc = Instance.new("UICorner")
            pc.CornerRadius = UDim.new(0, 8)
            pc.Parent = plusBtn

            local inputBox = Instance.new("TextBox")
            inputBox.Size = UDim2.new(0.2, 0, 0, 30)
            inputBox.Position = UDim2.new(0.42, 0, 0.5, -15)
            inputBox.Text = tostring(tonumber(config[configKey]) or 0)
            inputBox.TextColor3 = Color3.fromRGB(0, 0, 0)
            inputBox.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            inputBox.Font = Enum.Font.Fondamento
            inputBox.TextSize = 16
            inputBox.TextXAlignment = Enum.TextXAlignment.Center
            inputBox.PlaceholderText = "数值"
            inputBox.ClearTextOnFocus = false
            inputBox.Parent = bg
            local ibCorner = Instance.new("UICorner")
            ibCorner.CornerRadius = UDim.new(0, 6)
            ibCorner.Parent = inputBox

            local function updateDisplay()
                local val = tonumber(config[configKey]) or 0
                display.Text = tostring(val)
                inputBox.Text = tostring(val)
                local method = "set" .. configKey:gsub("^%l", string.upper)
                if enabled and _G.RingControl and _G.RingControl[method] then
                    _G.RingControl[method](val)
                end
            end

            minusBtn.MouseButton1Click:Connect(function()
                local newVal = math.max(minVal, (tonumber(config[configKey]) or 0) - step)
                config[configKey] = newVal
                updateDisplay()
            end)

            plusBtn.MouseButton1Click:Connect(function()
                local newVal = math.min(maxVal, (tonumber(config[configKey]) or 0) + step)
                config[configKey] = newVal
                updateDisplay()
            end)

            inputBox.FocusLost:Connect(function(enterPressed)
                if enterPressed then
                    local newVal = tonumber(inputBox.Text)
                    if newVal then
                        newVal = math.clamp(newVal, minVal, maxVal)
                        config[configKey] = newVal
                        updateDisplay()
                    else
                        local val = tonumber(config[configKey]) or 0
                        inputBox.Text = tostring(val)
                    end
                end
            end)
        end

        createControl("半径", "radius", 0.08, Color3.fromRGB(153, 153, 0), 10, 0, 1000)
        createControl("高度", "height", 0.22, Color3.fromRGB(153, 0, 153), 10, 0, 1000)
        createControl("转速", "rotationSpeed", 0.36, Color3.fromRGB(0, 153, 153), 1, 0, 200)
        createControl("吸力", "attractionStrength", 0.50, Color3.fromRGB(153, 0, 0), 100, 0, 50000)

        local hue = 0
        game:GetService("RunService").Heartbeat:Connect(function()
            if not mainFrame or not mainFrame.Parent then return end
            hue = (hue + 0.01) % 1
            mainFrame.BackgroundColor3 = Color3.fromHSV(hue, 1, 1)
        end)

        local textHue = 0
        game:GetService("RunService").Heartbeat:Connect(function()
            if not title or not title.Parent then return end
            textHue = (textHue + 0.01) % 1
            title.TextColor3 = Color3.fromHSV(textHue, 1, 1)
        end)
    end

    local function start()
        if con then return end
        enabled = true
        createGUI()
        con = game:GetService("RunService").Heartbeat:Connect(function()
            if not enabled then return end
            local root = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if not root then return end
            local center = root.Position
            for _, part in pairs(parts) do
                if part.Parent and not part.Anchored then
                    local pos = part.Position
                    local dist = (Vector3.new(pos.X, center.Y, pos.Z) - center).Magnitude
                    local angle = math.atan2(pos.Z - center.Z, pos.X - center.X)
                    local newAngle = angle + math.rad(config.rotationSpeed)
                    local targetPos = Vector3.new(
                        center.X + math.cos(newAngle) * math.min(config.radius, dist),
                        center.Y + (config.height * (math.abs(math.sin((pos.Y - center.Y) / config.height)))),
                        center.Z + math.sin(newAngle) * math.min(config.radius, dist)
                    )
                    local dir = (targetPos - part.Position).unit
                    part.Velocity = dir * config.attractionStrength
                end
            end
        end)
    end

    local function stop()
        enabled = false
        if con then con:Disconnect() con = nil end
        if screenGui then screenGui:Destroy() screenGui = nil end
        mainFrame = nil
        controls = {}
    end

    return {
        start = start,
        stop = stop,
        setRadius = function(v) config.radius = tonumber(v) or 50 end,
        setHeight = function(v) config.height = tonumber(v) or 100 end,
        setSpeed = function(v) config.rotationSpeed = tonumber(v) or 10 end,
        setStrength = function(v) config.attractionStrength = tonumber(v) or 1000 end,
    }
end

-- ================= 黑洞2 实现（原 9.txt） =================
local function createBlackhole2()
    local plr = game.Players.LocalPlayer
    local enabled = false
    local parts = {}
    local config = {radius = 50, height = 100, rotationSpeed = 10, attractionStrength = 1000}
    local con = nil
    local partCon = nil
    local removeCon = nil
    local screenGui = nil
    local mainFrame = nil
    local minimized = false
    local controls = {}

    local function retainPart(part)
        if part:IsA("BasePart") and not part.Anchored and part:IsDescendantOf(workspace) then
            if part.Parent == plr.Character or part:IsDescendantOf(plr.Character) then return false end
            pcall(function()
                part.CustomPhysicalProperties = PhysicalProperties.new(0, 0, 0, 0, 0)
                part.CanCollide = false
            end)
            return true
        end
        return false
    end

    local function addPart(part)
        if retainPart(part) and not table.find(parts, part) then
            table.insert(parts, part)
        end
    end

    local function removePart(part)
        local idx = table.find(parts, part)
        if idx then table.remove(parts, idx) end
    end

    for _, part in pairs(workspace:GetDescendants()) do addPart(part) end
    if partCon then partCon:Disconnect() end
    partCon = workspace.DescendantAdded:Connect(addPart)
    if removeCon then removeCon:Disconnect() end
    removeCon = workspace.DescendantRemoving:Connect(removePart)

    local function toggleMinimize()
        minimized = not minimized
        for _, c in pairs(controls) do
            c.Visible = not minimized
        end
        if minimized then
            mainFrame.Size = UDim2.new(0, 300, 0, 40)
        else
            mainFrame.Size = UDim2.new(0, 300, 0, 500)
        end
    end

    local function createGUI()
        if screenGui then return end
        screenGui = Instance.new("ScreenGui")
        screenGui.Name = "SuperRingPartsGUI2"
        screenGui.ResetOnSpawn = false
        screenGui.Parent = plr:WaitForChild("PlayerGui")

        mainFrame = Instance.new("Frame")
        mainFrame.Size = UDim2.new(0, 300, 0, 500)
        mainFrame.Position = UDim2.new(0.5, -150, 0.5, -250)
        mainFrame.BorderSizePixel = 0
        mainFrame.Parent = screenGui
        mainFrame.Active = true
        mainFrame.Draggable = true

        local uiCorner = Instance.new("UICorner")
        uiCorner.CornerRadius = UDim.new(0, 20)
        uiCorner.Parent = mainFrame

        local title = Instance.new("TextLabel")
        title.Size = UDim2.new(1, 0, 0, 40)
        title.Text = "黑洞2"
        title.TextColor3 = Color3.fromRGB(0, 0, 0)
        title.BackgroundColor3 = Color3.fromRGB(0, 204, 204)
        title.Font = Enum.Font.Fondamento
        title.TextSize = 22
        title.Parent = mainFrame
        local titleCorner = Instance.new("UICorner")
        titleCorner.CornerRadius = UDim.new(0, 20)
        titleCorner.Parent = title

        local closeBtn = Instance.new("TextButton")
        closeBtn.Size = UDim2.new(0, 30, 0, 30)
        closeBtn.Position = UDim2.new(1, -35, 0, 5)
        closeBtn.Text = "✕"
        closeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
        closeBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        closeBtn.Font = Enum.Font.Fondamento
        closeBtn.TextSize = 15
        closeBtn.Parent = mainFrame
        local closeCorner = Instance.new("UICorner")
        closeCorner.CornerRadius = UDim.new(0, 15)
        closeCorner.Parent = closeBtn
        closeBtn.MouseButton1Click:Connect(function()
            if screenGui then screenGui:Destroy() screenGui = nil end
            _G.CloseBlackhole2 = true
            if _G.HyggUI and _G.HyggUI.toggleBH2 then
                _G.HyggUI.toggleBH2(false)
            end
        end)

        local minBtn = Instance.new("TextButton")
        minBtn.Size = UDim2.new(0, 30, 0, 30)
        minBtn.Position = UDim2.new(1, -70, 0, 5)
        minBtn.Text = "－"
        minBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
        minBtn.BackgroundColor3 = Color3.fromRGB(255, 255, 200)
        minBtn.Font = Enum.Font.Fondamento
        minBtn.TextSize = 20
        minBtn.Parent = mainFrame
        local minCorner = Instance.new("UICorner")
        minCorner.CornerRadius = UDim.new(0, 15)
        minCorner.Parent = minBtn
        minBtn.MouseButton1Click:Connect(toggleMinimize)

        local function createControl(labelText, configKey, posY, color, step, minVal, maxVal)
            step = step or 10
            minVal = minVal or 0
            maxVal = maxVal or 10000

            local bg = Instance.new("Frame")
            bg.Size = UDim2.new(0.9, 0, 0, 42)
            bg.Position = UDim2.new(0.05, 0, posY, 0)
            bg.BackgroundColor3 = color
            bg.BorderSizePixel = 0
            bg.Parent = mainFrame
            table.insert(controls, bg)
            local bgCorner = Instance.new("UICorner")
            bgCorner.CornerRadius = UDim.new(0, 8)
            bgCorner.Parent = bg

            local label = Instance.new("TextLabel")
            label.Size = UDim2.new(0.45, 0, 1, 0)
            label.Position = UDim2.new(0.02, 0, 0, 0)
            label.Text = labelText
            label.TextColor3 = Color3.fromRGB(0, 0, 0)
            label.BackgroundTransparency = 1
            label.Font = Enum.Font.Fondamento
            label.TextSize = 18
            label.TextXAlignment = Enum.TextXAlignment.Left
            label.Parent = bg

            local display = Instance.new("TextLabel")
            display.Size = UDim2.new(0.2, 0, 1, 0)
            display.Position = UDim2.new(0.42, 0, 0, 0)
            display.Text = tostring(tonumber(config[configKey]) or 0)
            display.TextColor3 = Color3.fromRGB(0, 0, 0)
            display.BackgroundTransparency = 1
            display.Font = Enum.Font.Fondamento
            display.TextSize = 18
            display.Parent = bg

            local minusBtn = Instance.new("TextButton")
            minusBtn.Size = UDim2.new(0, 30, 0, 30)
            minusBtn.Position = UDim2.new(0.7, 0, 0.5, -15)
            minusBtn.Text = "-"
            minusBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
            minusBtn.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
            minusBtn.Font = Enum.Font.Fondamento
            minusBtn.TextSize = 20
            minusBtn.Parent = bg
            local mc = Instance.new("UICorner")
            mc.CornerRadius = UDim.new(0, 8)
            mc.Parent = minusBtn

            local plusBtn = Instance.new("TextButton")
            plusBtn.Size = UDim2.new(0, 30, 0, 30)
            plusBtn.Position = UDim2.new(0.82, 0, 0.5, -15)
            plusBtn.Text = "+"
            plusBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
            plusBtn.BackgroundColor3 = Color3.fromRGB(200, 200, 200)
            plusBtn.Font = Enum.Font.Fondamento
            plusBtn.TextSize = 20
            plusBtn.Parent = bg
            local pc = Instance.new("UICorner")
            pc.CornerRadius = UDim.new(0, 8)
            pc.Parent = plusBtn

            local inputBox = Instance.new("TextBox")
            inputBox.Size = UDim2.new(0.2, 0, 0, 30)
            inputBox.Position = UDim2.new(0.42, 0, 0.5, -15)
            inputBox.Text = tostring(tonumber(config[configKey]) or 0)
            inputBox.TextColor3 = Color3.fromRGB(0, 0, 0)
            inputBox.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
            inputBox.Font = Enum.Font.Fondamento
            inputBox.TextSize = 16
            inputBox.TextXAlignment = Enum.TextXAlignment.Center
            inputBox.PlaceholderText = "数值"
            inputBox.ClearTextOnFocus = false
            inputBox.Parent = bg
            local ibCorner = Instance.new("UICorner")
            ibCorner.CornerRadius = UDim.new(0, 6)
            ibCorner.Parent = inputBox

            local function updateDisplay()
                local val = tonumber(config[configKey]) or 0
                display.Text = tostring(val)
                inputBox.Text = tostring(val)
                local method = "set" .. configKey:gsub("^%l", string.upper)
                if enabled and _G.RingControl2 and _G.RingControl2[method] then
                    _G.RingControl2[method](val)
                end
            end

            minusBtn.MouseButton1Click:Connect(function()
                local newVal = math.max(minVal, (tonumber(config[configKey]) or 0) - step)
                config[configKey] = newVal
                updateDisplay()
            end)

            plusBtn.MouseButton1Click:Connect(function()
                local newVal = math.min(maxVal, (tonumber(config[configKey]) or 0) + step)
                config[configKey] = newVal
                updateDisplay()
            end)

            inputBox.FocusLost:Connect(function(enterPressed)
                if enterPressed then
                    local newVal = tonumber(inputBox.Text)
                    if newVal then
                        newVal = math.clamp(newVal, minVal, maxVal)
                        config[configKey] = newVal
                        updateDisplay()
                    else
                        local val = tonumber(config[configKey]) or 0
                        inputBox.Text = tostring(val)
                    end
                end
            end)
        end

        createControl("半径", "radius", 0.08, Color3.fromRGB(153, 153, 0), 10, 0, 10000)
        createControl("高度", "height", 0.22, Color3.fromRGB(153, 0, 153), 10, 0, 10000)
        createControl("转速", "rotationSpeed", 0.36, Color3.fromRGB(0, 153, 153), 1, 0, 10000)
        createControl("吸力", "attractionStrength", 0.50, Color3.fromRGB(153, 0, 0), 100, 0, 10000)

        local hue = 0
        game:GetService("RunService").Heartbeat:Connect(function()
            if not mainFrame or not mainFrame.Parent then return end
            hue = (hue + 0.01) % 1
            mainFrame.BackgroundColor3 = Color3.fromHSV(hue, 1, 1)
        end)

        local textHue = 0
        game:GetService("RunService").Heartbeat:Connect(function()
            if not title or not title.Parent then return end
            textHue = (textHue + 0.01) % 1
            title.TextColor3 = Color3.fromHSV(textHue, 1, 1)
        end)
    end

    local function start()
        if con then return end
        enabled = true
        createGUI()
        con = game:GetService("RunService").Heartbeat:Connect(function()
            if not enabled then return end
            local root = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart")
            if not root then return end
            local center = root.Position
            for _, part in pairs(parts) do
                if part.Parent and not part.Anchored then
                    local pos = part.Position
                    local dist = (Vector3.new(pos.X, center.Y, pos.Z) - center).Magnitude
                    local angle = math.atan2(pos.Z - center.Z, pos.X - center.X)
                    local newAngle = angle + math.rad(config.rotationSpeed)
                    local targetPos = Vector3.new(
                        center.X + math.cos(newAngle) * math.min(config.radius, dist),
                        center.Y + (config.height * (math.abs(math.sin((pos.Y - center.Y) / config.height)))),
                        center.Z + math.sin(newAngle) * math.min(config.radius, dist)
                    )
                    local dir = (targetPos - part.Position).unit
                    part.Velocity = dir * config.attractionStrength
                end
            end
        end)
    end

    local function stop()
        enabled = false
        if con then con:Disconnect() con = nil end
        if screenGui then screenGui:Destroy() screenGui = nil end
        mainFrame = nil
        controls = {}
    end

    return {
        start = start,
        stop = stop,
        setRadius = function(v) config.radius = tonumber(v) or 50 end,
        setHeight = function(v) config.height = tonumber(v) or 100 end,
        setSpeed = function(v) config.rotationSpeed = tonumber(v) or 10 end,
        setStrength = function(v) config.attractionStrength = tonumber(v) or 1000 end,
    }
end

-- ================= 主界面：选择器 + 互斥开关 =================
local blackholeTab = Window:Tab({Title = "黑洞", Icon = "circle"})

local bhConfig = {radius = 50, height = 100, speed = 10, strength = 1000}
local bh2Config = {radius = 50, height = 100, speed = 10, strength = 1000}
local bhRunning = false
local bh2Running = false
local bhApi = nil
local bh2Api = nil
local selectedBlackhole = "黑洞1"
local bhToggleControl = nil

local function startBlackhole1()
if not bhApi then
local success = pcall(function()
bhApi = createBlackhole1()
end)
if not success then
WindUI:Notify({Title = "黑洞1", Content = "启动失败", Duration = 3, Icon = "alert"})
end
end
if bhApi then
bhApi:setRadius(bhConfig.radius)
bhApi:setHeight(bhConfig.height)
bhApi:setSpeed(bhConfig.speed)
bhApi:setStrength(bhConfig.strength)
bhApi:start()
bhRunning = true
WindUI:Notify({Title = "黑洞1", Content = "已开启", Duration = 2, Icon = "check"})
end
end

local function stopBlackhole1(silent)
if bhApi then pcall(function() bhApi:stop() end) end
bhRunning = false
if not silent then
WindUI:Notify({Title = "黑洞1", Content = "已关闭", Duration = 2, Icon = "x"})
end
end

local function startBlackhole2()
if not bh2Api then
local success = pcall(function()
bh2Api = createBlackhole2()
end)
if not success then
WindUI:Notify({Title = "黑洞2", Content = "启动失败", Duration = 3, Icon = "alert"})
end
end
if bh2Api then
bh2Api:setRadius(bh2Config.radius)
bh2Api:setHeight(bh2Config.height)
bh2Api:setSpeed(bh2Config.speed)
bh2Api:setStrength(bh2Config.strength)
bh2Api:start()
bh2Running = true
WindUI:Notify({Title = "黑洞2", Content = "已开启", Duration = 2, Icon = "check"})
end
end

local function stopBlackhole2(silent)
if bh2Api then pcall(function() bh2Api:stop() end) end
bh2Running = false
if not silent then
WindUI:Notify({Title = "黑洞2", Content = "已关闭", Duration = 2, Icon = "x"})
end
end

-- 统一开关：只启动当前选择的黑洞，且互斥（开一个自动关另一个）
local function toggleBlackhole(val)
if val then
if selectedBlackhole == "黑洞1" then
if bh2Running then stopBlackhole2(true) end
startBlackhole1()
else
if bhRunning then stopBlackhole1(true) end
startBlackhole2()
end
else
if bhRunning then stopBlackhole1() end
if bh2Running then stopBlackhole2() end
end
end

-- 兼容悬浮窗关闭按钮的回调（内部会调用 _G.HyggUI.toggleBlackhole / toggleBH2）
_G.HyggUI = _G.HyggUI or {}
_G.HyggUI.toggleBlackhole = function(val)
if not val then stopBlackhole1() end
if bhToggleControl then pcall(function() bhToggleControl:Set(false) end) end
end
_G.HyggUI.toggleBH2 = function(val)
if not val then stopBlackhole2() end
if bhToggleControl then pcall(function() bhToggleControl:Set(false) end) end
end

local section = blackholeTab:Section({
Title = "黑洞",
Box = true,
})
section:Dropdown({
Title = "选择黑洞",
Values = {"黑洞1", "黑洞2"},
Value = "黑洞1",
Callback = function(selected)
selectedBlackhole = selected
if bhRunning or bh2Running then
toggleBlackhole(true)
end
end,
})
bhToggleControl = section:Toggle({
Title = "开关",
Value = false,
Callback = function(val) toggleBlackhole(val) end,
})

return blackholeTab

end
